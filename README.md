# Elliot Qx V4.4 - Bootstrap/Login Repair

First-time setup now creates the administrator and signed-in session in one request. Keep existing DB, FILES and BOOTSTRAP_TOKEN bindings. Upload all files to the repository root.
