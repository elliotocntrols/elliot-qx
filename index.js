const STATIC_ASSETS = new Map([
  ["/", { type: "text/html; charset=utf-8", body: "<!doctype html>\n<html lang=\"en\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\"><meta name=\"theme-color\" content=\"#14283d\"><meta name=\"description\" content=\"Elliot Qx - next-generation field QA, commissioning, BMS, systems completion and handover\"><link rel=\"manifest\" href=\"/manifest.webmanifest\"><link rel=\"icon\" href=\"/icons/qx.svg\"><link rel=\"stylesheet\" href=\"/styles.css?v=8.4.0\"><title>Elliot Qx V8.4 - Commissioning OS</title></head><body><div id=\"app\"><div class=\"boot\"><div class=\"brandmark\">QX</div><div><strong>Elliot Qx</strong><span>Loading commissioning workspace\u2026</span></div></div></div><script type=\"module\" src=\"/app.js?v=8.4.0\"></script></body></html>\n" }],
  ["/index.html", { type: "text/html; charset=utf-8", body: "<!doctype html>\n<html lang=\"en\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\"><meta name=\"theme-color\" content=\"#14283d\"><meta name=\"description\" content=\"Elliot Qx - next-generation field QA, commissioning, BMS, systems completion and handover\"><link rel=\"manifest\" href=\"/manifest.webmanifest\"><link rel=\"icon\" href=\"/icons/qx.svg\"><link rel=\"stylesheet\" href=\"/styles.css?v=8.4.0\"><title>Elliot Qx V8.4 - Commissioning OS</title></head><body><div id=\"app\"><div class=\"boot\"><div class=\"brandmark\">QX</div><div><strong>Elliot Qx</strong><span>Loading commissioning workspace\u2026</span></div></div></div><script type=\"module\" src=\"/app.js?v=8.4.0\"></script></body></html>\n" }],
  ["/app.js", { type: "application/javascript; charset=utf-8", body: "const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];\nconst esc=(v='')=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#039;'}[m]));\nconst fmtDate=v=>v?new Date(v).toLocaleDateString('en-AU',{day:'2-digit',month:'short',year:'numeric'}):'\u2014';\nconst fmtTime=v=>v?new Date(v).toLocaleString('en-AU',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}):'\u2014';\nconst pct=(n,d)=>d?Math.round(Number(n||0)*100/Number(d)):0;\nconst app=$('#app');\nconst state={user:null,view:'dashboard',projectId:'',projects:[],systems:[],equipment:[],templates:[],inspections:[],defects:[],points:[],turnover:[],witness:[],dashboard:{},users:[],actions:[],myWork:{actions:[],defects:[],inspections:[],witness:[]},matrix:[],files:[],query:'',filters:{},intelligence:null,copilot:null,documents:[],signoffs:[],notifications:[],drawings:[],v7ready:false,v8ready:false,portfolio:[],observations:[],milestones:[],equipmentReadiness:[],syncConflicts:[],offlinePackAt:null,smartForm:null};\n\nconst API_CACHE='qx-api-cache:'; const QUEUE_KEY='qx-offline-queue-v2';\nfunction getQueue(){try{return JSON.parse(localStorage.getItem(QUEUE_KEY)||'[]')}catch{return[]}}\nfunction setQueue(q){localStorage.setItem(QUEUE_KEY,JSON.stringify(q))}\nasync function api(path,options={}){const method=(options.method||'GET').toUpperCase();const headers={...(options.body&&!(options.body instanceof Blob)?{'content-type':'application/json'}:{}),...(options.headers||{})};let r;try{r=await fetch(path,{credentials:'same-origin',headers,...options})}catch(err){if(method==='GET'){try{const hit=JSON.parse(localStorage.getItem(API_CACHE+path)||'null');if(hit?.data)return hit.data}catch{}throw err}const queueable=['POST','PATCH','DELETE'].includes(method)&&typeof options.body==='string'&&!path.startsWith('/api/auth/')&&!path.startsWith('/api/setup/');if(queueable&&!navigator.onLine){const q=getQueue();q.push({id:crypto.randomUUID(),path,method,body:options.body,headers,queued_at:new Date().toISOString()});setQueue(q);updateQueueBadge();return {queued:true,offline:true}}throw err}let d=null;try{d=await r.json()}catch{}if(!r.ok)throw new Error(d?.error||`Request failed (${r.status})`);if(method==='GET'&&d)localStorage.setItem(API_CACHE+path,JSON.stringify({at:Date.now(),data:d}));return d}\nasync function flushQueue(){if(!navigator.onLine)return;const q=getQueue();if(!q.length)return;const remaining=[];for(const item of q){try{const r=await fetch(item.path,{method:item.method,body:item.body,credentials:'same-origin',headers:item.headers});if(!r.ok)remaining.push(item)}catch{remaining.push(item)}}setQueue(remaining);updateQueueBadge();if(q.length!==remaining.length){toast(`${q.length-remaining.length} offline update${q.length-remaining.length===1?'':'s'} synchronised`);try{await loadAll();render()}catch{}}}\nfunction updateQueueBadge(){const el=$('#networkState');if(!el)return;const n=getQueue().length;if(!navigator.onLine){el.textContent=`\u25cf OFFLINE${n?` \u2022 ${n} queued`:''}`;el.classList.add('off')}else{el.textContent=n?`\u25cf LIVE \u2022 ${n} syncing`:'\u25cf LIVE';el.classList.remove('off')}}\nfunction toast(msg){document.body.insertAdjacentHTML('beforeend',`<div class=\"toast\">${esc(msg)}</div>`);setTimeout(()=>$('.toast')?.remove(),2200)}\nfunction pill(v){const s=String(v||'not_started').replaceAll('_',' ');return `<span class=\"pill ${esc(String(v||''))}\">${esc(s)}</span>`}\nfunction progress(v){const n=Math.max(0,Math.min(100,Number(v||0)));return `<div class=\"readiness\"><div class=\"progress\"><i style=\"width:${n}%\"></i></div><b>${n}%</b></div>`}\nfunction currentProject(){return state.projects.find(p=>p.id===state.projectId)||null}\nfunction projectName(id){const p=state.projects.find(x=>x.id===id);return p?`${p.code} \u2014 ${p.name}`:'\u2014'}\nfunction filtered(rows){if(!state.projectId)return rows;return rows.filter(x=>x.project_id===state.projectId)}\n\nfunction navBtn(id,icon,label){return `<button class=\"navbtn ${state.view===id?'active':''}\" data-view=\"${id}\"><span class=\"navicon\">${icon}</span>${label}</button>`}\nfunction shell(content){const u=state.user||{};return `<div class=\"layout\"><aside class=\"sidebar\" id=\"sidebar\"><div class=\"brand\"><div class=\"brandmark\">QX</div><div><b>Elliot Qx</b><small>Commissioning OS V8.4</small></div></div><div class=\"navlabel\">Command</div>${navBtn('portfolio','\u25eb','Portfolio')}${navBtn('dashboard','\u25c8','Command Centre')}${navBtn('fieldhub','\u26a1','Field Hub')}${navBtn('mywork','\u2611','My Work')}${navBtn('matrix','\u25a6','Completion Matrix')}${navBtn('intelligence','\u25c6','Qx Intelligence')}${navBtn('notifications','\u25c9','Alerts & Escalations')}<div class=\"navlabel\">Project</div>${navBtn('projects','\u25a7','Projects')}${navBtn('systems','\u2318','Systems & Readiness')}${navBtn('equipment','\u25c7','Asset Passports')}${navBtn('builder','\u2726','Project Builder')}${navBtn('drawings','\u2317','Drawings & Markups')}${navBtn('planner','\u25f7','Commissioning Plan')}<div class=\"navlabel\">Field Execution</div>${navBtn('inspections','\u2713','QA & Commissioning')}${navBtn('points','\u21af','BMS I/O Checkout')}${navBtn('defects','!','Defects / Punch')}${navBtn('evidence','\u25eb','Evidence Hub')}${navBtn('observations','\u25cf','Field Observations')}<div class=\"navlabel\">Completion</div>${navBtn('witness','\u25ce','Witness & Hold Points')}${navBtn('turnover','\u25a3','Turnover Packages')}${navBtn('reports','\u25a4','Reports & Handover')}${navBtn('closeout','\u2726','Closeout Studio')}${navBtn('templates','\u2261','Template Library')}${navBtn('smarttemplates','\u2726','Smart Template Builder')}${u.role==='admin'?navBtn('admin','\u2699','Admin'):''}<div class=\"sidefoot\"><div class=\"userline\"><div class=\"avatar\">${esc((u.name||'U')[0])}</div><div><b>${esc(u.name||'User')}</b><small>${esc(u.role||'')}</small></div></div><button class=\"navbtn\" id=\"logout\" style=\"margin-top:8px\"><span class=\"navicon\">\u21aa</span>Sign out</button></div></aside><main class=\"main\"><header class=\"topbar\"><button class=\"btn btn-small mobile-menu\" id=\"menuBtn\">\u2630</button><select class=\"project-picker\" id=\"projectPicker\"><option value=\"\">All projects / portfolio</option>${state.projects.map(p=>`<option value=\"${p.id}\" ${p.id===state.projectId?'selected':''}>${esc(p.code)} \u2014 ${esc(p.name)}</option>`).join('')}</select><button class=\"btn btn-small\" id=\"scanAsset\">\u25a3 Scan asset</button><span class=\"online ${navigator.onLine?'':'off'}\" id=\"networkState\">${navigator.onLine?'\u25cf LIVE':'\u25cf OFFLINE'}</span></header><section class=\"content\">${content}</section></main></div>`}\nfunction hero(title,sub,actions=''){return `<div class=\"hero\"><div><div class=\"eyebrow\">${esc(currentProject()?.code||'ELLIOT CONTROLS')}</div><h1>${esc(title)}</h1><p>${esc(sub)}</p></div><div class=\"actions\">${actions}</div></div>`}\nfunction card(label,value,sub='',tone=''){return `<div class=\"card kpi\"><div class=\"label\">${label}</div><div class=\"metric ${tone}\">${value}</div><div class=\"sub\">${sub}</div></div>`}\n\nfunction renderLogin(initialized=true){app.innerHTML=`<div class=\"auth\"><div class=\"auth-card\"><div class=\"brandmark\">QX</div><h1>Elliot Qx</h1><p>${initialized?'QA, commissioning, systems completion and turnover \u2014 one source of truth from install to handover.':'Create the first secure administrator account.'}</p><form id=\"authForm\">${!initialized?`<div class=\"field\"><label>Your name</label><input name=\"name\" required></div>`:''}<div class=\"field\"><label>Email</label><input name=\"email\" type=\"email\" required></div><div class=\"field\"><label>Password</label><input name=\"password\" type=\"password\" minlength=\"12\" required></div><button class=\"btn btn-primary\" style=\"width:100%\">${initialized?'Sign in':'Create administrator'}</button></form><div id=\"authError\"></div></div></div>`;$('#authForm').onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));try{if(initialized){const r=await api('/api/auth/login',{method:'POST',body:JSON.stringify(data)});state.user=r.user;await loadAll();render()}else{const r=await api('/api/setup/bootstrap',{method:'POST',body:JSON.stringify(data)});state.user=r.user;await loadAll();render()}}catch(err){$('#authError').innerHTML=`<div class=\"error\" style=\"margin-top:12px\">${esc(err.message)}</div>`}}}\n\nasync function loadAll(){const q=state.projectId?`?project_id=${encodeURIComponent(state.projectId)}`:'';const [p,t,d,s,e,i,df,pts,top,w,u,a,mw,fs]=await Promise.all([api('/api/projects'),api('/api/templates'),api(`/api/dashboard${q}`),api(`/api/systems${q}`),api(`/api/equipment${q}`),api(`/api/inspections${q}`),api(`/api/defects${q}`),api(`/api/bms-points${q}`),api(`/api/turnover${q}`),api(`/api/witness${q}`),api('/api/users'),api(`/api/actions${q}`),api(`/api/my-work${q}`),state.projectId?api(`/api/files?project_id=${encodeURIComponent(state.projectId)}`):Promise.resolve({files:[]})]);state.projects=p.projects;state.templates=t.templates;state.dashboard=d;state.systems=s.systems;state.equipment=e.equipment;state.inspections=i.inspections;state.defects=df.defects;state.points=pts.points;state.turnover=top.packages;state.witness=w.witness;state.users=u.users;state.actions=a.actions||[];state.myWork=mw||{actions:[],defects:[],inspections:[],witness:[]};state.files=fs.files||[];state.matrix=[];if(state.projectId){try{state.matrix=(await api(`/api/completion-matrix?project_id=${encodeURIComponent(state.projectId)}`)).systems||[]}catch{state.matrix=[]}}}\nasync function boot(){try{setQueue(getQueue().filter(x=>!String(x.path||'').startsWith('/api/auth/')&&!String(x.path||'').startsWith('/api/setup/')));if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});const st=await api('/api/setup/status');if(!st.initialized)return renderLogin(false);const me=await api('/api/me');state.user=me.user;await loadAll();render();const asset=new URLSearchParams(location.search).get('asset');if(asset)setTimeout(()=>openAssetPassport(asset),50)}catch(e){renderLogin(true)}}\n\nfunction dashboardView(){const d=state.dashboard,eq=d.equipment||{},df=d.defects||{},ins=d.inspections||{},pts=d.points||{},top=d.turnover||{},wit=d.witness||{};const eqp=pct(eq.complete,eq.total),pointp=pct(pts.passed,pts.total),turn=Math.round(Number(top.avg_readiness||0));const p=currentProject();return `${hero(p?'Project Command Centre':'Portfolio Command Centre','Live readiness, blockers, field execution and handover position',`<button class=\"btn btn-primary\" id=\"quickTest\">+ Start field test</button><button class=\"btn\" id=\"quickDefect\">+ Defect</button>`)}<div class=\"banner\"><div style=\"font-size:22px\">\u26a1</div><div><strong>Commissioning Readiness Engine</strong><p>Qx measures actual readiness from equipment, test completion, BMS I/O, Category A punch items, witness points and turnover evidence \u2014 not just checklist percentages.</p></div></div><div class=\"cards\">${card('Equipment complete',`${eqp}%`,`${eq.complete||0} / ${eq.total||0} assets`,'green')}${card('BMS I/O proven',`${pointp}%`,`${pts.failed||0} failed points`,pts.failed?'red':'blue')}${card('Open punch',df.open||0,`${df.cat_a||0} Category A`,df.cat_a?'red':'amber')}${card('Witness holds',ins.witness||wit.requested||0,'Awaiting witness / approval','amber')}${card('Turnover readiness',`${turn}%`,`${top.ready||0} packages ready`,turn>85?'green':'blue')}${card('Live projects',d.projects?.active||0,`${d.projects?.total||0} total`,'blue')}</div><div class=\"grid2\"><div><div class=\"section-head\"><h3>${p?'System readiness':'Project readiness'}</h3><span>What is gating completion</span></div>${p?systemReadinessTable():projectTable(state.projects.slice(0,12))}</div><div><div class=\"section-head\"><h3>Recent field activity</h3><span>Audit trail</span></div><div class=\"card activity\">${(d.recent||[]).slice(0,10).map(a=>`<div class=\"activity-item\"><b>${esc(a.user_name||'System')}</b> ${esc(a.action.replaceAll('_',' '))} ${esc(a.entity_type)}<small>${fmtTime(a.created_at)}</small></div>`).join('')||'<div class=\"empty\">No activity yet</div>'}</div></div></div>`}\nfunction projectTable(rows){if(!rows.length)return '<div class=\"card empty\">No projects yet.</div>';return `<div class=\"table-wrap\"><table><thead><tr><th>Project</th><th>Status</th><th>Systems</th><th>Equipment</th><th>Cat A</th><th>Open punch</th><th>Turnover</th></tr></thead><tbody>${rows.map(p=>`<tr><td><strong>${esc(p.code)}</strong><br><span class=\"muted\">${esc(p.name)}</span></td><td>${pill(p.status)}</td><td>${p.system_count||0}</td><td>${p.equipment_count||0}</td><td>${p.cat_a||0}</td><td>${p.open_defects||0}</td><td>${progress(p.turnover_readiness||0)}</td></tr>`).join('')}</tbody></table></div>`}\nfunction systemReadinessTable(){if(!state.systems.length)return '<div class=\"card empty\">Create systems to start systemised completion tracking.</div>';return `<div class=\"table-wrap\"><table><thead><tr><th>System</th><th>Discipline</th><th>Status</th><th>Equipment</th><th>Open punch</th><th>Target</th></tr></thead><tbody>${state.systems.map(s=>`<tr><td><strong>${esc(s.code)}</strong><br><span class=\"muted\">${esc(s.name)}</span></td><td>${esc(s.discipline)}</td><td>${pill(s.status)}</td><td>${s.equipment_count||0}</td><td>${s.open_defects||0}</td><td>${fmtDate(s.target_date)}</td></tr>`).join('')}</tbody></table></div>`}\n\nasync function refreshIntelligence(){if(!state.projectId)return;try{state.intelligence=await api(`/api/readiness?project_id=${encodeURIComponent(state.projectId)}&snapshot=1`);render()}catch(e){toast(e.message)}}\nfunction intelligenceView(){if(!state.projectId)return `${hero('Qx Intelligence','Select a project to calculate real commissioning readiness')}<div class=\"card empty\">Choose a project from the top bar. Qx will calculate blockers across QA, BMS I/O, punch, witness and turnover.</div>`;const r=state.intelligence; if(!r){setTimeout(refreshIntelligence,0);return `${hero('Qx Intelligence','Calculating project readiness and completion blockers')}<div class=\"card empty\">Analysing live project data\u2026</div>`}return `${hero('Qx Intelligence','The shortest path from today to energisation, witness and handover','<button class=\"btn btn-primary\" id=\"refreshIntel\">Recalculate readiness</button>')}<div class=\"intel-grid\"><div class=\"card\"><div class=\"section-head\"><h3>Readiness score</h3><span>${esc(r.grade)}</span></div><div class=\"readiness-ring\" style=\"--p:${r.score}\"><strong>${r.score}%</strong></div><p class=\"muted\" style=\"text-align:center;margin-top:14px\">Weighted from live QA, Category A punch, BMS I/O, witness, turnover and asset completion.</p></div><div class=\"card\"><div class=\"section-head\"><h3>Completion blockers</h3><span>${r.blockers.length} active</span></div>${r.blockers.length?r.blockers.map(b=>`<div class=\"blocker\"><span class=\"sev ${esc(b.severity)}\"></span><div><b>${esc(b.label)}</b><small>${esc(b.code.replaceAll('_',' '))}</small></div></div>`).join(''):'<div class=\"empty\">No blocking conditions detected. Validate required evidence and authorised sign-offs before handover.</div>'}</div></div><div class=\"section-head\"><h3>Why Qx wins for Elliot</h3><span>Built around field delivery</span></div><div class=\"cards\"><div class=\"switch-reason\"><b>One completion graph</b><span class=\"muted\">Equipment, QA, I/O, punch, witness and turnover use the same live project structure.</span></div><div class=\"switch-reason\"><b>BMS-native</b><span class=\"muted\">Bulk point lists and four-stage I/O proof are first-class workflows, not generic forms.</span></div><div class=\"switch-reason\"><b>Automatic blockers</b><span class=\"muted\">PMs see why a system cannot be handed over instead of interpreting dozens of reports.</span></div><div class=\"switch-reason\"><b>Evidence at the asset</b><span class=\"muted\">Photos, tests, defects and sign-off remain tied to the equipment and system.</span></div></div>`}\n\nfunction projectsView(){return `${hero('Projects','Portfolio, commissioning ownership and completion dates','<button class=\"btn btn-primary\" id=\"addProject\">+ New project</button>')}${projectTable(state.projects)}`}\nfunction systemsView(){return `${hero('Systems & Readiness','Systemise the job exactly how it will be commissioned and handed over','<button class=\"btn btn-primary\" id=\"addSystem\">+ Add system</button><button class=\"btn\" id=\"addTurnover\">+ Turnover package</button>')}${systemReadinessTable()}`}\nfunction equipmentView(){const rows=state.equipment;return `${hero('Asset Passports','Every asset linked to QA, tests, punch, I/O, evidence and sign-off','<button class=\"btn btn-primary\" id=\"addEquipment\">+ Add equipment</button><button class=\"btn\" id=\"importEquipment\">\u21e7 Import CSV</button>')}<div class=\"toolbar\"><input class=\"search\" id=\"searchBox\" placeholder=\"Search tag, equipment, system, location\u2026\" value=\"${esc(state.query)}\"><select class=\"mini-select\" id=\"eqStatus\"><option value=\"\">All statuses</option><option>not_started</option><option>qa</option><option>commissioning</option><option>witness</option><option>blocked</option><option>complete</option></select></div>${equipmentTable(rows)}`}\nfunction equipmentTable(rows){const q=state.query.toLowerCase(),st=state.filters.eqStatus||'';const list=rows.filter(e=>(!q||[e.tag,e.name,e.system,e.subsystem,e.building,e.level,e.area].join(' ').toLowerCase().includes(q))&&(!st||e.status===st));if(!list.length)return '<div class=\"card empty\">No matching equipment.</div>';return `<div class=\"table-wrap\"><table><thead><tr><th>Tag</th><th>Equipment</th><th>System</th><th>Location</th><th>Status</th><th>Approved tests</th><th>Punch</th><th>QR</th></tr></thead><tbody>${list.map(e=>`<tr><td><strong>${esc(e.tag)}</strong></td><td>${esc(e.name)}<br><span class=\"muted\">${esc([e.manufacturer,e.model].filter(Boolean).join(' ')||e.equipment_type||'')}</span></td><td>${esc(e.system||'\u2014')}<br><span class=\"muted\">${esc(e.subsystem||'')}</span></td><td>${esc([e.building,e.level,e.area].filter(Boolean).join(' / ')||'\u2014')}</td><td>${pill(e.status)}</td><td>${e.approved_tests||0}</td><td>${e.open_defects||0}</td><td><button class=\"btn btn-small\" data-passport=\"${e.id}\">Open</button></td></tr>`).join('')}</tbody></table></div>`}\n\nfunction inspectionsView(){return `${hero('QA & Commissioning','Installation QA, ITPs, PFCs, FPTs and integrated testing','<button class=\"btn btn-primary\" id=\"addInspection\">+ Start test</button>')}<div class=\"toolbar\"><input class=\"search\" id=\"searchBox\" placeholder=\"Search tests, asset tags, templates\u2026\" value=\"${esc(state.query)}\"></div>${inspectionTable()}`}\nfunction inspectionTable(){const q=state.query.toLowerCase(),list=state.inspections.filter(i=>!q||[i.title,i.equipment_tag,i.template_name,i.discipline].join(' ').toLowerCase().includes(q));if(!list.length)return '<div class=\"card empty\">No QA or commissioning tests yet.</div>';return `<div class=\"table-wrap\"><table><thead><tr><th>Test / Inspection</th><th>Asset</th><th>Stage</th><th>Progress</th><th>Fails</th><th>Status</th><th>Updated</th></tr></thead><tbody>${list.map(i=>`<tr data-inspection=\"${i.id}\" style=\"cursor:pointer\"><td><strong>${esc(i.title)}</strong><br><span class=\"muted\">${esc(i.template_name)}</span></td><td>${esc(i.equipment_tag||'Project / system')}</td><td>${pill(i.stage)}</td><td>${progress(pct(i.answered,i.total_lines))}</td><td>${i.fails||0}</td><td>${pill(i.status)}</td><td>${fmtTime(i.updated_at)}</td></tr>`).join('')}</tbody></table></div>`}\n\nfunction pointsView(){const points=state.points,q=state.query.toLowerCase(),list=points.filter(p=>!q||[p.controller,p.point_name,p.point_type,p.equipment_tag,p.io_address,p.cable_number].join(' ').toLowerCase().includes(q));const pass=list.filter(p=>p.overall_status==='pass').length,fail=list.filter(p=>p.overall_status==='fail').length;return `${hero('BMS I/O Checkout','Bulk point-to-point verification built for controls contractors','<button class=\"btn btn-primary\" id=\"importPoints\">\u21e7 Import CSV</button>')}<div class=\"cards\" style=\"grid-template-columns:repeat(4,1fr)\">${card('Points',list.length,'Current register','blue')}${card('Proven',pass,`${pct(pass,list.length)}% complete`,'green')}${card('Failed',fail,'Requires rectification',fail?'red':'')}${card('Untested',Math.max(0,list.length-pass-fail),'Field workload','amber')}</div><div class=\"toolbar\"><input class=\"search\" id=\"searchBox\" placeholder=\"Search controller, point, address, cable\u2026\" value=\"${esc(state.query)}\"></div>${list.length?`<div class=\"table-wrap\"><table><thead><tr><th>Controller / Point</th><th>Type</th><th>Asset</th><th>Address</th><th>Field</th><th>Controller</th><th>Graphics</th><th>Alarm</th><th>Overall</th></tr></thead><tbody>${list.slice(0,1000).map(p=>`<tr data-point=\"${p.id}\" style=\"cursor:pointer\"><td><span class=\"muted\">${esc(p.controller||'')}</span><br><strong>${esc(p.point_name)}</strong></td><td>${pill(p.point_type)}</td><td>${esc(p.equipment_tag||'\u2014')}</td><td>${esc(p.io_address||'\u2014')}</td><td>${pill(p.field_status)}</td><td>${pill(p.controller_status)}</td><td>${pill(p.graphics_status)}</td><td>${pill(p.alarm_status)}</td><td>${pill(p.overall_status)}</td></tr>`).join('')}</tbody></table></div>`:'<div class=\"card empty\">No BMS points yet. Import a CSV from your points list or I/O schedule.</div>'}`}\n\nfunction defectsView(){const q=state.query.toLowerCase(),list=state.defects.filter(d=>!q||[d.title,d.description,d.equipment_tag,d.discipline,d.assigned_name].join(' ').toLowerCase().includes(q));const a=list.filter(d=>d.category==='A'&&!['closed','verified'].includes(d.status)).length;return `${hero('Defects & Punch','A/B/C completion punch with rectification evidence and verification','<button class=\"btn btn-primary\" id=\"addDefect\">+ Raise defect</button>')}<div class=\"cards\" style=\"grid-template-columns:repeat(4,1fr)\">${card('Category A',a,'Blocks energisation / turnover',a?'red':'green')}${card('Open',list.filter(d=>!['closed','verified'].includes(d.status)).length,'All active punch','amber')}${card('Rectified',list.filter(d=>d.status==='rectified').length,'Awaiting verification','blue')}${card('Closed',list.filter(d=>['closed','verified'].includes(d.status)).length,'Verified evidence','green')}</div><div class=\"toolbar\"><input class=\"search\" id=\"searchBox\" placeholder=\"Search defect, asset, owner\u2026\" value=\"${esc(state.query)}\"></div>${list.length?`<div class=\"table-wrap\"><table><thead><tr><th>Cat</th><th>Defect</th><th>Asset</th><th>Discipline</th><th>Severity</th><th>Status</th><th>Owner</th><th>Due</th></tr></thead><tbody>${list.map(d=>`<tr data-defect=\"${d.id}\" style=\"cursor:pointer\"><td>${pill(d.category?.toLowerCase())}</td><td><strong>${esc(d.title)}</strong><br><span class=\"muted\">${esc(d.description||'')}</span></td><td>${esc(d.equipment_tag||'\u2014')}</td><td>${esc(d.discipline)}</td><td>${pill(d.severity)}</td><td>${pill(d.status)}</td><td>${esc(d.assigned_name||'Unassigned')}</td><td>${fmtDate(d.due_date)}</td></tr>`).join('')}</tbody></table></div>`:'<div class=\"card empty\">No defects.</div>'}`}\n\nfunction witnessView(){return `${hero('Witness & Hold Points','Control client/consultant witness requests and approval gates','<button class=\"btn btn-primary\" id=\"addWitness\">+ Request witness</button>')}${state.witness.length?`<div class=\"table-wrap\"><table><thead><tr><th>Requested</th><th>System</th><th>Test / Hold Point</th><th>Witness party</th><th>Status</th><th>Outcome</th></tr></thead><tbody>${state.witness.map(w=>`<tr><td><strong>${fmtDate(w.requested_for)}</strong></td><td>${esc(w.system_code||'\u2014')}</td><td>${esc(w.inspection_title||'System witness')}</td><td>${esc(w.witness_party||'Client / consultant')}</td><td>${pill(w.status)}</td><td>${esc(w.outcome||'\u2014')}</td></tr>`).join('')}</tbody></table></div>`:'<div class=\"card empty\">No witness requests yet.</div>'}`}\n\nfunction turnoverView(){const avg=state.turnover.length?Math.round(state.turnover.reduce((a,b)=>a+Number(b.readiness_percent||0),0)/state.turnover.length):0;return `${hero('Turnover Packages','Automatic closeout dossier readiness by system','<button class=\"btn btn-primary\" id=\"addTurnover\">+ New package</button>')}<div class=\"banner\"><div style=\"font-size:22px\">\u25a3</div><div><strong>Handover without the end-of-job scramble</strong><p>Each package tracks as-builts, QA records, electrical tests, BMS I/O, functional tests, witness records, punch closure, O&M, backups and training evidence.</p></div></div><div class=\"cards\" style=\"grid-template-columns:repeat(3,1fr)\">${card('Portfolio readiness',`${avg}%`,'Across current packages',avg>85?'green':'blue')}${card('Ready packages',state.turnover.filter(x=>['ready','approved'].includes(x.status)).length,'Ready for acceptance','green')}${card('Incomplete',state.turnover.filter(x=>!['ready','approved'].includes(x.status)).length,'Outstanding dossier items','amber')}</div>${state.turnover.length?`<div class=\"grid3\">${state.turnover.map(t=>`<div class=\"card\" data-turnover=\"${t.id}\" style=\"cursor:pointer\"><div class=\"eyebrow\">${esc(t.system_code||'PROJECT')}</div><h3 style=\"margin:7px 0 3px\">${esc(t.name)}</h3><div class=\"muted\" style=\"font-size:11px\">${esc(t.system_name||'Project-wide package')}</div><div style=\"display:flex;justify-content:space-between;align-items:center;margin-top:17px\"><div class=\"score-ring\" style=\"--p:${Number(t.readiness_percent||0)}\"><b>${Number(t.readiness_percent||0)}%</b></div><div style=\"text-align:right\">${pill(t.status)}<div class=\"muted\" style=\"margin-top:8px;font-size:11px\">${t.complete_items||0}/${t.required_items||0} required items</div></div></div></div>`).join('')}</div>`:'<div class=\"card empty\">Create the first turnover package for a system.</div>'}`}\n\n\nfunction myWorkView(){const m=state.myWork||{},items=[...(m.defects||[]).map(x=>({kind:'Punch',title:x.title,project:x.project_code,asset:x.equipment_tag,status:x.status,due:x.due_date,priority:x.category==='A'?'critical':'high',id:x.id,open:'defect'})),...(m.inspections||[]).map(x=>({kind:'Test',title:x.title,project:x.project_code,asset:x.equipment_tag,status:x.status,due:x.updated_at,priority:'normal',id:x.id,open:'inspection'})),...(m.actions||[]).map(x=>({kind:'Action',title:x.title,project:x.project_code,asset:x.equipment_tag,status:x.status,due:x.due_at,priority:x.priority||'normal',id:x.id,open:'action'}))];return `${hero('My Work','One field queue: what you personally need to complete next','<button class=\"btn btn-primary\" id=\"quickTest\">+ Start field test</button>')}<div class=\"cards\" style=\"grid-template-columns:repeat(4,1fr)\">${card('Assigned tests',(m.inspections||[]).length,'Open QA / commissioning','blue')}${card('Assigned punch',(m.defects||[]).length,'Items requiring action',(m.defects||[]).some(x=>x.category==='A')?'red':'amber')}${card('Actions',(m.actions||[]).length,'PM / field actions','amber')}${card('Witness upcoming',(m.witness||[]).length,'Requested / scheduled','green')}</div>${items.length?`<div class=\"table-wrap\"><table><thead><tr><th>Priority</th><th>Type</th><th>Work item</th><th>Project</th><th>Asset</th><th>Status</th><th>Due / updated</th></tr></thead><tbody>${items.map(x=>`<tr ${x.open==='defect'?`data-defect=\"${x.id}\"`:x.open==='inspection'?`data-inspection=\"${x.id}\"`:''} style=\"cursor:${x.open==='action'?'default':'pointer'}\"><td>${pill(x.priority)}</td><td>${esc(x.kind)}</td><td><strong>${esc(x.title)}</strong></td><td>${esc(x.project||'\u2014')}</td><td>${esc(x.asset||'\u2014')}</td><td>${pill(x.status)}</td><td>${fmtDate(x.due)}</td></tr>`).join('')}</tbody></table></div>`:'<div class=\"card empty\">Nothing assigned to you. Your personal field queue is clear.</div>'}`}\n\nfunction matrixCell(v,label){const n=Math.max(0,Math.min(100,Number(v||0)));const cls=n===100?'matrix-good':n>=75?'matrix-near':n>0?'matrix-risk':'matrix-empty';return `<div class=\"matrix-cell ${cls}\"><b>${n}%</b><span>${esc(label)}</span></div>`}\nfunction matrixView(){if(!state.projectId)return `${hero('Completion Matrix','Select a project to see every system and every readiness gate')}<div class=\"card empty\">The matrix becomes the PM/Cx meeting screen: system by system, install through turnover.</div>`;return `${hero('Completion Matrix','One live matrix from installation to handover','<button class=\"btn\" id=\"refreshMatrix\">Refresh matrix</button>')}<div class=\"banner\"><div>\u25a6</div><div><strong>No more spreadsheet completion trackers.</strong><p>Every system is scored from equipment completion, approved QA/Cx tests, BMS proof, Category A punch and turnover evidence.</p></div></div>${state.matrix.length?`<div class=\"table-wrap matrix-wrap\"><table><thead><tr><th>System</th><th>Install</th><th>QA / Cx</th><th>BMS I/O</th><th>Cat A</th><th>Turnover</th><th>Readiness</th></tr></thead><tbody>${state.matrix.map(s=>`<tr><td><strong>${esc(s.code)}</strong><br><span class=\"muted\">${esc(s.name)}</span></td><td>${matrixCell(s.install_percent,'install')}</td><td>${matrixCell(s.qa_percent,'tests')}</td><td>${matrixCell(s.io_percent,'I/O')}</td><td>${s.punch_clear?'<div class=\"gate-pass\">CLEAR</div>':`<div class=\"gate-block\">${Number(s.cat_a||0)} OPEN</div>`}</td><td>${matrixCell(s.turnover_percent,'handover')}</td><td><div class=\"score-big ${Number(s.readiness_score)>=90?'green':Number(s.readiness_score)>=70?'amber':'red'}\">${Number(s.readiness_score||0)}%</div></td></tr>`).join('')}</tbody></table></div>`:'<div class=\"card empty\">Create commissioning systems to activate the live completion matrix.</div>'}`}\n\nfunction evidenceView(){if(!state.projectId)return `${hero('Evidence Hub','Select a project to browse and capture field evidence')}<div class=\"card empty\">Photos, test evidence and closeout files are stored privately in R2 and linked to the asset or record.</div>`;return `${hero('Evidence Hub','Photos and documents tied to the asset, test and defect','<button class=\"btn btn-primary\" id=\"captureEvidence\">+ Capture / upload evidence</button>')}<div class=\"cards\" style=\"grid-template-columns:repeat(3,1fr)\">${card('Evidence files',state.files.length,'Stored in private R2','blue')}${card('Linked to assets',state.files.filter(f=>f.equipment_id).length,'Asset passport evidence','green')}${card('Today',state.files.filter(f=>String(f.created_at||'').slice(0,10)===new Date().toISOString().slice(0,10)).length,'Captured today','amber')}</div>${state.files.length?`<div class=\"evidence-grid\">${state.files.map(f=>`<a class=\"evidence-card\" href=\"/api/files/${f.id}\" target=\"_blank\"><div class=\"file-icon\">${String(f.content_type||'').startsWith('image/')?'\u25a7':'\u25a4'}</div><div><strong>${esc(f.file_name)}</strong><span>${esc(f.equipment_tag||'Project evidence')} \u2022 ${fmtTime(f.created_at)}</span><small>${esc(f.uploaded_by_name||'')}</small></div></a>`).join('')}</div>`:'<div class=\"card empty\">No evidence uploaded yet. Use a phone camera to capture the first photo.</div>'}`}\n\nfunction builderView(){return `${hero('Qx Project Builder','Turn schedules and scope text into a structured commissioning register','<button class=\"btn\" id=\"importEquipment\">\u21e7 Import equipment CSV</button>')}<div class=\"grid2\"><div class=\"card\"><div class=\"eyebrow\">SMART SETUP</div><h3>Equipment / scope extractor</h3><p class=\"muted\">Paste equipment schedules, scope text or a cleaned drawing extract. Qx identifies common HVAC, electrical and BMS tags and creates a reviewable proposal before anything is applied.</p><div class=\"field\"><label>Source text</label><textarea id=\"builderText\" style=\"min-height:260px\" placeholder=\"Paste equipment schedule, project scope, sequence summary or drawing text here\u2026\"></textarea></div><button class=\"btn btn-primary\" id=\"runBuilder\">Analyse project text</button></div><div class=\"card\" id=\"builderResult\"><div class=\"empty\">Run the builder to generate a proposed equipment structure. Human review remains mandatory against approved project documents.</div></div></div>`}\n\nfunction closeoutView(){if(!state.projectId)return `${hero('Closeout Studio','Select a project to build the digital handover package')}<div class=\"card empty\">Choose a project first.</div>`;const c=state.copilot||{};return `${hero('Closeout Studio','Digital signoff, controlled documents, live client view and one-click handover dossier','<button class=\"btn btn-primary\" id=\"openDossier\">Generate Handover Dossier</button><button class=\"btn\" id=\"createShare\">Create Client Link</button>')}<div class=\"cards\">${card('Qx forecast',esc(c.forecast||'Analyse project'),`${c.score??'\u2014'} readiness score`,'blue')}${card('Documents',state.documents.length,'Controlled closeout records','green')}${card('Digital signoffs',state.signoffs.length,'Witness / acceptance records','green')}${card('Priority actions',(c.recommendations||[]).length,'Recommended next moves',(c.recommendations||[]).length?'amber':'green')}</div><div class=\"grid2\"><div class=\"card\"><div class=\"section-head\"><h3>Commissioning Copilot</h3><button class=\"btn btn-small\" id=\"refreshCopilot\">Refresh analysis</button></div>${(c.recommendations||[]).length?(c.recommendations||[]).slice(0,8).map((x,i)=>`<div class=\"action-row\"><div class=\"priority\">${i+1}</div><div><b>${esc(x.title)}</b><p>${esc(x.why)}</p></div><button class=\"btn btn-small\" data-route=\"${esc(x.route)}\">Open</button></div>`).join(''):'<div class=\"empty\">Run the analysis to get the next highest-value commissioning actions.</div>'}</div><div class=\"card\"><div class=\"section-head\"><h3>Controlled documents</h3><button class=\"btn btn-small\" id=\"addDocument\">+ Add document</button></div>${state.documents.slice(0,12).map(d=>`<div class=\"file-row\"><div><b>${esc(d.title)}</b><small>${esc(d.doc_type)} \u2022 Rev ${esc(d.revision||'\u2014')} \u2022 ${esc(d.status)}</small></div>${d.object_file_id?`<a class=\"btn btn-small\" target=\"_blank\" href=\"/api/files/${d.object_file_id}\">Open</a>`:''}</div>`).join('')||'<div class=\"empty\">No controlled documents registered yet.</div>'}</div></div><div class=\"grid2\"><div class=\"card\"><div class=\"section-head\"><h3>Digital acceptance</h3><button class=\"btn btn-small\" id=\"projectSignoff\">+ Project signoff</button></div>${state.signoffs.slice(0,10).map(s=>`<div class=\"file-row\"><div><b>${esc(s.signer_name)}</b><small>${esc(s.signoff_type)} \u2022 ${esc(s.signer_company||'')} \u2022 ${fmtTime(s.signed_at)}</small></div>${s.signature_data?'<span class=\"pill approved\">SIGNED</span>':''}</div>`).join('')||'<div class=\"empty\">No digital signoffs yet.</div>'}</div><div class=\"card\"><div class=\"eyebrow\">ONE SOURCE OF TRUTH</div><h3>Automatic handover dossier</h3><p class=\"muted\">Qx compiles systems, equipment, QA/Cx records, punch, BMS I/O, turnover, document register and signatures into one printable dossier. Use Print / Save PDF to issue a controlled PDF without rebuilding spreadsheets.</p><button class=\"btn btn-primary\" id=\"openDossier2\">Open live dossier</button></div></div>`}\n\nfunction reportsView(){if(!state.projectId)return `${hero('Reports & Handover','Select a project to generate live commissioning status reports')}<div class=\"card empty\">Choose a project to create a live executive/commissioning report.</div>`;return `${hero('Reports & Handover','Generate current status without rebuilding spreadsheets','<button class=\"btn btn-primary\" id=\"printReport\">Print / Save PDF</button><button class=\"btn\" id=\"exportCsv\">Export equipment CSV</button>')}<div id=\"reportSheet\" class=\"report-sheet\"><div class=\"report-brand\"><div class=\"brandmark\">QX</div><div><h2>${esc(currentProject()?.code||'')} \u2014 ${esc(currentProject()?.name||'')}</h2><p>Commissioning Status & Operational Readiness Report</p></div></div>${dashboardView().replace(/<div class=\"hero\">[\\s\\S]*?<\\/div><\\/div>/,'')}</div>`}\n\nfunction adminView(){if(state.user?.role!=='admin')return `${hero('Admin','Administrator access required')}<div class=\"card empty\">You do not have administrator permission.</div>`;return `${hero('Administration','Users, access and company commissioning standards','<button class=\"btn btn-primary\" id=\"addUser\">+ Add user</button>')}<div class=\"cards\" style=\"grid-template-columns:repeat(4,1fr)\">${card('Users',state.users.length,'Active Qx accounts','blue')}${card('Admins',state.users.filter(u=>u.role==='admin').length,'Platform administrators','green')}${card('Engineers',state.users.filter(u=>u.role==='engineer').length,'Engineering / Cx','blue')}${card('Technicians',state.users.filter(u=>u.role==='technician').length,'Field users','amber')}</div><div class=\"table-wrap\"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Created</th></tr></thead><tbody>${state.users.map(u=>`<tr><td><strong>${esc(u.name)}</strong></td><td>${esc(u.email)}</td><td>${pill(u.role)}</td><td>${u.active?pill('active'):pill('inactive')}</td><td>${fmtDate(u.created_at)}</td></tr>`).join('')}</tbody></table></div>`}\n\nfunction templatesView(){const canEdit=['admin','pm','engineer'].includes(state.user?.role);const list=state.templates;return `${hero('Template Library','Company-controlled QA, ITP, PFC, FPT and IST standards',canEdit?'<button class=\"btn btn-primary\" id=\"newTemplate\">+ New template</button>':'')}<div class=\"banner\"><div>\u2261</div><div><strong>One controlled library \u2014 reusable on every project</strong><p>Open any template to review its checks, hold points and response types. Engineers and PMs can create, edit, duplicate and retire company standards.</p></div></div>${list.length?`<div class=\"grid3\">${list.map(t=>`<button class=\"card template-card\" data-template=\"${t.id}\" style=\"text-align:left;color:inherit;cursor:pointer\"><div style=\"display:flex;justify-content:space-between;gap:10px\"><div class=\"eyebrow\">${esc(t.discipline)} \u2022 ${esc(t.stage)}</div>${pill('v'+(t.version||1))}</div><h3 style=\"margin:8px 0\">${esc(t.name)}</h3><p class=\"muted\" style=\"font-size:12px;line-height:1.5;min-height:55px\">${esc(t.description||'No description')}</p><div class=\"mini-metrics\"><span><b>${Number(t.item_count||0)}</b> checks</span><span><b>${Number(t.hold_points||0)}</b> hold points</span></div><div style=\"margin-top:12px;color:#6ed9ff;font-size:11px;font-weight:800\">Open template \u2192</div></button>`).join('')}</div>`:'<div class=\"card empty\">No templates are active. Create the first company standard.</div>'}`}\n\nasync function openTemplate(tid){try{const d=await api(`/api/templates/${tid}`);const t=d.template,items=d.items||[];const canEdit=['admin','pm','engineer'].includes(state.user?.role);modal(t.name,`<div class=\"banner\"><div>\u2261</div><div><strong>${esc(t.discipline)} \u2022 ${esc(t.stage)} \u2022 Version ${Number(t.version||1)}</strong><p>${esc(t.description||'')}</p></div></div><div class=\"actions\" style=\"margin-bottom:12px\">${state.projectId?'<button class=\"btn btn-primary\" id=\"startFromTemplate\">\u25b6 Start test</button>':''}${canEdit?'<button class=\"btn\" id=\"editTemplateMeta\">Edit details</button><button class=\"btn\" id=\"duplicateTemplate\">Duplicate</button><button class=\"btn\" id=\"addTemplateItem\">+ Add check</button><button class=\"btn\" id=\"archiveTemplate\">Archive</button>':''}</div>${items.length?`<div class=\"table-wrap\"><table><thead><tr><th>#</th><th>Section</th><th>Check / Requirement</th><th>Response</th><th>Mandatory</th><th>Hold</th>${canEdit?'<th></th>':''}</tr></thead><tbody>${items.map((x,i)=>`<tr><td>${i+1}</td><td>${esc(x.section||'Checks')}</td><td><strong>${esc(x.item_text)}</strong></td><td>${esc((x.response_type||'pass_fail_na').replaceAll('_',' '))}</td><td>${x.mandatory?'Yes':'No'}</td><td>${x.hold_point?'<span class=\"pill fail\">HOLD</span>':'\u2014'}</td>${canEdit?`<td><button class=\"btn btn-small\" data-edit-template-item=\"${x.id}\">Edit</button></td>`:''}</tr>`).join('')}</tbody></table></div>`:'<div class=\"card empty\">This template has no checks yet.</div>'}`,true);\nif($('#startFromTemplate'))$('#startFromTemplate').onclick=()=>{const project=currentProject();if(!project)return toast('Select a project first');const equipOpts=state.equipment.map(e=>`<option value=\"${e.id}\">${esc(e.tag)} \u2014 ${esc(e.name)}</option>`).join('');modal(`Start ${t.name}`,`<form id=\"startTplForm\"><div class=\"field\"><label>Test title</label><input name=\"title\" value=\"${esc(t.name)}\" required></div><div class=\"field\"><label>Equipment / asset (optional)</label><select name=\"equipment_id\"><option value=\"\">Project / system level</option>${equipOpts}</select></div><div class=\"field\"><label>Assign to (optional)</label><select name=\"assigned_to\"><option value=\"\">Unassigned</option>${state.users.map(u=>`<option value=\"${u.id}\">${esc(u.name)}</option>`).join('')}</select></div><button class=\"btn btn-primary\">Create & open test</button></form>`);$('#startTplForm').onsubmit=async e=>{e.preventDefault();try{const x=Object.fromEntries(new FormData(e.target));x.project_id=state.projectId;x.template_id=t.id;const r=await api('/api/inspections',{method:'POST',body:JSON.stringify(x)});$('#modal').remove();await loadAll();state.view='inspections';render();openInspection(r.id)}catch(err){toast(err.message)}}};\nif($('#editTemplateMeta'))$('#editTemplateMeta').onclick=()=>openTemplateForm(t);\nif($('#duplicateTemplate'))$('#duplicateTemplate').onclick=async()=>{try{const r=await api(`/api/templates/${t.id}/duplicate`,{method:'POST',body:JSON.stringify({})});$('#modal').remove();await loadAll();render();toast('Template duplicated');openTemplate(r.id)}catch(err){toast(err.message)}};\nif($('#addTemplateItem'))$('#addTemplateItem').onclick=()=>openTemplateItemForm(t,null);\nif($('#archiveTemplate'))$('#archiveTemplate').onclick=async()=>{if(!confirm(`Archive ${t.name}? Existing completed tests are not affected.`))return;try{await api(`/api/templates/${t.id}`,{method:'DELETE'});$('#modal').remove();await loadAll();render();toast('Template archived')}catch(err){toast(err.message)}};\n$$('[data-edit-template-item]').forEach(b=>b.onclick=()=>openTemplateItemForm(t,items.find(x=>x.id===b.dataset.editTemplateItem)));\n}catch(err){toast(err.message)}}\n\nfunction openTemplateForm(existing=null){const t=existing||{};modal(existing?'Edit template':'New template',`<form id=\"templateForm\"><div class=\"formgrid\"><div class=\"field span2\"><label>Name</label><input name=\"name\" value=\"${esc(t.name||'')}\" required></div><div class=\"field\"><label>Discipline</label><select name=\"discipline\">${['Electrical','BMS','Mechanical/BMS','Mechanical','Integrated Systems','Life Safety','General'].map(x=>`<option ${x===t.discipline?'selected':''}>${x}</option>`).join('')}</select></div><div class=\"field\"><label>Stage</label><select name=\"stage\">${[['qa','QA / ITP'],['prefunctional','Pre-functional'],['commissioning','Commissioning'],['functional','Functional Performance'],['integrated','Integrated Systems Test'],['handover','Handover']].map(([v,l])=>`<option value=\"${v}\" ${v===t.stage?'selected':''}>${l}</option>`).join('')}</select></div><div class=\"field span2\"><label>Description</label><textarea name=\"description\">${esc(t.description||'')}</textarea></div></div><button class=\"btn btn-primary\">${existing?'Save template':'Create template'}</button></form>`);$('#templateForm').onsubmit=async e=>{e.preventDefault();try{const data=Object.fromEntries(new FormData(e.target));const r=await api(existing?`/api/templates/${existing.id}`:'/api/templates',{method:existing?'PATCH':'POST',body:JSON.stringify(data)});$('#modal').remove();await loadAll();render();toast(existing?'Template updated':'Template created');openTemplate(existing?.id||r.id)}catch(err){toast(err.message)}}}\n\nfunction openTemplateItemForm(template,item=null){const x=item||{};modal(item?'Edit template check':'Add template check',`<form id=\"templateItemForm\"><div class=\"formgrid\"><div class=\"field\"><label>Section</label><input name=\"section\" value=\"${esc(x.section||'Checks')}\" required></div><div class=\"field\"><label>Response type</label><select name=\"response_type\">${[['pass_fail_na','Pass / Fail / N/A'],['text','Text'],['number','Number'],['yes_no','Yes / No']].map(([v,l])=>`<option value=\"${v}\" ${v===x.response_type?'selected':''}>${l}</option>`).join('')}</select></div><div class=\"field span2\"><label>Check / requirement</label><textarea name=\"item_text\" required>${esc(x.item_text||'')}</textarea></div><div class=\"field\"><label><input type=\"checkbox\" name=\"mandatory\" ${x.mandatory!==0?'checked':''}> Mandatory</label></div><div class=\"field\"><label><input type=\"checkbox\" name=\"hold_point\" ${x.hold_point?'checked':''}> Witness / hold point</label></div></div><div class=\"actions\"><button class=\"btn btn-primary\">Save check</button>${item?'<button type=\"button\" class=\"btn\" id=\"deleteTemplateItem\">Delete check</button>':''}</div></form>`);$('#templateItemForm').onsubmit=async e=>{e.preventDefault();try{const fd=new FormData(e.target),data=Object.fromEntries(fd);data.mandatory=fd.has('mandatory');data.hold_point=fd.has('hold_point');await api(item?`/api/template-items/${item.id}`:`/api/templates/${template.id}/items`,{method:item?'PATCH':'POST',body:JSON.stringify(data)});$('#modal').remove();await loadAll();render();toast('Template check saved');openTemplate(template.id)}catch(err){toast(err.message)}};if(item&&$('#deleteTemplateItem'))$('#deleteTemplateItem').onclick=async()=>{if(!confirm('Delete this check from the template?'))return;try{await api(`/api/template-items/${item.id}`,{method:'DELETE'});$('#modal').remove();await loadAll();render();toast('Check deleted');openTemplate(template.id)}catch(err){toast(err.message)}}}\n\n\nfunction modal(title,body,wide=false){\n  const existing=$('#modal'); if(existing) existing.remove();\n  document.body.insertAdjacentHTML('beforeend',`<div class=\"modal-backdrop\" id=\"modal\" role=\"presentation\"><div class=\"modal ${wide?'wide':''}\" role=\"dialog\" aria-modal=\"true\" aria-label=\"${esc(title)}\"><div class=\"modal-head\"><div><div class=\"eyebrow\">ELLIOT QX</div><h2>${esc(title)}</h2></div><button type=\"button\" class=\"modal-x\" id=\"closeModal\" aria-label=\"Close\">\u00d7</button></div>${body}</div></div>`);\n  const overlay=$('#modal'), close=()=>{ if(overlay?.isConnected) overlay.remove(); document.removeEventListener('keydown',onKey); };\n  const onKey=e=>{ if(e.key==='Escape') close(); };\n  $('#closeModal')?.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();close()});\n  overlay?.addEventListener('click',e=>{ if(e.target===overlay) close(); });\n  document.addEventListener('keydown',onKey);\n}\nfunction projectOptions(){return state.projects.map(p=>`<option value=\"${p.id}\" ${p.id===state.projectId?'selected':''}>${esc(p.code)} \u2014 ${esc(p.name)}</option>`).join('')}\nfunction systemOptions(){return state.systems.map(s=>`<option value=\"${s.id}\">${esc(s.code)} \u2014 ${esc(s.name)}</option>`).join('')}\nfunction equipmentOptions(){return state.equipment.map(e=>`<option value=\"${e.id}\">${esc(e.tag)} \u2014 ${esc(e.name)}</option>`).join('')}\nfunction userOptions(){return state.users.map(u=>`<option value=\"${u.id}\">${esc(u.name)} \u2014 ${esc(u.role)}</option>`).join('')}\n\nfunction wireActions(){\n  $('#projectPicker').onchange=async e=>{state.projectId=e.target.value;state.query='';await loadAll();render()};\n  $('#menuBtn')?.addEventListener('click',()=>$('#sidebar').classList.toggle('open'));\n  $$('.navbtn[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;state.query='';$('#sidebar').classList.remove('open');render()});\n  $('#logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'}).catch(()=>{});state.user=null;renderLogin(true)};\n  $('#searchBox')?.addEventListener('input',e=>{state.query=e.target.value;renderKeepFocus()});\n  $('#eqStatus')?.addEventListener('change',e=>{state.filters.eqStatus=e.target.value;render()});\n  $('#addProject')?.addEventListener('click',openProjectForm); $('#addSystem')?.addEventListener('click',openSystemForm); $('#addEquipment')?.addEventListener('click',openEquipmentForm); $('#addInspection')?.addEventListener('click',openInspectionForm); $('#quickTest')?.addEventListener('click',openInspectionForm); $('#addDefect')?.addEventListener('click',openDefectForm); $('#quickDefect')?.addEventListener('click',openDefectForm); $('#importPoints')?.addEventListener('click',openPointImport); $('#addTurnover')?.addEventListener('click',openTurnoverForm); $('#addWitness')?.addEventListener('click',openWitnessForm); $('#importEquipment')?.addEventListener('click',openEquipmentImport); $('#captureEvidence')?.addEventListener('click',openEvidenceUpload); $('#scanAsset')?.addEventListener('click',openScanner); $('#runBuilder')?.addEventListener('click',runBuilder); $('#printReport')?.addEventListener('click',()=>window.print()); $('#exportCsv')?.addEventListener('click',exportEquipmentCsv); $('#addUser')?.addEventListener('click',openUserForm); $('#refreshMatrix')?.addEventListener('click',async()=>{await loadAll();render()});\n  $$('[data-inspection]').forEach(r=>r.onclick=()=>openInspection(r.dataset.inspection)); $$('[data-point]').forEach(r=>r.onclick=()=>openPoint(r.dataset.point)); $$('[data-defect]').forEach(r=>r.onclick=()=>openDefect(r.dataset.defect)); $$('[data-turnover]').forEach(r=>r.onclick=()=>openTurnover(r.dataset.turnover)); $$('[data-passport]').forEach(b=>b.onclick=()=>openAssetPassport(b.dataset.passport));\n}\nfunction renderKeepFocus(){const pos=$('#searchBox')?.selectionStart;render();const box=$('#searchBox');if(box){box.focus();box.setSelectionRange(pos,pos)}}\n\n\nfunction riskPill(v){return `<span class=\"risk-pill ${esc(v)}\">${esc(String(v||'').replaceAll('_',' '))}</span>`}\nfunction portfolioView(){const rows=state.portfolio||[];const critical=rows.filter(x=>x.risk==='critical').length,avg=rows.length?Math.round(rows.reduce((n,x)=>n+Number(x.readiness_score||0),0)/rows.length):0;return `${hero('Portfolio Control Tower','Cross-project readiness, risk and handover position for Elliot Controls')}<div class=\"cards\">${card('Active projects',rows.length,'Live projects in Qx','blue')}${card('Portfolio readiness',`${avg}%`,'Weighted completion intelligence',avg>=85?'green':'amber')}${card('Critical projects',critical,'Category A / failed BMS blockers',critical?'red':'green')}${card('Open defects',rows.reduce((n,x)=>n+Number(x.open_defects||0),0),'Across portfolio','amber')}</div><div class=\"portfolio-grid\">${rows.map(p=>`<button class=\"project-tile\" data-open-project=\"${p.id}\"><div class=\"tile-top\"><div><span>${esc(p.code)}</span><h3>${esc(p.name)}</h3></div>${riskPill(p.risk)}</div><div class=\"score-line\"><strong>${p.readiness_score}%</strong>${progress(p.readiness_score)}</div><div class=\"mini-metrics\"><span>Assets <b>${p.asset_percent}%</b></span><span>Tests <b>${p.test_percent}%</b></span><span>BMS <b>${p.bms_percent}%</b></span><span>Turnover <b>${p.turnover_percent}%</b></span></div><div class=\"tile-foot\"><span>${p.cat_a} Cat A</span><span>${p.failed_points} failed I/O</span><span>${p.witness_open} witness</span></div></button>`).join('')||'<div class=\"card empty\">Create your first project to start the portfolio view.</div>'}</div>`}\nfunction fieldHubView(){if(!state.projectId)return `${hero('Field Hub','The fastest route from site condition to verified commissioning record')}<div class=\"card empty\">Select a project first.</div>`;const pack=localStorage.getItem(`qx-offline-pack:${state.projectId}`);let meta=null;try{meta=JSON.parse(pack||'null')}catch{}const q=getQueue().length;return `${hero('Field Hub','Large touch targets, scan-first workflow and offline project packs',`<button class=\"btn btn-primary\" id=\"downloadPack\">${meta?'Refresh':'Download'} offline pack</button>`)}<div class=\"field-status-strip\"><div><span>Connection</span><b>${navigator.onLine?'LIVE':'OFFLINE'}</b></div><div><span>Queued updates</span><b>${q}</b></div><div><span>Offline pack</span><b>${meta?fmtTime(meta.generated_at):'Not downloaded'}</b></div><div><span>Open Cat A</span><b>${state.defects.filter(x=>x.category==='A'&&!['closed','verified'].includes(x.status)).length}</b></div></div><div class=\"field-launcher\"><button data-route=\"equipment\"><i>\u25a3</i><b>Scan / Assets</b><span>Open asset passport in seconds</span></button><button data-route=\"inspections\"><i>\u2713</i><b>QA / ITP</b><span>Complete inspections and tests</span></button><button data-route=\"points\"><i>\u21af</i><b>BMS I/O</b><span>Point-to-point commissioning</span></button><button data-route=\"defects\"><i>!</i><b>Punch</b><span>Raise and close defects</span></button><button data-route=\"observations\"><i>\u25cf</i><b>Observation</b><span>Capture site condition / note</span></button><button id=\"captureEvidence\"><i>\u25eb</i><b>Evidence</b><span>Photo or document to asset</span></button></div><div class=\"grid2\"><div class=\"card\"><div class=\"section-head\"><h3>My next work</h3><span>${(state.myWork.actions?.length||0)+(state.myWork.inspections?.length||0)+(state.myWork.defects?.length||0)} items</span></div>${[...(state.myWork.actions||[]).slice(0,3),...(state.myWork.inspections||[]).slice(0,3)].map(x=>`<div class=\"activity-item\"><b>${esc(x.title||x.name||'Assigned work')}</b><small>${esc(x.status||'open')}</small></div>`).join('')||'<div class=\"empty\">No assigned work.</div>'}</div><div class=\"card\"><div class=\"section-head\"><h3>Readiness blockers</h3></div>${(state.equipmentReadiness||[]).filter(x=>!x.ready&&x.blockers?.length).slice(0,6).map(x=>`<div class=\"activity-item\"><b>${esc(x.tag)} \u2014 ${esc(x.name)}</b><small>${esc(x.blockers.join(' \u2022 '))}</small></div>`).join('')||'<div class=\"empty\">No asset blockers detected.</div>'}</div></div>`}\nfunction observationsView(){if(!state.projectId)return `${hero('Field Observations','Capture site conditions, design comments and construction observations')}<div class=\"card empty\">Select a project first.</div>`;const rows=state.observations||[];return `${hero('Field Observations','One register for observations before they become defects','<button class=\"btn btn-primary\" id=\"addObservation\">+ Observation</button>')}<div class=\"cards\">${card('Open',rows.filter(x=>x.status==='open').length,'Awaiting action','amber')}${card('Critical',rows.filter(x=>x.severity==='critical'&&x.status!=='closed').length,'Immediate review','red')}${card('Total',rows.length,'Captured field intelligence','blue')}</div><div class=\"table-wrap\"><table><thead><tr><th>Type</th><th>Observation</th><th>Asset/System</th><th>Owner</th><th>Severity</th><th>Status</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x.observation_type)}</td><td><strong>${esc(x.title)}</strong><br><span class=\"muted\">${esc(x.description||'')}</span></td><td>${esc(x.equipment_tag||x.system_code||'\u2014')}</td><td>${esc(x.assigned_name||'Unassigned')}</td><td>${pill(x.severity)}</td><td>${pill(x.status)}</td></tr>`).join('')||'<tr><td colspan=\"6\">No observations.</td></tr>'}</tbody></table></div>`}\nfunction plannerView(){if(!state.projectId)return `${hero('Commissioning Plan','Milestones from install readiness through integrated systems testing and handover')}<div class=\"card empty\">Select a project first.</div>`;const rows=state.milestones||[];return `${hero('Commissioning Plan','Milestone and gate control for the commissioning programme','<button class=\"btn btn-primary\" id=\"addMilestone\">+ Milestone</button>')}<div class=\"timeline\">${rows.map(m=>`<div class=\"timeline-row ${m.status}\"><div class=\"timeline-date\">${fmtDate(m.target_at)}</div><div class=\"timeline-dot\"></div><div class=\"timeline-card\"><div><span>${esc(m.phase)}</span><h3>${esc(m.name)}</h3><p>${esc(m.system_code||'Project-wide')} \u2022 ${esc(m.owner_name||'Unassigned')}</p></div>${pill(m.status)}</div></div>`).join('')||'<div class=\"card empty\">No milestones yet. Add energisation, PFC, FPT, IST, witness and handover gates.</div>'}</div>`}\nfunction smartTemplatesView(){if(!state.projectId)return `${hero('Smart Template Builder','Turn project requirements into a reviewable QA / commissioning test draft')}<div class=\"card empty\">Select a project first.</div>`;return `${hero('Smart Template Builder','Generate project-specific ITP, PFC and FPT drafts from scope, specification or sequence text')}<div class=\"grid2\"><div class=\"card\"><form id=\"smartForm\"><div class=\"formgrid\"><div class=\"field span2\"><label>Template name</label><input name=\"name\" required placeholder=\"AHU Functional Performance Test \u2014 Project Specific\"></div><div class=\"field\"><label>Discipline</label><select name=\"discipline\"><option>BMS</option><option>Electrical</option><option>Mechanical/BMS</option><option>Integrated Systems</option></select></div><div class=\"field\"><label>Stage</label><select name=\"stage\"><option value=\"qa\">QA / ITP</option><option value=\"prefunctional\">Pre-functional</option><option value=\"functional\" selected>Functional</option><option value=\"integrated\">Integrated systems</option></select></div><div class=\"field span2\"><label>Paste approved scope / sequence / specification extract</label><textarea name=\"source_text\" style=\"min-height:280px\" placeholder=\"Paste the relevant project requirements here...\"></textarea></div></div><button class=\"btn btn-primary\">Build test draft</button></form></div><div class=\"card\" id=\"smartResult\">${state.smartForm?smartProposalHtml(state.smartForm):'<div class=\"empty\">Qx will create a reviewable test draft. Approved project documents remain the authority.</div>'}</div></div>`}\nfunction smartProposalHtml(r){const p=r.proposal||{};return `<div class=\"eyebrow\">REVIEW BEFORE APPLYING</div><h2>${esc(p.name||'Draft')}</h2><p class=\"muted\">${esc(p.warning||'')}</p><div class=\"smart-checks\">${(p.items||[]).map((x,i)=>`<div><span>${i+1}</span><p><b>${esc(x.section)}</b><br>${esc(x.item_text)}</p>${x.hold_point?'<em>HOLD</em>':''}</div>`).join('')}</div><button class=\"btn btn-primary\" id=\"applySmart\" data-job=\"${esc(r.id)}\">Add to Template Library</button>`}\nasync function loadV8(){try{await api('/api/v8/ensure',{method:'POST',body:'{}'});state.v8ready=true;const pf=await api('/api/portfolio');state.portfolio=pf.projects||[];if(state.projectId){const q=`?project_id=${encodeURIComponent(state.projectId)}`;const [o,m,r,c]=await Promise.all([api(`/api/observations${q}`),api(`/api/milestones${q}`),api(`/api/equipment-readiness${q}`),api(`/api/sync/conflicts${q}`)]);state.observations=o.observations||[];state.milestones=m.milestones||[];state.equipmentReadiness=r.equipment||[];state.syncConflicts=c.conflicts||[];const pack=localStorage.getItem(`qx-offline-pack:${state.projectId}`);try{state.offlinePackAt=JSON.parse(pack||'null')?.generated_at||null}catch{state.offlinePackAt=null}}else{state.observations=[];state.milestones=[];state.equipmentReadiness=[];state.syncConflicts=[]}}catch(e){console.warn('V8 extensions unavailable',e)}}\nfunction openObservationForm(){modal('New field observation',`<form id=\"obsForm\"><div class=\"formgrid\"><div class=\"field span2\"><label>Title</label><input name=\"title\" required placeholder=\"Access panel clashes with BMS sensor location\"></div><div class=\"field\"><label>Type</label><select name=\"observation_type\"><option value=\"field\">Field observation</option><option value=\"design\">Design review</option><option value=\"construction\">Construction review</option><option value=\"client\">Client comment</option></select></div><div class=\"field\"><label>Severity</label><select name=\"severity\"><option>normal</option><option>high</option><option>critical</option></select></div><div class=\"field\"><label>System</label><select name=\"system_id\"><option value=\"\">Project wide</option>${systemOptions()}</select></div><div class=\"field\"><label>Equipment</label><select name=\"equipment_id\"><option value=\"\">None</option>${equipmentOptions()}</select></div><div class=\"field span2\"><label>Description</label><textarea name=\"description\"></textarea></div></div><button class=\"btn btn-primary\">Save observation</button></form>`);$('#obsForm').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));d.project_id=state.projectId;await api('/api/observations',{method:'POST',body:JSON.stringify(d)});$('#modal').remove();await loadAll();render();toast('Observation captured')}}\nfunction openMilestoneForm(){modal('Add commissioning milestone',`<form id=\"mileForm\"><div class=\"formgrid\"><div class=\"field span2\"><label>Milestone / gate</label><input name=\"name\" required placeholder=\"AHU systems ready for FPT\"></div><div class=\"field\"><label>Phase</label><select name=\"phase\"><option>installation</option><option>pre-energisation</option><option>commissioning</option><option>functional testing</option><option>IST</option><option>witness</option><option>handover</option></select></div><div class=\"field\"><label>Target date</label><input name=\"target_at\" type=\"date\"></div><div class=\"field\"><label>System</label><select name=\"system_id\"><option value=\"\">Project wide</option>${systemOptions()}</select></div><div class=\"field\"><label>Owner</label><select name=\"owner_id\"><option value=\"\">Unassigned</option>${userOptions()}</select></div></div><button class=\"btn btn-primary\">Add milestone</button></form>`);$('#mileForm').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));d.project_id=state.projectId;await api('/api/milestones',{method:'POST',body:JSON.stringify(d)});$('#modal').remove();await loadAll();render();toast('Milestone added')}}\nasync function downloadOfflinePack(){if(!state.projectId)return toast('Select a project');try{const pack=await api(`/api/offline/pack?project_id=${encodeURIComponent(state.projectId)}`);localStorage.setItem(`qx-offline-pack:${state.projectId}`,JSON.stringify(pack));for(const [name,data] of Object.entries({projects:{projects:[pack.project]},systems:{systems:pack.systems},equipment:{equipment:pack.equipment},templates:{templates:pack.templates},inspections:{inspections:pack.inspections},defects:{defects:pack.defects},points:{points:pack.points},turnover:{packages:pack.turnover},witness:{witness:pack.witness},actions:{actions:pack.actions}})){const pathMap={projects:'/api/projects',systems:`/api/systems?project_id=${state.projectId}`,equipment:`/api/equipment?project_id=${state.projectId}`,templates:'/api/templates',inspections:`/api/inspections?project_id=${state.projectId}`,defects:`/api/defects?project_id=${state.projectId}`,points:`/api/bms-points?project_id=${state.projectId}`,turnover:`/api/turnover?project_id=${state.projectId}`,witness:`/api/witness?project_id=${state.projectId}`,actions:`/api/actions?project_id=${state.projectId}`};localStorage.setItem(API_CACHE+pathMap[name],JSON.stringify({at:Date.now(),data}))}state.offlinePackAt=pack.generated_at;render();toast('Offline project pack ready')}catch(e){toast(e.message)}}\n\nfunction notificationsView(){const n=state.notifications||[];return `${hero('Alerts & Escalations','One priority inbox for overdue, blocking and commissioning-critical work','<button class=\"btn btn-primary\" id=\"refreshAlerts\">Refresh risk scan</button>')}<div class=\"cards\">${card('Critical',n.filter(x=>x.severity==='critical'&&!x.read_at).length,'Requires immediate action','red')}${card('Unread',n.filter(x=>!x.read_at).length,'Across selected project','amber')}${card('Open alerts',n.length,'Auto-generated from live project data','blue')}</div><div class=\"card\">${n.length?n.map(x=>`<div class=\"activity-item\"><b>${esc(x.title)}</b><p>${esc(x.message)}</p><small>${esc(x.severity.toUpperCase())} \u2022 ${fmtTime(x.created_at)}</small></div>`).join(''):'<div class=\"empty\">No active escalations. Qx will generate them from overdue actions, Category A punch, failed BMS points and witness holds.</div>'}</div>`}\nfunction drawingsView(){if(!state.projectId)return `${hero('Drawings & Markups','Drawing-centric QA with issue pins and field evidence')}<div class=\"card empty\">Select a project to manage drawings.</div>`;const rows=state.drawings||[];return `${hero('Drawings & Markups','Controlled drawing register with field issue pins','<button class=\"btn btn-primary\" id=\"addDrawing\">+ Register drawing</button>')}<div class=\"banner\"><div>\u2317</div><div><strong>Drawing-first commissioning</strong><p>Register approved drawings, open them from R2 evidence, and pin defects or observations to a sheet coordinate so field teams can find the exact location.</p></div></div><div class=\"table-wrap\"><table><thead><tr><th>Drawing</th><th>Title</th><th>Rev</th><th>Discipline</th><th>Pins</th><th>Updated</th></tr></thead><tbody>${rows.map(x=>`<tr data-drawing=\"${x.id}\" style=\"cursor:pointer\"><td><strong>${esc(x.drawing_no)}</strong></td><td>${esc(x.title)}</td><td>${esc(x.revision||'\u2014')}</td><td>${esc(x.discipline||'\u2014')}</td><td>${x.pin_count||0}</td><td>${fmtTime(x.updated_at)}</td></tr>`).join('')||'<tr><td colspan=\"6\">No drawings registered yet.</td></tr>'}</tbody></table></div>`}\nasync function loadV7(){try{await api('/api/v7/ensure',{method:'POST',body:'{}'});state.v7ready=true;const q=state.projectId?`?project_id=${encodeURIComponent(state.projectId)}`:'';const [n,d]=await Promise.all([api(`/api/notifications${q}`),state.projectId?api(`/api/drawings${q}`):Promise.resolve({drawings:[]})]);state.notifications=n.notifications||[];state.drawings=d.drawings||[]}catch(e){console.warn('V7 extensions unavailable',e)}}\nfunction openDrawingForm(){modal('Register controlled drawing',`<form id=\"drawingForm\"><div class=\"formgrid\"><div class=\"field\"><label>Drawing number</label><input name=\"drawing_no\" required placeholder=\"E-401\"></div><div class=\"field\"><label>Revision</label><input name=\"revision\" placeholder=\"C\"></div><div class=\"field span2\"><label>Title</label><input name=\"title\" required placeholder=\"MSSB / BMS Layout\"></div><div class=\"field\"><label>Discipline</label><select name=\"discipline\"><option>Electrical</option><option>BMS</option><option>Mechanical</option><option>Controls</option><option>General</option></select></div><div class=\"field\"><label>Existing R2 file ID (optional)</label><input name=\"file_id\" placeholder=\"Link an uploaded PDF/image\"></div></div><button class=\"btn btn-primary\">Register drawing</button></form>`);$('#drawingForm').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));d.project_id=state.projectId;await api('/api/drawings',{method:'POST',body:JSON.stringify(d)});$('#modal').remove();await loadV7();render();toast('Drawing registered')}}\nasync function openDrawing(id){const d=await api(`/api/drawings/${id}`);const x=d.drawing;modal(`${x.drawing_no} \u2014 ${x.title}`,`<div class=\"banner\"><div>\u2317</div><div><strong>Revision ${esc(x.revision||'\u2014')}</strong><p>${esc(x.discipline||'')} \u2022 ${d.pins.length} field pin(s)</p></div></div>${x.file_id?`<a class=\"btn\" target=\"_blank\" href=\"/api/files/${x.file_id}\">Open source drawing</a>`:''}<div class=\"card\" style=\"margin-top:14px\"><h3>Add field pin</h3><form id=\"pinForm\"><div class=\"formgrid\"><div class=\"field span2\"><label>Issue / observation</label><input name=\"title\" required placeholder=\"VSD isolator location differs from drawing\"></div><div class=\"field\"><label>X position %</label><input name=\"x\" type=\"number\" min=\"0\" max=\"100\" value=\"50\"></div><div class=\"field\"><label>Y position %</label><input name=\"y\" type=\"number\" min=\"0\" max=\"100\" value=\"50\"></div><div class=\"field\"><label>Severity</label><select name=\"severity\"><option>normal</option><option>high</option><option>critical</option></select></div></div><button class=\"btn btn-primary\">Pin to drawing</button></form></div><div class=\"section-head\"><h3>Markup pins</h3></div>${d.pins.map(p=>`<div class=\"activity-item\"><b>${esc(p.title)}</b><p>${esc(p.severity)} \u2022 X ${p.x_percent}% / Y ${p.y_percent}%</p><small>${fmtTime(p.created_at)}</small></div>`).join('')||'<div class=\"empty\">No pins yet.</div>'}`,true);$('#pinForm').onsubmit=async e=>{e.preventDefault();const p=Object.fromEntries(new FormData(e.target));await api(`/api/drawings/${id}/pins`,{method:'POST',body:JSON.stringify(p)});$('#modal').remove();await loadV7();openDrawing(id)}}\nfunction render(){const views={portfolio:portfolioView,dashboard:dashboardView,fieldhub:fieldHubView,notifications:notificationsView,drawings:drawingsView,planner:plannerView,observations:observationsView,smarttemplates:smartTemplatesView,mywork:myWorkView,matrix:matrixView,projects:projectsView,systems:systemsView,intelligence:intelligenceView,equipment:equipmentView,builder:builderView,inspections:inspectionsView,points:pointsView,defects:defectsView,evidence:evidenceView,witness:witnessView,turnover:turnoverView,reports:reportsView,closeout:closeoutView,templates:templatesView,admin:adminView};app.innerHTML=shell((views[state.view]||dashboardView)());wireActions();updateQueueBadge()}\n\nfunction formSubmit(form,path,after){form.onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));try{await api(path,{method:'POST',body:JSON.stringify(data)});$('#modal')?.remove();await loadAll();render();toast(after||'Saved')}catch(err){form.insertAdjacentHTML('afterbegin',`<div class=\"error\">${esc(err.message)}</div>`)}}}\nasync function loadCloseout(){if(!state.projectId)return;try{const [c,d,s]=await Promise.all([api(`/api/copilot/project?project_id=${encodeURIComponent(state.projectId)}`),api(`/api/documents?project_id=${encodeURIComponent(state.projectId)}`),api(`/api/signoffs?project_id=${encodeURIComponent(state.projectId)}`)]);state.copilot=c;state.documents=d.documents||[];state.signoffs=s.signoffs||[]}catch(e){toast(e.message)}}\nfunction wireCloseout(){if(state.view!=='closeout')return;loadCloseout().then(()=>{if(state.view==='closeout')render()}).catch(()=>{});}\nfunction openSignature(){modal('Project digital acceptance',`<form id=\"signForm\"><div class=\"formgrid\"><div class=\"field\"><label>Signer name</label><input name=\"signer_name\" required></div><div class=\"field\"><label>Company</label><input name=\"signer_company\"></div><div class=\"field\"><label>Role</label><input name=\"signer_role\" placeholder=\"Commissioning Manager / Client\"></div><div class=\"field\"><label>Signoff type</label><select name=\"signoff_type\"><option value=\"project_acceptance\">Project acceptance</option><option value=\"witness\">Witness acceptance</option><option value=\"handover\">Handover acceptance</option></select></div><div class=\"field span2\"><label>Declaration</label><textarea name=\"declaration\">I confirm the listed commissioning/closeout status has been reviewed and accepted subject to recorded outstanding items.</textarea></div></div><div class=\"field\"><label>Signature \u2014 draw below</label><canvas id=\"sigCanvas\" width=\"700\" height=\"180\" style=\"width:100%;height:180px;background:white;border-radius:8px;touch-action:none\"></canvas></div><button type=\"button\" class=\"btn\" id=\"clearSig\">Clear</button> <button class=\"btn btn-primary\">Save signed acceptance</button></form>`,true);const c=$('#sigCanvas'),ctx=c.getContext('2d');ctx.lineWidth=2.5;ctx.lineCap='round';ctx.strokeStyle='#111';let down=false;const pos=e=>{const r=c.getBoundingClientRect(),q=e.touches?.[0]||e;return[(q.clientX-r.left)*c.width/r.width,(q.clientY-r.top)*c.height/r.height]};const start=e=>{down=true;const [x,y]=pos(e);ctx.beginPath();ctx.moveTo(x,y);e.preventDefault()};const move=e=>{if(!down)return;const [x,y]=pos(e);ctx.lineTo(x,y);ctx.stroke();e.preventDefault()};const end=()=>down=false;c.onpointerdown=start;c.onpointermove=move;c.onpointerup=end;c.onpointerleave=end;$('#clearSig').onclick=()=>ctx.clearRect(0,0,c.width,c.height);$('#signForm').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));d.project_id=state.projectId;d.entity_type='project';d.entity_id=state.projectId;d.signature_data=c.toDataURL('image/png');try{await api('/api/signoffs',{method:'POST',body:JSON.stringify(d)});$('#modal').remove();await loadCloseout();render();toast('Digital acceptance saved')}catch(err){toast(err.message)}}}\nfunction openDocumentForm(){modal('Add controlled document',`<form id=\"docForm\"><div class=\"formgrid\"><div class=\"field\"><label>Document number</label><input name=\"document_no\"></div><div class=\"field\"><label>Revision</label><input name=\"revision\" placeholder=\"A / P01 / 1\"></div><div class=\"field span2\"><label>Title</label><input name=\"title\" required></div><div class=\"field\"><label>Type</label><select name=\"doc_type\"><option>As-built drawing</option><option>O&M manual</option><option>Commissioning report</option><option>Electrical test record</option><option>BMS backup</option><option>Training record</option><option>Certificate</option><option>Other</option></select></div><div class=\"field\"><label>Status</label><select name=\"status\"><option value=\"current\">Current</option><option value=\"draft\">Draft</option><option value=\"superseded\">Superseded</option></select></div><div class=\"field span2\"><label>File</label><input id=\"docFile\" type=\"file\" required></div></div><button class=\"btn btn-primary\">Upload & register</button></form>`);$('#docForm').onsubmit=async e=>{e.preventDefault();const f=$('#docFile').files[0];if(!f)return;try{const qs=new URLSearchParams({project_id:state.projectId});const r=await fetch(`/api/files?${qs}`,{method:'POST',body:f,credentials:'same-origin',headers:{'content-type':f.type||'application/octet-stream','x-file-name':f.name}});const fd=await r.json();if(!r.ok)throw new Error(fd.error||'Upload failed');const d=Object.fromEntries(new FormData(e.target));delete d[''];d.project_id=state.projectId;d.object_file_id=fd.id;await api('/api/documents',{method:'POST',body:JSON.stringify(d)});$('#modal').remove();await loadCloseout();render();toast('Controlled document registered')}catch(err){toast(err.message)}}}\n\nfunction openProjectForm(){modal('Create project',`<form id=\"f\"><div class=\"formgrid\"><div class=\"field\"><label>Project code</label><input name=\"code\" required placeholder=\"MEL06\"></div><div class=\"field\"><label>Project name</label><input name=\"name\" required></div><div class=\"field\"><label>Client</label><input name=\"client\"></div><div class=\"field\"><label>Project type</label><select name=\"project_type\"><option>data_centre</option><option selected>commercial</option><option>healthcare</option><option>infrastructure</option><option>industrial</option></select></div><div class=\"field\"><label>Commissioning manager</label><input name=\"commissioning_manager\"></div><div class=\"field\"><label>Practical completion</label><input name=\"practical_completion\" type=\"date\"></div><div class=\"field span2\"><label>Site address</label><input name=\"site_address\"></div></div><button class=\"btn btn-primary\">Create project</button></form>`);formSubmit($('#f'),'/api/projects','Project created')}\nfunction openSystemForm(){modal('Add commissioning system',`<form id=\"f\"><div class=\"formgrid\"><div class=\"field\"><label>Project</label><select name=\"project_id\" required>${projectOptions()}</select></div><div class=\"field\"><label>System code</label><input name=\"code\" required placeholder=\"SYS-BMS-01\"></div><div class=\"field\"><label>System name</label><input name=\"name\" required placeholder=\"BMS Controls Network\"></div><div class=\"field\"><label>Discipline</label><select name=\"discipline\"><option>BMS</option><option>Electrical</option><option>Mechanical</option><option>Fire</option><option>Integrated Systems</option></select></div><div class=\"field\"><label>Location / area</label><input name=\"location\"></div><div class=\"field\"><label>Target ready date</label><input name=\"target_date\" type=\"date\"></div></div><button class=\"btn btn-primary\">Add system</button></form>`);formSubmit($('#f'),'/api/systems','System added')}\nfunction openEquipmentForm(){modal('Add equipment',`<form id=\"f\"><div class=\"formgrid\"><div class=\"field\"><label>Project</label><select name=\"project_id\" required>${projectOptions()}</select></div><div class=\"field\"><label>Tag</label><input name=\"tag\" required placeholder=\"AHU-L03-01\"></div><div class=\"field span2\"><label>Description</label><input name=\"name\" required placeholder=\"Level 3 Air Handling Unit 01\"></div><div class=\"field\"><label>System</label><input name=\"system\" placeholder=\"AHU System\"></div><div class=\"field\"><label>Subsystem</label><input name=\"subsystem\" placeholder=\"Level 3\"></div><div class=\"field\"><label>Building</label><input name=\"building\"></div><div class=\"field\"><label>Level</label><input name=\"level\"></div><div class=\"field\"><label>Area</label><input name=\"area\"></div><div class=\"field\"><label>Equipment type</label><input name=\"equipment_type\" placeholder=\"AHU\"></div><div class=\"field\"><label>Manufacturer</label><input name=\"manufacturer\"></div><div class=\"field\"><label>Model</label><input name=\"model\"></div></div><button class=\"btn btn-primary\">Add equipment</button></form>`);formSubmit($('#f'),'/api/equipment','Equipment added')}\nfunction openInspectionForm(){if(!state.projects.length)return toast('Create a project first');modal('Start QA / commissioning test',`<form id=\"f\"><div class=\"formgrid\"><div class=\"field\"><label>Project</label><select name=\"project_id\" required>${projectOptions()}</select></div><div class=\"field\"><label>Equipment</label><select name=\"equipment_id\"><option value=\"\">Project / system wide</option>${equipmentOptions()}</select></div><div class=\"field span2\"><label>Template</label><select name=\"template_id\" required>${state.templates.map(t=>`<option value=\"${t.id}\">${esc(t.discipline)} \u2014 ${esc(t.name)}</option>`).join('')}</select></div><div class=\"field span2\"><label>Test title</label><input name=\"title\" required placeholder=\"AHU-L03-01 Functional Performance Test\"></div><div class=\"field\"><label>Assign to</label><select name=\"assigned_to\"><option value=\"\">Unassigned</option>${userOptions()}</select></div></div><button class=\"btn btn-primary\">Start field test</button></form>`);formSubmit($('#f'),'/api/inspections','Test created')}\nfunction openDefectForm(){if(!state.projects.length)return toast('Create a project first');modal('Raise defect / punch item',`<form id=\"f\"><div class=\"formgrid\"><div class=\"field\"><label>Project</label><select name=\"project_id\" required>${projectOptions()}</select></div><div class=\"field\"><label>Equipment</label><select name=\"equipment_id\"><option value=\"\">Project / system wide</option>${equipmentOptions()}</select></div><div class=\"field span2\"><label>Defect title</label><input name=\"title\" required></div><div class=\"field span2\"><label>Description / required action</label><textarea name=\"description\"></textarea></div><div class=\"field\"><label>Category</label><select name=\"category\"><option value=\"A\">A \u2014 blocks energisation / turnover</option><option value=\"B\" selected>B \u2014 must close before final handover</option><option value=\"C\">C \u2014 minor / cosmetic</option></select></div><div class=\"field\"><label>Severity</label><select name=\"severity\"><option>low</option><option selected>medium</option><option>high</option><option>critical</option></select></div><div class=\"field\"><label>Discipline</label><select name=\"discipline\"><option>BMS</option><option>Electrical</option><option>Mechanical</option><option>Switchboards</option><option>Fire</option><option>General</option></select></div><div class=\"field\"><label>Assign to</label><select name=\"assigned_to\"><option value=\"\">Unassigned</option>${userOptions()}</select></div><div class=\"field\"><label>Due date</label><input name=\"due_date\" type=\"date\"></div></div><button class=\"btn btn-primary\">Raise defect</button></form>`);formSubmit($('#f'),'/api/defects','Defect raised')}\nfunction openTurnoverForm(){if(!state.projects.length)return toast('Create a project first');modal('Create turnover package',`<form id=\"f\"><div class=\"field\"><label>Project</label><select name=\"project_id\" required>${projectOptions()}</select></div><div class=\"field\"><label>System</label><select name=\"system_id\"><option value=\"\">Project-wide</option>${systemOptions()}</select></div><div class=\"field\"><label>Package name</label><input name=\"name\" required placeholder=\"BMS System Completion Package\"></div><button class=\"btn btn-primary\">Create package with Qx readiness gates</button></form>`);formSubmit($('#f'),'/api/turnover','Turnover package created')}\nfunction openWitnessForm(){modal('Request witness / hold point',`<form id=\"f\"><div class=\"field\"><label>Project</label><select name=\"project_id\" required>${projectOptions()}</select></div><div class=\"field\"><label>System</label><select name=\"system_id\"><option value=\"\">Project-wide</option>${systemOptions()}</select></div><div class=\"field\"><label>Inspection / test</label><select name=\"inspection_id\"><option value=\"\">System witness</option>${state.inspections.map(i=>`<option value=\"${i.id}\">${esc(i.title)}</option>`).join('')}</select></div><div class=\"field\"><label>Requested date & time</label><input name=\"requested_for\" type=\"datetime-local\" required></div><div class=\"field\"><label>Witness party</label><input name=\"witness_party\" placeholder=\"Client / CxA / consultant\"></div><button class=\"btn btn-primary\">Request witness</button></form>`);formSubmit($('#f'),'/api/witness','Witness requested')}\n\n\nfunction openEquipmentImport(){if(!state.projectId)return toast('Select a project first');modal('Import equipment register',`<div class=\"banner\"><div>\u21e7</div><div><strong>Bulk asset setup</strong><p>Paste CSV from Excel. Required: tag,name. Optional: system,subsystem,building,level,area,equipment_type,manufacturer,model,serial_number.</p></div></div><div class=\"field\"><label>CSV data</label><textarea id=\"eqCsv\" style=\"min-height:280px\" placeholder=\"tag,name,system,level,equipment_type\\nAHU-01,Air Handling Unit 01,SYS-AHU,L01,AHU\"></textarea></div><button class=\"btn btn-primary\" id=\"doEqImport\">Import assets</button>`,true);$('#doEqImport').onclick=async()=>{try{const equipment=parseCSV($('#eqCsv').value);if(!equipment.length)throw new Error('No valid CSV rows found');const r=await api('/api/equipment/import',{method:'POST',body:JSON.stringify({project_id:state.projectId,equipment})});$('#modal').remove();await loadAll();render();toast(`${r.created} assets imported \u2022 ${r.skipped} skipped`)}catch(e){toast(e.message)}}}\n\nfunction openEvidenceUpload(){if(!state.projectId)return toast('Select a project first');modal('Capture / upload evidence',`<form id=\"evidenceForm\"><div class=\"field\"><label>Link to equipment</label><select name=\"equipment_id\"><option value=\"\">Project-level evidence</option>${equipmentOptions()}</select></div><div class=\"field\"><label>Photo / file</label><input id=\"evidenceFile\" type=\"file\" accept=\"image/*,application/pdf\" capture=\"environment\" required></div><button class=\"btn btn-primary\">Upload evidence</button></form>`);$('#evidenceForm').onsubmit=async e=>{e.preventDefault();const file=$('#evidenceFile').files[0];if(!file)return;const eq=new FormData(e.target).get('equipment_id');try{const qs=new URLSearchParams({project_id:state.projectId});if(eq)qs.set('equipment_id',eq);const r=await fetch(`/api/files?${qs}`,{method:'POST',body:file,credentials:'same-origin',headers:{'content-type':file.type||'application/octet-stream','x-file-name':file.name}});const d=await r.json();if(!r.ok)throw new Error(d.error||'Upload failed');$('#modal').remove();await loadAll();render();toast('Evidence uploaded')}catch(err){toast(err.message)}}}\n\nasync function openAssetPassport(eid){try{const d=await api(`/api/equipment/${eid}/passport`),e=d.equipment;const ioPass=(d.points||[]).filter(x=>x.overall_status==='pass').length,ioFail=(d.points||[]).filter(x=>x.overall_status==='fail').length,openDef=(d.defects||[]).filter(x=>!['closed','verified'].includes(x.status)).length;modal(`Asset Passport \u2014 ${e.tag}`,`<div class=\"passport-head\"><div><div class=\"eyebrow\">${esc(e.project_code)} \u2022 ${esc(e.equipment_type||'ASSET')}</div><h2>${esc(e.tag)} \u2014 ${esc(e.name)}</h2><p>${esc([e.building,e.level,e.area].filter(Boolean).join(' / ')||'Location not set')}</p></div><div class=\"passport-score\">${pill(e.status)}</div></div><div class=\"cards\" style=\"grid-template-columns:repeat(4,1fr)\">${card('QA / Cx tests',(d.inspections||[]).length,`${(d.inspections||[]).filter(x=>x.status==='approved').length} approved`,'green')}${card('BMS points',(d.points||[]).length,`${ioPass} proven \u2022 ${ioFail} failed`,ioFail?'red':'blue')}${card('Open punch',openDef,'Asset-linked defects',openDef?'amber':'green')}${card('Evidence',(d.files||[]).length,'Photos / documents','blue')}</div><div class=\"grid2\"><div class=\"card\"><div class=\"section-head\"><h3>Asset record</h3><button class=\"btn btn-small\" id=\"copyAssetLink\">Copy QR link</button></div><div class=\"detail-list\"><div><span>System</span><b>${esc(e.system||'\u2014')}</b></div><div><span>Manufacturer</span><b>${esc(e.manufacturer||'\u2014')}</b></div><div><span>Model</span><b>${esc(e.model||'\u2014')}</b></div><div><span>Serial</span><b>${esc(e.serial_number||'\u2014')}</b></div></div><form id=\"assetStatus\"><div class=\"field\"><label>Completion status</label><select name=\"status\">${['not_started','qa','commissioning','witness','blocked','complete'].map(x=>`<option value=\"${x}\" ${x===e.status?'selected':''}>${x.replaceAll('_',' ')}</option>`).join('')}</select></div><button class=\"btn btn-primary\">Update asset</button></form></div><div class=\"card\"><div class=\"section-head\"><h3>Recent evidence</h3><button class=\"btn btn-small\" id=\"assetEvidence\">+ Photo</button></div>${(d.files||[]).slice(0,8).map(f=>`<a class=\"file-row\" target=\"_blank\" href=\"/api/files/${f.id}\"><b>${esc(f.file_name)}</b><span>${fmtTime(f.created_at)}</span></a>`).join('')||'<div class=\"empty\">No asset evidence yet.</div>'}</div></div><div class=\"section-head\"><h3>Commissioning history</h3><span>Asset-linked records</span></div>${(d.inspections||[]).length?`<div class=\"table-wrap\"><table><thead><tr><th>Test</th><th>Template</th><th>Status</th><th>Updated</th></tr></thead><tbody>${d.inspections.map(i=>`<tr data-inspection=\"${i.id}\" style=\"cursor:pointer\"><td><strong>${esc(i.title)}</strong></td><td>${esc(i.template_name||'')}</td><td>${pill(i.status)}</td><td>${fmtTime(i.updated_at)}</td></tr>`).join('')}</tbody></table></div>`:'<div class=\"card empty\">No asset tests yet.</div>'}`,true);$('#copyAssetLink').onclick=()=>{navigator.clipboard?.writeText(`${location.origin}/?asset=${eid}`);toast('Asset QR link copied')};$('#assetStatus').onsubmit=async ev=>{ev.preventDefault();await api(`/api/equipment/${eid}`,{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(ev.target)))});toast('Asset status updated');$('#modal').remove();await loadAll();render()};$('#assetEvidence').onclick=()=>{const m=$('#modal');m.remove();openEvidenceUpload()};$$('[data-inspection]').forEach(r=>r.onclick=()=>openInspection(r.dataset.inspection))}catch(err){toast(err.message)}}\n\nasync function openScanner(){if(!('BarcodeDetector'in window)){modal('Scan asset',`<div class=\"banner\"><div>\u25a3</div><div><strong>Camera QR scanning is not supported in this browser.</strong><p>Use the manual tag search below, or open Qx in a current Chromium/Android browser.</p></div></div><div class=\"field\"><label>Asset tag</label><input id=\"manualAsset\" placeholder=\"AHU-01\"></div><button class=\"btn btn-primary\" id=\"findAsset\">Find asset</button>`);$('#findAsset').onclick=()=>{const tag=$('#manualAsset').value.trim().toLowerCase(),e=state.equipment.find(x=>String(x.tag).toLowerCase()===tag);if(!e)return toast('Asset tag not found');$('#modal').remove();openAssetPassport(e.id)};return}modal('Scan asset QR',`<video id=\"scanVideo\" autoplay playsinline style=\"width:100%;border-radius:14px;background:#000;min-height:300px\"></video><p class=\"muted\">Point the camera at a Qx asset QR code.</p>`,true);try{const stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}}),video=$('#scanVideo');video.srcObject=stream;const detector=new BarcodeDetector({formats:['qr_code']});const tick=async()=>{if(!document.body.contains(video)){stream.getTracks().forEach(t=>t.stop());return}try{const codes=await detector.detect(video);if(codes[0]){const raw=codes[0].rawValue||'',u=new URL(raw,location.origin),eid=u.searchParams.get('asset');if(eid){stream.getTracks().forEach(t=>t.stop());$('#modal').remove();openAssetPassport(eid);return}}}catch{}requestAnimationFrame(tick)};tick()}catch(e){toast('Camera access unavailable')};}\n\nasync function runBuilder(){if(!state.projectId)return toast('Select a project first');const text=$('#builderText')?.value.trim();if(!text)return toast('Paste project text first');const box=$('#builderResult');box.innerHTML='<div class=\"empty\">Analysing project text\u2026</div>';try{const r=await api('/api/ai/project-builder',{method:'POST',body:JSON.stringify({project_id:state.projectId,source_text:text})});const eq=r.proposal?.equipment||[];box.innerHTML=`<div class=\"eyebrow\">REVIEW PROPOSAL</div><h3>${eq.length} equipment tags identified</h3><p class=\"muted\">${esc(r.proposal?.notes||'')}</p><div class=\"tag-cloud\">${eq.slice(0,120).map(e=>`<span>${esc(e.tag)}</span>`).join('')}</div><button class=\"btn btn-primary\" id=\"applyBuilder\" ${eq.length?'':'disabled'}>Apply ${eq.length} assets to project</button>`;$('#applyBuilder')?.addEventListener('click',async()=>{const a=await api('/api/ai/project-builder/apply',{method:'POST',body:JSON.stringify({job_id:r.id})});await loadAll();toast(`${a.created} assets created \u2022 ${a.skipped} skipped`);state.view='equipment';render()})}catch(e){box.innerHTML=`<div class=\"error\">${esc(e.message)}</div>`}}\n\nfunction exportEquipmentCsv(){const rows=[['tag','name','system','subsystem','building','level','area','equipment_type','manufacturer','model','serial_number','status'],...state.equipment.map(e=>[e.tag,e.name,e.system,e.subsystem,e.building,e.level,e.area,e.equipment_type,e.manufacturer,e.model,e.serial_number,e.status])];const q=v=>`\"${String(v??'').replaceAll('\"','\"\"')}\"`,csv=rows.map(r=>r.map(q).join(',')).join('\\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=`${currentProject()?.code||'qx'}-equipment.csv`;a.click();URL.revokeObjectURL(a.href)}\n\nfunction openUserForm(){modal('Add Qx user',`<form id=\"userForm\"><div class=\"formgrid\"><div class=\"field\"><label>Name</label><input name=\"name\" required></div><div class=\"field\"><label>Email</label><input name=\"email\" type=\"email\" required></div><div class=\"field\"><label>Role</label><select name=\"role\"><option value=\"technician\">Technician</option><option value=\"engineer\">Engineer</option><option value=\"pm\">Project Manager</option><option value=\"admin\">Administrator</option></select></div><div class=\"field\"><label>Temporary password</label><input name=\"password\" type=\"password\" minlength=\"12\" required></div></div><button class=\"btn btn-primary\">Create user</button></form>`);formSubmit($('#userForm'),'/api/users','User created')}\n\nasync function openInspection(iid){try{const d=await api(`/api/inspections/${iid}`);const i=d.inspection;modal(i.title,`<div class=\"banner\"><div>\u2713</div><div><strong>${esc(i.template_name)}</strong><p>${esc(i.equipment_tag||'Project/system test')} \u2022 ${esc(i.discipline)} \u2022 ${esc(i.stage)}</p></div></div><div id=\"results\">${d.results.map(r=>`<div class=\"result-row\"><div class=\"meta\"><span>${esc(r.section)}</span>${r.hold_point?'<span class=\"pill witness_required\">HOLD POINT</span>':''}</div><h4>${esc(r.item_text)}</h4><div class=\"result-buttons\" data-result-id=\"${r.id}\"><button class=\"btn btn-small ${r.result==='pass'?'selected pass':''}\" data-value=\"pass\">PASS</button><button class=\"btn btn-small ${r.result==='fail'?'selected fail':''}\" data-value=\"fail\">FAIL</button><button class=\"btn btn-small ${r.result==='na'?'selected na':''}\" data-value=\"na\">N/A</button></div></div>`).join('')}</div>`,true);$$('[data-result-id] button').forEach(b=>b.onclick=async()=>{const row=b.closest('[data-result-id]');try{const r=await api(`/api/inspection-results/${row.dataset.resultId}`,{method:'PATCH',body:JSON.stringify({result:b.dataset.value})});row.querySelectorAll('button').forEach(x=>x.className='btn btn-small');b.classList.add('selected',b.dataset.value);toast(b.dataset.value==='fail'?'Failed check \u2014 Category A punch auto-created':`Result saved \u2022 ${r.status.replaceAll('_',' ')}`);await loadAll()}catch(err){toast(err.message)}})}catch(err){toast(err.message)}}\n\nasync function openPoint(pid){const p=state.points.find(x=>x.id===pid);if(!p)return;modal(`I/O Checkout \u2014 ${p.point_name}`,`<div class=\"banner\"><div>\u21af</div><div><strong>${esc(p.controller||'Controller')} \u2022 ${esc(p.point_type)}</strong><p>${esc([p.io_address,p.cable_number,p.terminal].filter(Boolean).join(' \u2022 ')||'Field point')}</p></div></div><form id=\"pf\"><div class=\"formgrid\"><div class=\"field\"><label>Field device</label><select name=\"field_status\">${statusOpts(p.field_status)}</select></div><div class=\"field\"><label>Controller I/O</label><select name=\"controller_status\">${statusOpts(p.controller_status)}</select></div><div class=\"field\"><label>Graphics / HMI</label><select name=\"graphics_status\">${statusOpts(p.graphics_status)}</select></div><div class=\"field\"><label>Alarm / sequence</label><select name=\"alarm_status\">${statusOpts(p.alarm_status)}</select></div><div class=\"field span2\"><label>Comment</label><textarea name=\"comment\">${esc(p.comment||'')}</textarea></div></div><button class=\"btn btn-primary\">Save point test</button></form>`);$('#pf').onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));try{await api(`/api/bms-points/${pid}`,{method:'PATCH',body:JSON.stringify(data)});$('#modal').remove();await loadAll();render();toast('I/O point updated')}catch(err){toast(err.message)}}}\nfunction statusOpts(v){return ['not_tested','pass','fail','na'].map(x=>`<option value=\"${x}\" ${x===v?'selected':''}>${x.replaceAll('_',' ')}</option>`).join('')}\n\nfunction openPointImport(){if(!currentProject()&&!state.projectId)return toast('Select a project before importing points');modal('Import BMS I/O points from CSV',`<div class=\"banner\"><div>\u21e7</div><div><strong>Fast point-list ingestion</strong><p>Paste CSV exported from Excel/Simpro/design schedules. Required columns: point_name, point_type. Optional: controller, io_address, cable_number, terminal, field_device, engineering_units, normal_state.</p></div></div><div class=\"field\"><label>CSV data</label><textarea id=\"csv\" style=\"min-height:260px\" placeholder=\"controller,point_name,point_type,io_address,cable_number,terminal\\nJACE-01,AHU-01 Supply Air Temp,AI,UI1,BMS-001,X1:1\\nJACE-01,AHU-01 Fan Enable,DO,UO1,BMS-002,X1:2\"></textarea></div><button class=\"btn btn-primary\" id=\"doImport\">Import points</button>`,true);$('#doImport').onclick=async()=>{try{const rows=parseCSV($('#csv').value);if(!rows.length)throw new Error('No valid CSV rows found');const r=await api('/api/bms-points/import',{method:'POST',body:JSON.stringify({project_id:state.projectId,points:rows})});$('#modal').remove();await loadAll();render();toast(`${r.created} points imported`)}catch(err){toast(err.message)}}}\nfunction parseCSV(text){const lines=text.trim().split(/\\r?\\n/).filter(Boolean);if(lines.length<2)return[];const split=line=>{const out=[];let cur='',q=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='\"'){if(q&&line[i+1]==='\"'){cur+='\"';i++}else q=!q}else if(c===','&&!q){out.push(cur.trim());cur=''}else cur+=c}out.push(cur.trim());return out};const headers=split(lines[0]).map(x=>x.toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_|_$/g,''));return lines.slice(1).map(line=>{const vals=split(line),o={};headers.forEach((h,i)=>o[h]=vals[i]||'');return o}).filter(x=>x.point_name&&x.point_type)}\n\nasync function openDefect(did){const d=state.defects.find(x=>x.id===did);if(!d)return;modal(`Punch ${d.category} \u2014 ${d.title}`,`<div class=\"banner\"><div>!</div><div><strong>${esc(d.equipment_tag||'Project / system')}</strong><p>${esc(d.description||'No description')}</p></div></div><div class=\"formgrid\"><div class=\"card\"><div class=\"eyebrow\">Status</div><div style=\"margin-top:8px\">${pill(d.status)}</div></div><div class=\"card\"><div class=\"eyebrow\">Owner</div><div style=\"margin-top:8px\">${esc(d.assigned_name||'Unassigned')}</div></div></div><form id=\"df\" style=\"margin-top:12px\"><div class=\"field\"><label>Update status</label><select name=\"status\"><option value=\"open\">Open</option><option value=\"assigned\">Assigned</option><option value=\"rectified\">Rectified \u2014 ready to verify</option><option value=\"verified\">Verified</option><option value=\"closed\">Closed</option></select></div><div class=\"field\"><label>Rectification / verification note</label><textarea name=\"rectification_note\">${esc(d.rectification_note||'')}</textarea></div><button class=\"btn btn-primary\">Update punch item</button></form>`);$('#df').onsubmit=async e=>{e.preventDefault();try{await api(`/api/defects/${did}`,{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});$('#modal').remove();await loadAll();render();toast('Punch item updated')}catch(err){toast(err.message)}}}\n\nasync function openTurnover(tid){try{const d=await api(`/api/turnover/${tid}`);modal(d.package.name,`<div class=\"banner\"><div>\u25a3</div><div><strong>${Number(d.package.readiness_percent||0)}% ready for handover</strong><p>${esc(d.package.system_code||'Project-wide')} \u2022 ${d.package.complete_items||0}/${d.package.required_items||0} required items complete</p></div></div><div class=\"table-wrap\"><table><thead><tr><th>Category</th><th>Required evidence / gate</th><th>Status</th></tr></thead><tbody>${d.items.map(x=>`<tr><td>${esc(x.category)}</td><td><strong>${esc(x.name)}</strong>${x.note?`<br><span class=\"muted\">${esc(x.note)}</span>`:''}</td><td><select class=\"mini-select\" data-toi=\"${x.id}\"><option value=\"missing\" ${x.status==='missing'?'selected':''}>Missing</option><option value=\"in_progress\" ${x.status==='in_progress'?'selected':''}>In progress</option><option value=\"complete\" ${x.status==='complete'?'selected':''}>Complete</option><option value=\"accepted\" ${x.status==='accepted'?'selected':''}>Accepted</option></select></td></tr>`).join('')}</tbody></table></div>`,true);$$('[data-toi]').forEach(s=>s.onchange=async()=>{try{await api(`/api/turnover-items/${s.dataset.toi}`,{method:'PATCH',body:JSON.stringify({status:s.value})});toast('Turnover gate updated');await loadAll()}catch(err){toast(err.message)}})}catch(err){toast(err.message)}}\n\n\ndocument.addEventListener('click',async e=>{const b=e.target.closest('[data-route]');if(b){state.view=b.dataset.route;render();return}if(e.target.id==='openDossier'||e.target.id==='openDossier2'){window.open(`/api/handover/dossier?project_id=${encodeURIComponent(state.projectId)}`,'_blank');return}if(e.target.id==='projectSignoff'){openSignature();return}if(e.target.id==='addDocument'){openDocumentForm();return}if(e.target.id==='refreshCopilot'){await loadCloseout();render();return}if(e.target.id==='createShare'){try{const r=await api('/api/client-share',{method:'POST',body:JSON.stringify({project_id:state.projectId,label:'Client live status',expires_days:30})});await navigator.clipboard?.writeText(r.url);modal('Client live status link',`<div class=\"banner\"><div>\u2197</div><div><strong>Read-only client link created</strong><p>Expires ${fmtDate(r.expires_at)}. The link has also been copied to your clipboard.</p></div></div><div class=\"field\"><input value=\"${esc(r.url)}\" readonly></div><a class=\"btn btn-primary\" href=\"${esc(r.url)}\" target=\"_blank\">Open client view</a>`)}catch(err){toast(err.message)}}});\nconst _renderV6=render;render=function(){_renderV6();if(state.view==='closeout'&&!state.copilot){loadCloseout().then(()=>{if(state.view==='closeout'){const c=state.copilot;_renderV6()}}).catch(()=>{})}};\n\nwindow.addEventListener('online',()=>{updateQueueBadge();toast('Connection restored \u2014 synchronising queued field updates');flushQueue()});window.addEventListener('offline',()=>{updateQueueBadge();toast('Offline field mode \u2014 updates will queue and sync automatically')});\n\nconst _loadAllV7=loadAll;loadAll=async function(){await _loadAllV7();await loadV7()};\ndocument.addEventListener('click',async e=>{if(e.target.id==='addDrawing')return openDrawingForm();if(e.target.id==='refreshAlerts'){await api('/api/notifications/scan',{method:'POST',body:JSON.stringify({project_id:state.projectId||null})});await loadV7();render();toast('Risk scan complete');return}const r=e.target.closest('[data-drawing]');if(r)return openDrawing(r.dataset.drawing)});\n\n\nconst _loadAllV8=loadAll;loadAll=async function(){await _loadAllV8();await loadV8()};\ndocument.addEventListener('click',async e=>{const tc=e.target.closest('[data-template]');if(tc){openTemplate(tc.dataset.template);return}if(e.target.id==='newTemplate'){openTemplateForm();return}const p=e.target.closest('[data-open-project]');if(p){state.projectId=p.dataset.openProject;state.view='dashboard';await loadAll();render();return}if(e.target.id==='downloadPack')return downloadOfflinePack();if(e.target.id==='addObservation')return openObservationForm();if(e.target.id==='addMilestone')return openMilestoneForm();if(e.target.id==='applySmart'){try{const r=await api('/api/ai/form-builder/apply',{method:'POST',body:JSON.stringify({job_id:e.target.dataset.job})});state.smartForm=null;await loadAll();state.view='templates';render();toast(`Template created \u2022 ${r.item_count} checks`)}catch(err){toast(err.message)}}});\ndocument.addEventListener('submit',async e=>{if(e.target.id==='smartForm'){e.preventDefault();try{const d=Object.fromEntries(new FormData(e.target));d.project_id=state.projectId;const r=await api('/api/ai/form-builder',{method:'POST',body:JSON.stringify(d)});state.smartForm=r;render()}catch(err){toast(err.message)}}});\nboot();\nsetTimeout(()=>flushQueue(),1200);\n" }],
  ["/styles.css", { type: "text/css; charset=utf-8", body: ":root{--bg:#f4f7fb;--panel:#ffffff;--panel2:#f7f9fc;--line:#dce4ed;--text:#172033;--muted:#6f7e90;--blue:#2f80ed;--cyan:#26a6d1;--green:#17a56b;--amber:#e79a13;--red:#e34b54;--purple:#7457d9;--shadow:0 10px 28px rgba(25,45,70,.08);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;color-scheme:light}*{box-sizing:border-box}body{margin:0;background:#f4f7fb;color:var(--text);min-height:100vh}button,input,select,textarea{font:inherit}.boot{min-height:100vh;display:flex;align-items:center;justify-content:center;gap:14px}.boot span{display:block;color:var(--muted);font-size:13px;margin-top:4px}.brandmark{width:52px;height:52px;border-radius:16px;display:grid;place-items:center;font-weight:900;letter-spacing:-1px;background:linear-gradient(135deg,var(--blue),var(--cyan));color:#06101e;box-shadow:0 12px 30px rgba(67,214,208,.22)}.auth{min-height:100vh;display:grid;place-items:center;padding:24px}.auth-card{width:min(460px,100%);background:rgba(14,26,44,.92);border:1px solid var(--line);border-radius:24px;padding:28px;box-shadow:var(--shadow);backdrop-filter:blur(18px)}.auth-card h1{margin:16px 0 5px;font-size:30px}.auth-card p{color:var(--muted);line-height:1.5}.field{display:grid;gap:7px;margin:14px 0}.field label{font-size:12px;color:#b8c4d5;font-weight:700;letter-spacing:.02em}.field input,.field select,.field textarea{width:100%;border:1px solid #30435f;background:#081424;color:var(--text);border-radius:12px;padding:11px 12px;outline:none}.field input:focus,.field select:focus,.field textarea:focus{border-color:var(--blue);box-shadow:0 0 0 3px rgba(90,168,255,.12)}.field textarea{min-height:90px;resize:vertical}.btn{border:1px solid transparent;border-radius:11px;padding:10px 14px;font-weight:800;cursor:pointer;color:var(--text);background:#1a2a42;transition:.18s}.btn:hover{transform:translateY(-1px);filter:brightness(1.07)}.btn-primary{background:linear-gradient(135deg,#2d8cff,#36c9c4);color:#03101d}.btn-danger{background:#401923;border-color:#6c2735}.btn-small{padding:7px 10px;font-size:12px}.layout{display:grid;grid-template-columns:250px 1fr;min-height:100vh}.sidebar{position:sticky;top:0;height:100vh;border-right:1px solid var(--line);background:rgba(7,16,30,.92);backdrop-filter:blur(18px);padding:18px 12px;display:flex;flex-direction:column;z-index:5}.brand{display:flex;align-items:center;gap:10px;padding:4px 8px 20px}.brand .brandmark{width:40px;height:40px;border-radius:12px}.brand b{font-size:16px}.brand small{display:block;color:var(--muted);margin-top:2px}.navlabel{font-size:10px;text-transform:uppercase;letter-spacing:.13em;color:#63738a;font-weight:900;padding:10px 10px 6px}.navbtn{display:flex;gap:11px;align-items:center;width:100%;border:0;background:transparent;color:#aebbd0;border-radius:10px;padding:10px 11px;text-align:left;cursor:pointer;font-size:13px;font-weight:700}.navbtn:hover{background:#101e32;color:white}.navbtn.active{background:linear-gradient(90deg,rgba(90,168,255,.18),rgba(67,214,208,.08));color:#fff;border:1px solid rgba(90,168,255,.19)}.navicon{width:20px;text-align:center;color:var(--blue)}.sidefoot{margin-top:auto;border-top:1px solid var(--line);padding:14px 8px 4px}.userline{display:flex;align-items:center;gap:10px}.avatar{width:34px;height:34px;border-radius:10px;background:#182b45;display:grid;place-items:center;font-weight:900;color:var(--cyan)}.userline small{display:block;color:var(--muted)}.main{min-width:0}.topbar{height:68px;border-bottom:1px solid var(--line);display:flex;align-items:center;gap:12px;padding:0 24px;position:sticky;top:0;background:rgba(8,18,32,.86);backdrop-filter:blur(18px);z-index:4}.project-picker{min-width:260px;max-width:380px;border:1px solid var(--line);background:#0e1a2c;color:var(--text);border-radius:10px;padding:9px 11px}.online{margin-left:auto;font-size:11px;font-weight:900;border:1px solid var(--line);border-radius:999px;padding:6px 9px;color:var(--green)}.online.off{color:var(--amber)}.content{padding:24px;max-width:1700px;margin:0 auto}.hero{display:flex;justify-content:space-between;gap:20px;align-items:flex-end;margin-bottom:20px}.eyebrow{font-size:10px;text-transform:uppercase;letter-spacing:.16em;color:var(--cyan);font-weight:900}.hero h1{font-size:28px;margin:6px 0 2px;letter-spacing:-.03em}.hero p{margin:0;color:var(--muted);font-size:13px}.actions{display:flex;gap:8px;flex-wrap:wrap}.cards{display:grid;grid-template-columns:repeat(6,minmax(140px,1fr));gap:12px;margin-bottom:18px}.card{background:linear-gradient(145deg,rgba(18,34,56,.93),rgba(12,25,43,.94));border:1px solid var(--line);border-radius:16px;padding:16px;box-shadow:0 8px 26px rgba(0,0,0,.12)}.kpi .label{color:var(--muted);font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.metric{font-size:30px;font-weight:900;letter-spacing:-.04em;margin:8px 0 4px}.sub{font-size:11px;color:var(--muted)}.metric.green{color:var(--green)}.metric.amber{color:var(--amber)}.metric.red{color:var(--red)}.metric.blue{color:var(--blue)}.grid2{display:grid;grid-template-columns:1.25fr .75fr;gap:16px}.grid3{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.section-head{display:flex;justify-content:space-between;align-items:center;margin:22px 0 10px}.section-head h3{margin:0;font-size:15px}.section-head span{font-size:11px;color:var(--muted)}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:15px;background:rgba(12,24,41,.85)}table{width:100%;border-collapse:collapse;font-size:12px}th{position:sticky;top:0;background:#111f33;color:#8495ad;text-transform:uppercase;letter-spacing:.06em;font-size:10px;text-align:left;padding:11px 12px;border-bottom:1px solid var(--line)}td{padding:11px 12px;border-bottom:1px solid rgba(36,52,77,.72);vertical-align:middle}tr:last-child td{border-bottom:0}tbody tr:hover{background:rgba(90,168,255,.045)}.muted{color:var(--muted)}.pill{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:4px 8px;font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.03em;background:#1a2a42;color:#c6d1df;white-space:nowrap}.pill.pass,.pill.complete,.pill.approved,.pill.ready,.pill.closed,.pill.verified{background:rgba(66,211,146,.13);color:#6ce4aa}.pill.fail,.pill.critical,.pill.blocked,.pill.rejected,.pill.a{background:rgba(255,107,114,.13);color:#ff8b91}.pill.witness_required,.pill.requested,.pill.medium,.pill.b{background:rgba(244,189,80,.13);color:#ffd477}.pill.in_progress,.pill.active,.pill.commissioning,.pill.blue{background:rgba(90,168,255,.14);color:#88beff}.pill.c{background:rgba(168,139,255,.14);color:#c3adff}.progress{height:7px;border-radius:99px;background:#1b2a40;overflow:hidden;min-width:90px}.progress>i{display:block;height:100%;background:linear-gradient(90deg,var(--blue),var(--cyan));border-radius:99px}.readiness{display:flex;align-items:center;gap:8px}.empty{padding:36px;text-align:center;color:var(--muted)}.modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.62);display:grid;place-items:center;padding:18px;z-index:20}.modal{width:min(760px,100%);max-height:92vh;overflow:auto;background:#0d1a2c;border:1px solid #314460;border-radius:20px;padding:20px;box-shadow:var(--shadow)}.modal.wide{width:min(1120px,100%)}.modal-head{display:flex;justify-content:space-between;gap:16px;align-items:start;margin-bottom:10px}.modal h2{margin:3px 0 0}.formgrid{display:grid;grid-template-columns:1fr 1fr;gap:0 12px}.span2{grid-column:1/-1}.result-row{border:1px solid var(--line);border-radius:13px;padding:13px;margin:9px 0;background:#0a1627}.result-row h4{margin:4px 0 10px;font-size:13px;line-height:1.35}.result-row .meta{display:flex;gap:7px;align-items:center;color:var(--muted);font-size:10px}.result-buttons{display:flex;gap:7px;flex-wrap:wrap}.result-buttons button.selected.pass{background:rgba(66,211,146,.18);border-color:#338965;color:#7aebb6}.result-buttons button.selected.fail{background:rgba(255,107,114,.18);border-color:#9a3c45;color:#ff9fa4}.result-buttons button.selected.na{background:rgba(168,139,255,.16);border-color:#6954a4}.tabs{display:flex;gap:8px;border-bottom:1px solid var(--line);margin-bottom:14px;overflow:auto}.tab{border:0;background:none;color:var(--muted);padding:10px 4px;margin-right:12px;font-weight:800;font-size:12px;cursor:pointer;border-bottom:2px solid transparent;white-space:nowrap}.tab.active{color:white;border-color:var(--blue)}.matrix{display:grid;grid-template-columns:220px repeat(4,1fr) 100px;gap:1px;background:var(--line);border:1px solid var(--line);border-radius:12px;overflow:hidden;font-size:11px}.matrix>div{background:#0d192a;padding:9px}.matrix .head{background:#14233a;color:#91a0b5;font-size:9px;text-transform:uppercase;font-weight:900}.matrix select{width:100%;background:#0a1525;border:1px solid #2a3b55;color:white;border-radius:7px;padding:6px}.score-ring{width:88px;height:88px;border-radius:50%;display:grid;place-items:center;background:conic-gradient(var(--green) calc(var(--p)*1%),#1b2a40 0);position:relative}.score-ring:after{content:\"\";position:absolute;inset:8px;background:#0d1a2c;border-radius:50%}.score-ring b{z-index:1;font-size:20px}.activity{display:grid;gap:8px}.activity-item{padding:10px;border-left:2px solid #2c466b;background:#0b1728;border-radius:0 10px 10px 0;font-size:11px}.activity-item b{color:#cdd6e3}.activity-item small{display:block;color:var(--muted);margin-top:4px}.toolbar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:10px}.search{min-width:230px;flex:1;border:1px solid var(--line);background:#0d1a2c;color:#fff;border-radius:10px;padding:9px 11px}.mini-select{border:1px solid var(--line);background:#0d1a2c;color:#fff;border-radius:10px;padding:9px 11px}.banner{border:1px solid rgba(90,168,255,.25);background:linear-gradient(90deg,rgba(90,168,255,.10),rgba(67,214,208,.07));border-radius:15px;padding:15px;margin-bottom:16px;display:flex;gap:13px;align-items:flex-start}.banner strong{display:block}.banner p{margin:4px 0 0;color:var(--muted);font-size:12px;line-height:1.5}.error{background:#3b1720;border:1px solid #6a2632;color:#ff9fa6;padding:10px;border-radius:10px;font-size:12px;margin-bottom:10px}.toast{position:fixed;right:20px;bottom:20px;background:#13253d;border:1px solid #31517c;border-radius:12px;padding:11px 14px;box-shadow:var(--shadow);z-index:30;font-size:12px}.mobile-menu{display:none}.hide{display:none!important}\n@media(max-width:1200px){.cards{grid-template-columns:repeat(3,1fr)}.grid2{grid-template-columns:1fr}.grid3{grid-template-columns:1fr 1fr}}\n@media(max-width:820px){.layout{grid-template-columns:1fr}.sidebar{position:fixed;left:-270px;width:250px;transition:.2s;box-shadow:var(--shadow)}.sidebar.open{left:0}.mobile-menu{display:inline-flex}.topbar{padding:0 12px}.project-picker{min-width:0;max-width:58vw}.content{padding:16px}.cards{grid-template-columns:1fr 1fr}.hero{align-items:flex-start;flex-direction:column}.grid3{grid-template-columns:1fr}.formgrid{grid-template-columns:1fr}.span2{grid-column:auto}.matrix{grid-template-columns:170px repeat(4,100px) 80px;overflow:auto}.modal{padding:16px}}\n@media(max-width:500px){.cards{grid-template-columns:1fr 1fr}.metric{font-size:25px}.content{padding:12px}.actions{width:100%}.actions .btn{flex:1}.topbar{height:60px}.project-picker{max-width:65vw}}\n.intel-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:16px}.readiness-ring{width:150px;height:150px;border-radius:50%;display:grid;place-items:center;background:conic-gradient(currentColor calc(var(--p)*1%),#e8edf4 0);position:relative;margin:auto}.readiness-ring:after{content:'';position:absolute;inset:14px;background:white;border-radius:50%}.readiness-ring strong{position:relative;z-index:1;font-size:34px}.blocker{display:flex;gap:12px;align-items:flex-start;padding:12px 0;border-bottom:1px solid #e8edf4}.blocker:last-child{border-bottom:0}.sev{width:9px;height:9px;border-radius:50%;margin-top:6px;background:#d97706}.sev.critical{background:#dc2626}.sev.high{background:#ea580c}.switch-reason{padding:14px;border:1px solid #e8edf4;border-radius:12px;background:#fff}.switch-reason b{display:block;margin-bottom:4px}@media(max-width:900px){.intel-grid{grid-template-columns:1fr}}\n\n/* Qx V5 market-leader field UX additions */\n.matrix-wrap table{min-width:1040px}.matrix-cell{min-width:92px;padding:8px 10px;border-radius:10px;display:flex;flex-direction:column;gap:2px;align-items:center}.matrix-cell b{font-size:15px}.matrix-cell span{font-size:9px;text-transform:uppercase;letter-spacing:.08em}.matrix-good{background:rgba(52,211,153,.12);color:#6ee7b7}.matrix-near{background:rgba(56,189,248,.12);color:#7dd3fc}.matrix-risk{background:rgba(251,191,36,.12);color:#fcd34d}.matrix-empty{background:rgba(148,163,184,.08);color:#94a3b8}.gate-pass,.gate-block{padding:9px 10px;border-radius:10px;text-align:center;font-weight:800;font-size:10px;letter-spacing:.08em}.gate-pass{background:rgba(52,211,153,.12);color:#6ee7b7}.gate-block{background:rgba(248,113,113,.13);color:#fda4af}.score-big{font-size:20px;font-weight:900}.score-big.green{color:#6ee7b7}.score-big.amber{color:#fcd34d}.score-big.red{color:#fda4af}\n.evidence-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.evidence-card{display:flex;gap:12px;align-items:center;background:var(--card);border:1px solid var(--line);border-radius:14px;padding:14px;text-decoration:none;color:var(--text);transition:.16s ease}.evidence-card:hover{transform:translateY(-1px);border-color:#38bdf8}.evidence-card .file-icon{width:44px;height:44px;border-radius:12px;background:rgba(56,189,248,.12);display:grid;place-items:center;font-size:20px}.evidence-card strong,.evidence-card span,.evidence-card small{display:block}.evidence-card span,.evidence-card small{font-size:10px;color:var(--muted);margin-top:3px}\n.passport-head{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:18px}.passport-head h2{margin:5px 0}.passport-head p{color:var(--muted);margin:0}.passport-score{display:flex;align-items:center}.detail-list{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:14px}.detail-list div{padding:10px;background:rgba(148,163,184,.05);border-radius:10px}.detail-list span,.detail-list b{display:block}.detail-list span{font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}.detail-list b{font-size:12px;margin-top:3px}.file-row{display:flex;justify-content:space-between;gap:10px;padding:10px 0;border-bottom:1px solid var(--line);color:var(--text);text-decoration:none}.file-row span{color:var(--muted);font-size:10px}.tag-cloud{display:flex;flex-wrap:wrap;gap:7px;margin:14px 0 18px;max-height:300px;overflow:auto}.tag-cloud span{padding:6px 9px;border-radius:999px;background:rgba(56,189,248,.1);border:1px solid rgba(56,189,248,.22);font-size:10px}.report-sheet{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:22px}.report-brand{display:flex;gap:14px;align-items:center;margin-bottom:16px}.report-brand h2,.report-brand p{margin:0}.report-brand p{color:var(--muted);font-size:11px}\n@media(max-width:900px){.evidence-grid{grid-template-columns:1fr}.passport-head{flex-direction:column}.detail-list{grid-template-columns:1fr}.topbar #scanAsset{display:none}}\n@media print{body{background:white!important;color:#111!important}.sidebar,.topbar,.hero .actions,.toast,.modal-backdrop{display:none!important}.main{margin:0!important}.content{padding:0!important}.report-sheet,.card,.table-wrap{background:white!important;color:#111!important;border-color:#ddd!important;box-shadow:none!important}.muted,.sub,.label,.eyebrow{color:#555!important}.progress{border:1px solid #aaa!important}.progress i{background:#333!important}.report-sheet{border:0!important}.report-sheet .hero{display:none!important}}\n\n\n/* Qx V8 head-to-head UX */\n.portfolio-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.project-tile{appearance:none;text-align:left;color:var(--text);background:linear-gradient(145deg,rgba(16,34,57,.98),rgba(10,23,39,.98));border:1px solid var(--line);border-radius:18px;padding:18px;cursor:pointer;box-shadow:0 12px 30px rgba(0,0,0,.12);transition:.18s}.project-tile:hover{transform:translateY(-2px);border-color:#3b82f6}.tile-top,.tile-foot,.mini-metrics,.score-line{display:flex;align-items:center;justify-content:space-between;gap:10px}.tile-top h3{margin:4px 0 14px;font-size:16px}.tile-top span,.tile-foot,.mini-metrics{font-size:10px;color:var(--muted)}.score-line strong{font-size:28px}.score-line .readiness{flex:1}.mini-metrics{margin:16px 0;padding:12px 0;border-top:1px solid var(--line);border-bottom:1px solid var(--line)}.mini-metrics span{display:flex;flex-direction:column;gap:3px}.mini-metrics b{font-size:13px;color:var(--text)}.risk-pill{padding:5px 8px;border-radius:999px;font-size:9px;font-weight:900;text-transform:uppercase;white-space:nowrap}.risk-pill.on_track{background:rgba(52,211,153,.13);color:#6ee7b7}.risk-pill.watch{background:rgba(56,189,248,.13);color:#7dd3fc}.risk-pill.at_risk{background:rgba(251,191,36,.13);color:#fcd34d}.risk-pill.critical{background:rgba(248,113,113,.14);color:#fda4af}.field-status-strip{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:14px}.field-status-strip>div{background:var(--card);border:1px solid var(--line);border-radius:13px;padding:12px}.field-status-strip span,.field-status-strip b{display:block}.field-status-strip span{font-size:9px;text-transform:uppercase;color:var(--muted);letter-spacing:.08em}.field-status-strip b{margin-top:5px;font-size:13px}.field-launcher{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:16px}.field-launcher button{appearance:none;color:var(--text);background:linear-gradient(145deg,#112641,#0c1c31);border:1px solid #29415f;border-radius:18px;min-height:140px;padding:18px;text-align:left;cursor:pointer;transition:.16s}.field-launcher button:hover{border-color:#55b7ff;transform:translateY(-2px)}.field-launcher i,.field-launcher b,.field-launcher span{display:block}.field-launcher i{font-style:normal;font-size:28px;color:#6ed9ff;margin-bottom:14px}.field-launcher b{font-size:16px}.field-launcher span{margin-top:5px;color:var(--muted);font-size:11px}.timeline{max-width:900px}.timeline-row{display:grid;grid-template-columns:110px 22px 1fr;gap:12px;min-height:90px}.timeline-date{font-size:11px;color:var(--muted);padding-top:18px;text-align:right}.timeline-dot{position:relative}.timeline-dot:before{content:'';position:absolute;left:9px;top:0;bottom:0;width:2px;background:var(--line)}.timeline-dot:after{content:'';position:absolute;left:4px;top:20px;width:12px;height:12px;border-radius:50%;background:#3b82f6;border:2px solid #0b1627}.timeline-row.complete .timeline-dot:after{background:#34d399}.timeline-row.at_risk .timeline-dot:after{background:#fbbf24}.timeline-card{display:flex;justify-content:space-between;gap:15px;background:var(--card);border:1px solid var(--line);border-radius:14px;padding:14px;margin-bottom:10px}.timeline-card span{font-size:9px;text-transform:uppercase;color:var(--muted)}.timeline-card h3{font-size:14px;margin:4px 0}.timeline-card p{margin:0;color:var(--muted);font-size:10px}.smart-checks{display:grid;gap:8px;margin:14px 0;max-height:520px;overflow:auto}.smart-checks>div{display:grid;grid-template-columns:30px 1fr auto;gap:10px;align-items:start;padding:10px;border:1px solid var(--line);border-radius:10px;background:rgba(148,163,184,.04)}.smart-checks>div>span{width:26px;height:26px;display:grid;place-items:center;border-radius:8px;background:rgba(56,189,248,.12);font-size:10px}.smart-checks p{margin:0;font-size:11px;line-height:1.45}.smart-checks em{font-style:normal;font-size:9px;font-weight:900;color:#fcd34d;background:rgba(251,191,36,.1);padding:4px 6px;border-radius:6px}@media(max-width:900px){.portfolio-grid{grid-template-columns:1fr}.field-launcher{grid-template-columns:1fr 1fr}.field-status-strip{grid-template-columns:1fr 1fr}}@media(max-width:520px){.field-launcher{grid-template-columns:1fr}.field-launcher button{min-height:105px}.mini-metrics{display:grid;grid-template-columns:1fr 1fr}.timeline-row{grid-template-columns:70px 18px 1fr}.timeline-date{font-size:9px}}\n\n/* Qx V8.1 Template Library */\n.template-card{font:inherit;width:100%;appearance:none}.template-card:hover{border-color:#3b82f6;transform:translateY(-2px)}.template-card .mini-metrics{margin:12px 0 0}.template-card .pill{font-size:9px}\n\n/* =========================================================\n   Qx V8.2 LIGHT / ELLIOT ESTIMATOR THEME\n   Clean light workspace + navy navigation + orange accent\n   ========================================================= */\n:root{\n  --bg:#f4f7fb;\n  --panel:#ffffff;\n  --panel2:#f8fafc;\n  --card:#ffffff;\n  --line:#dfe6ee;\n  --text:#172033;\n  --muted:#6d7b8d;\n  --blue:#f58220;\n  --cyan:#ff9f45;\n  --green:#16a36a;\n  --amber:#d98b09;\n  --red:#dc4c55;\n  --purple:#7467d8;\n  --navy:#14283d;\n  --navy2:#0e2032;\n  --soft:#eef3f8;\n  --shadow:0 8px 26px rgba(25,45,70,.08);\n  color-scheme:light;\n}\nhtml{background:var(--bg)}\nbody{background:linear-gradient(180deg,#f7f9fc 0,#f3f6fa 100%);color:var(--text)}\n.boot{background:var(--bg)}\n.brandmark{background:linear-gradient(135deg,#f58220,#ffad61);color:#fff;box-shadow:0 10px 26px rgba(245,130,32,.24)}\n.auth{background:linear-gradient(145deg,#eef3f8 0,#f8fafc 48%,#edf2f7 100%)}\n.auth-card{background:#fff;border:1px solid #dfe6ee;box-shadow:0 18px 50px rgba(26,44,65,.12);backdrop-filter:none}\n.auth-card h1{color:#172033}.auth-card p{color:var(--muted)}\n.field label{color:#536274}\n.field input,.field select,.field textarea{border:1px solid #cfd9e5;background:#fff;color:#172033}\n.field input::placeholder,.field textarea::placeholder{color:#9aa7b6}\n.field input:focus,.field select:focus,.field textarea:focus{border-color:#f58220;box-shadow:0 0 0 3px rgba(245,130,32,.14)}\n.btn{color:#263548;background:#edf2f7;border-color:#dbe3ec;box-shadow:0 1px 1px rgba(20,40,61,.03)}\n.btn:hover{filter:none;background:#e7edf4;border-color:#cbd6e2}\n.btn-primary{background:#f58220;color:#fff;border-color:#f58220;box-shadow:0 6px 16px rgba(245,130,32,.18)}\n.btn-primary:hover{background:#e97312;border-color:#e97312}\n.btn-danger{background:#fff1f2;border-color:#fecdd3;color:#b42331}\n.layout{background:var(--bg)}\n.sidebar{background:linear-gradient(180deg,#14283d 0,#102438 100%);border-right:0;box-shadow:8px 0 30px rgba(14,32,50,.06);backdrop-filter:none}\n.brand{border-bottom:1px solid rgba(255,255,255,.08);margin-bottom:8px;padding-bottom:18px}.brand b{color:#fff}.brand small{color:#9fb0c2}\n.navlabel{color:#70869c}\n.navbtn{color:#c4d0dc}.navbtn:hover{background:rgba(255,255,255,.07);color:#fff}\n.navbtn.active{background:#fff;color:#15283c;border:0;box-shadow:0 6px 16px rgba(0,0,0,.12)}\n.navbtn.active .navicon{color:#f58220}.navicon{color:#f6a15a}\n.sidefoot{border-top-color:rgba(255,255,255,.09)}.avatar{background:#f58220;color:#fff}.userline b{color:#fff}.userline small{color:#9fb0c2}\n.main{background:var(--bg)}\n.topbar{background:rgba(255,255,255,.94);border-bottom:1px solid #dfe6ee;backdrop-filter:blur(16px);box-shadow:0 2px 12px rgba(25,45,70,.035)}\n.project-picker,.search,.mini-select{background:#fff;color:#273548;border-color:#d6e0ea}\n.project-picker:focus,.search:focus,.mini-select:focus{outline:none;border-color:#f58220;box-shadow:0 0 0 3px rgba(245,130,32,.11)}\n.online{background:#f6fbf8;border-color:#ccebdc;color:#16835a}.online.off{background:#fff9ed;border-color:#f5dfac;color:#b67605}\n.content{background:transparent}.hero h1{color:#172033}.hero p{color:#6d7b8d}.eyebrow{color:#d9660e}\n.card,.report-sheet,.field-status-strip>div{background:#fff;border-color:#dfe6ee;box-shadow:var(--shadow)}\n.kpi .label,.sub,.muted{color:#738196}.metric{color:#1c2a3b}.metric.green{color:#11835a}.metric.amber{color:#c17b06}.metric.red{color:#cc3f49}.metric.blue{color:#e97516}\n.section-head h3{color:#263548}.section-head span{color:#7c8999}\n.table-wrap{background:#fff;border-color:#dfe6ee;box-shadow:0 6px 22px rgba(25,45,70,.05)}\nth{background:#f6f8fb;color:#68778a;border-bottom-color:#dfe6ee}td{border-bottom-color:#e8edf3;color:#273548}tbody tr:hover{background:#fff8f1}\n.pill{background:#eef2f6;color:#56667a}\n.pill.pass,.pill.complete,.pill.approved,.pill.ready,.pill.closed,.pill.verified{background:#e9f8f1;color:#16714f}\n.pill.fail,.pill.critical,.pill.blocked,.pill.rejected,.pill.a{background:#fff0f1;color:#b93640}\n.pill.witness_required,.pill.requested,.pill.medium,.pill.b{background:#fff7e8;color:#a86a03}\n.pill.in_progress,.pill.active,.pill.commissioning,.pill.blue{background:#fff2e6;color:#c9600b}\n.pill.c{background:#f1efff;color:#6657bb}\n.progress{background:#e9eef4}.progress>i{background:linear-gradient(90deg,#f58220,#ffac5e)}\n.empty{color:#8491a0}\n.modal-backdrop{background:rgba(16,32,50,.38);backdrop-filter:blur(2px)}\n.modal{background:#fff;border-color:#dfe6ee;box-shadow:0 22px 60px rgba(16,32,50,.18);color:#172033}\n.result-row{background:#f9fbfd;border-color:#dfe6ee}.result-row .meta{color:#788698}\n.result-buttons button.selected.pass{background:#eaf8f1;border-color:#6bc49a;color:#116c4a}.result-buttons button.selected.fail{background:#fff0f1;border-color:#e68b91;color:#b6333d}.result-buttons button.selected.na{background:#f1efff;border-color:#a99ee0;color:#6256ad}\n.tabs{border-bottom-color:#dfe6ee}.tab{color:#748397}.tab.active{color:#d9650d;border-color:#f58220}\n.matrix{background:#dfe6ee;border-color:#dfe6ee}.matrix>div{background:#fff}.matrix .head{background:#f5f7fa;color:#69788a}.matrix select{background:#fff;border-color:#d5dee8;color:#263548}\n.score-ring{background:conic-gradient(#f58220 calc(var(--p)*1%),#e8edf4 0)}.score-ring:after{background:#fff}.score-ring b{color:#172033}\n.activity-item{border-left-color:#f58220;background:#fff9f3}.activity-item b{color:#2d3b4d}\n.banner{border-color:#ffd4ad;background:linear-gradient(90deg,#fff6ed,#fffaf5)}\n.readiness-ring{background:conic-gradient(currentColor calc(var(--p)*1%),#e8edf4 0)}.readiness-ring:after{background:#fff}\n.switch-reason{background:#fff;border-color:#dfe6ee}\n.matrix-good{background:#eaf8f1;color:#16714f}.matrix-near{background:#fff3e7;color:#c9600b}.matrix-risk{background:#fff7e8;color:#a86a03}.matrix-empty{background:#f2f5f8;color:#7a8797}\n.gate-pass{background:#eaf8f1;color:#16714f}.gate-block{background:#fff0f1;color:#b93640}.score-big.green{color:#16714f}.score-big.amber{color:#b77708}.score-big.red{color:#c13f48}\n.evidence-card{background:#fff;border-color:#dfe6ee;color:#263548;box-shadow:0 5px 18px rgba(25,45,70,.05)}.evidence-card:hover{border-color:#f58220;box-shadow:0 8px 22px rgba(245,130,32,.12)}.evidence-card .file-icon{background:#fff2e6;color:#d9650d}\n.detail-list div{background:#f6f8fb}.tag-cloud span{background:#fff2e6;border-color:#ffd4ad;color:#b65b0d}.file-row{border-bottom-color:#e6ebf1;color:#273548}\n.project-tile{color:#273548;background:#fff;border-color:#dfe6ee;box-shadow:0 8px 24px rgba(25,45,70,.07)}.project-tile:hover{border-color:#f58220;box-shadow:0 10px 28px rgba(245,130,32,.12)}.tile-top span,.tile-foot,.mini-metrics{color:#758397}.mini-metrics{border-color:#e5ebf1}.mini-metrics b{color:#273548}\n.risk-pill.on_track{background:#eaf8f1;color:#16714f}.risk-pill.watch{background:#fff2e6;color:#c9600b}.risk-pill.at_risk{background:#fff7e8;color:#a86a03}.risk-pill.critical{background:#fff0f1;color:#b93640}\n.field-launcher button{color:#273548;background:#fff;border-color:#dfe6ee;box-shadow:0 6px 20px rgba(25,45,70,.055)}.field-launcher button:hover{border-color:#f58220;box-shadow:0 10px 26px rgba(245,130,32,.11)}.field-launcher i{color:#f58220}.field-launcher span{color:#748397}\n.timeline-dot:before{background:#dfe6ee}.timeline-dot:after{background:#f58220;border-color:#fff;box-shadow:0 0 0 1px #e1e7ee}.timeline-row.complete .timeline-dot:after{background:#1da56d}.timeline-row.at_risk .timeline-dot:after{background:#d89109}.timeline-card{background:#fff;border-color:#dfe6ee}.timeline-card span,.timeline-card p{color:#798697}.timeline-card h3{color:#263548}\n.smart-checks>div{background:#fafbfd;border-color:#dfe6ee}.smart-checks>div>span{background:#fff1e5;color:#d9650d}.smart-checks em{color:#a66b08;background:#fff6e5}\n.template-card{background:#fff!important;border-color:#dfe6ee!important;color:#273548!important;box-shadow:0 6px 20px rgba(25,45,70,.055)}.template-card:hover{border-color:#f58220!important;box-shadow:0 10px 28px rgba(245,130,32,.12)!important}\n\n/* Common hard-coded dark surfaces used by generated screen fragments */\n[style*=\"background:#0\"],[style*=\"background: #0\"]{color:inherit}\nhr{border:0;border-top:1px solid #e2e8ef}\na{color:#d9650d}\n::-webkit-scrollbar{width:10px;height:10px}::-webkit-scrollbar-track{background:#eef2f6}::-webkit-scrollbar-thumb{background:#c8d2dd;border-radius:10px;border:2px solid #eef2f6}::-webkit-scrollbar-thumb:hover{background:#acb8c5}\n\n@media(max-width:820px){.sidebar{box-shadow:14px 0 36px rgba(14,32,50,.18)}.topbar{background:#fff}}\n\n\n/* =========================================================\n   Qx V8.3 FORCE-LIGHT WORKSPACE\n   Navy navigation remains dark. Every content surface is light.\n   ========================================================= */\nhtml,body,#app,.layout,.main,.content{background:#f5f7fa !important;color:#172033 !important;color-scheme:light !important}\n.main{min-height:100vh !important}\n.sidebar,.sidebar *{color-scheme:dark}\n.sidebar{background:linear-gradient(180deg,#102a43 0%,#0b2239 100%) !important;color:#fff !important}\n.topbar{background:#ffffff !important;border-bottom:1px solid #dde4ec !important;color:#172033 !important}\n.content{background:#f5f7fa !important}\n.hero,.hero h1,.hero h2,.section-head h3{color:#172033 !important}\n.card,.table-wrap,.project-tile,.timeline-card,.evidence-card,.switch-reason,.report-sheet,.field-status-strip>div,.banner,.result-row,.smart-checks>div{\n  background:#ffffff !important;color:#172033 !important;border-color:#dfe5ec !important;\n}\n.card h1,.card h2,.card h3,.card h4,.project-tile h3,.timeline-card h3,.banner strong,.activity-item b{color:#172033 !important}\n.table-wrap table,table{background:#fff !important;color:#172033 !important}\nth{background:#f1f4f7 !important;color:#5e6b7a !important;border-color:#dde4ec !important}\ntd{background:#fff !important;color:#253447 !important;border-color:#e5eaf0 !important}\ntbody tr:hover td{background:#fff7ef !important}\n.activity-item{background:#fff8f1 !important;color:#253447 !important}\n.modal-backdrop{background:rgba(15,35,55,.36) !important;backdrop-filter:blur(2px)}\n.modal{background:#ffffff !important;color:#172033 !important;border:1px solid #d9e1ea !important;box-shadow:0 24px 70px rgba(15,35,55,.22) !important}\n.modal .banner,.modal .card,.modal .table-wrap,.modal .result-row{background:#fff !important;color:#172033 !important}\n.modal h2,.modal h3,.modal h4,.modal strong,.modal td{color:#172033 !important}\n.modal .muted,.modal p,.modal small{color:#6d7b8d !important}\n.modal-x{width:42px;height:42px;flex:0 0 42px;border:1px solid #d7e0e9;border-radius:10px;background:#fff;color:#172033;font-size:28px;line-height:1;display:grid;place-items:center;cursor:pointer;font-weight:400;transition:.15s}\n.modal-x:hover{background:#f58220;color:#fff;border-color:#f58220;transform:translateY(-1px)}\n.modal-x:focus-visible{outline:3px solid rgba(245,130,32,.25);outline-offset:2px}\n.project-picker,.search,.mini-select,input,select,textarea{background:#fff !important;color:#172033 !important;border-color:#ccd6e1 !important}\n.btn:not(.btn-primary):not(.btn-danger){background:#fff !important;color:#253447 !important;border-color:#d4dde7 !important}\n.btn:not(.btn-primary):not(.btn-danger):hover{background:#f3f6f9 !important;border-color:#bdc9d6 !important}\n.btn-primary{background:#f58220 !important;color:#fff !important;border-color:#f58220 !important}\n.template-card{background:#fff !important;color:#172033 !important}\n.template-card *{color:inherit}\n.template-card .muted,.template-card p,.template-card small{color:#6d7b8d !important}\n.matrix>div{background:#fff !important;color:#253447 !important}.matrix .head{background:#f1f4f7 !important;color:#68778a !important}\n.empty{background:#fff !important;color:#7a8796 !important;border-color:#dfe5ec !important}\n@media(max-width:820px){.content{background:#f5f7fa !important}.topbar{background:#fff !important}}\n\n/* Qx V8.4 definitive light workspace */\nbody .main,body .main .content{background:#f4f7fb!important;color:#172033!important}\nbody .main .topbar{background:#fff!important;color:#172033!important;border-bottom:1px solid #dce4ed!important}\nbody .main .hero h1,body .main h1,body .main h2,body .main h3,body .main h4,body .main strong,body .main b{color:#172033}\nbody .main .card,body .main .table-wrap,body .main .project-tile,body .main .timeline-card,body .main .evidence-card,body .main .report-sheet,body .main .field-status-strip>div,body .main .switch-reason,body .main .banner,body .main .activity-item,body .main .result-row,body .main .smart-checks>div{background:#fff!important;color:#172033!important;border-color:#dce4ed!important;box-shadow:0 5px 18px rgba(25,45,70,.055)!important}\nbody .main table,body .main tbody,body .main tr{background:#fff!important;color:#253447!important}\nbody .main th{background:#f5f7fa!important;color:#66768a!important;border-color:#dce4ed!important}\nbody .main td{background:#fff!important;color:#253447!important;border-color:#e7ecf2!important}\nbody .main tbody tr:hover td{background:#fff8f0!important}\nbody .main input,body .main select,body .main textarea{background:#fff!important;color:#172033!important;border-color:#cfd9e4!important}\nbody .main .metric{color:#172033}\nbody .main .metric.green{color:#159b65}body .main .metric.blue{color:#2f80ed}body .main .metric.amber{color:#d88b08}body .main .metric.red{color:#d63d47}\nbody .main .eyebrow{color:#ef6c00!important}\nbody .main .btn-primary{background:#f58220!important;color:#fff!important;border-color:#f58220!important}\nbody .main .btn:not(.btn-primary):not(.btn-danger){background:#fff!important;color:#243447!important;border-color:#d6dee8!important}\nbody .sidebar{background:linear-gradient(180deg,#0b2743,#071d33)!important;color:#fff!important}\nbody .sidebar .navbtn.active{background:#f58220!important;color:#fff!important;border-color:#f58220!important}\nbody .sidebar .navbtn.active .navicon{color:#fff!important}\n.modal-backdrop .modal{background:#fff!important;color:#172033!important;border-color:#d7e0e9!important}\n.modal-backdrop .modal .table-wrap,.modal-backdrop .modal .banner,.modal-backdrop .modal .result-row,.modal-backdrop .modal .card{background:#fff!important;color:#172033!important;border-color:#dce4ed!important}\n.modal-backdrop .modal th{background:#f5f7fa!important;color:#66768a!important}.modal-backdrop .modal td{background:#fff!important;color:#253447!important}\n" }],
  ["/manifest.webmanifest", { type: "application/manifest+json; charset=utf-8", body: "{\"name\":\"Elliot Qx V8.1 Head-to-Head Commissioning OS\",\"short_name\":\"Elliot Qx\",\"description\":\"Field-first QA, BMS commissioning, system completion, offline project packs and handover intelligence\",\"start_url\":\"/\",\"display\":\"standalone\",\"background_color\":\"#f4f7fb\",\"theme_color\":\"#14283d\",\"icons\":[{\"src\":\"/icons/qx.svg\",\"sizes\":\"any\",\"type\":\"image/svg+xml\",\"purpose\":\"any maskable\"}]}\n" }],
  ["/sw.js", { type: "application/javascript; charset=utf-8", body: "const CACHE='elliot-qx-v8.4-light-shell';\nconst SHELL=['/','/index.html','/styles.css?v=8.4.0','/app.js?v=8.4.0','/manifest.webmanifest','/icons/qx.svg'];\nself.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(SHELL)).then(()=>self.skipWaiting())));\nself.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));\nself.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(u.origin!==location.origin)return;if(u.pathname.startsWith('/api/'))return;e.respondWith(fetch(e.request).then(r=>{const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy));return r}).catch(()=>caches.match(e.request).then(r=>r||caches.match('/index.html'))))});\n" }],
  ["/icons/qx.svg", { type: "image/svg+xml; charset=utf-8", body: "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 512 512\"><defs><linearGradient id=\"g\" x1=\"0\" x2=\"1\" y1=\"0\" y2=\"1\"><stop stop-color=\"#5aa8ff\"/><stop offset=\"1\" stop-color=\"#43d6d0\"/></linearGradient></defs><rect width=\"512\" height=\"512\" rx=\"112\" fill=\"#07101e\"/><rect x=\"54\" y=\"54\" width=\"404\" height=\"404\" rx=\"94\" fill=\"url(#g)\"/><path d=\"M136 155h98c75 0 119 39 119 102 0 35-15 64-43 82l66 18-16 52-110-32c-6 1-12 1-19 1h-95V155Zm65 57v109h34c34 0 53-20 53-55 0-34-19-54-53-54h-34Z\" fill=\"#07101e\"/><path d=\"m328 172 49 0 0 49-49 0z\" fill=\"#07101e\"/></svg>\n" }],
]);

function serveStatic(pathname) { const asset=STATIC_ASSETS.get(pathname)||(pathname.endsWith('/')?STATIC_ASSETS.get('/index.html'):null); if(!asset)return new Response('Not found',{status:404}); const headers={'content-type':asset.type,'x-content-type-options':'nosniff'}; headers['cache-control']=(pathname==='/sw.js'||pathname==='/'||pathname==='/index.html')?'no-cache':'public, max-age=3600'; return new Response(asset.body,{headers}); }
const encoder = new TextEncoder();
const json = (data, status = 200, headers = {}) => new Response(JSON.stringify(data), { status, headers: { 'content-type': 'application/json; charset=utf-8', 'cache-control': 'no-store', ...headers } });
const now = () => new Date().toISOString();
const id = (prefix) => `${prefix}_${crypto.randomUUID()}`;
const toB64 = (bytes) => btoa(String.fromCharCode(...bytes));
const fromB64 = (s) => Uint8Array.from(atob(s), c => c.charCodeAt(0));
const clean = (v, n = 500) => String(v ?? '').trim().slice(0, n);
const nullable = (v, n = 500) => clean(v, n) || null;

async function sha256(value) {
  const digest = await crypto.subtle.digest('SHA-256', typeof value === 'string' ? encoder.encode(value) : value);
  return toB64(new Uint8Array(digest));
}
async function hashPassword(password, salt = crypto.getRandomValues(new Uint8Array(16))) {
  const saltB64 = toB64(salt);
  const digest = await crypto.subtle.digest('SHA-256', encoder.encode(`qx-v45:${saltB64}:${String(password)}`));
  return { salt: saltB64, hash: toB64(new Uint8Array(digest)) };
}
async function verifyPassword(password, saltB64, expectedB64) {
  const got = await hashPassword(password, fromB64(saltB64));
  return got.hash === String(expectedB64);
}
function getCookie(request, name) {
  const raw = request.headers.get('cookie') || '';
  for (const part of raw.split(';')) { const [k, ...v] = part.trim().split('='); if (k === name) return decodeURIComponent(v.join('=')); }
  return null;
}
async function currentUser(request, env) {
  const token = getCookie(request, 'qx_session'); if (!token) return null;
  return env.DB.prepare(`SELECT u.id,u.email,u.name,u.role,u.active FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token_hash=? AND s.expires_at>? LIMIT 1`).bind(await sha256(token), now()).first();
}
const roleAllowed = (u, roles) => u && u.active && roles.includes(u.role);
async function audit(env, userId, action, entityType, entityId, details = {}) {
  await env.DB.prepare(`INSERT INTO audit_log(id,user_id,action,entity_type,entity_id,details_json,created_at) VALUES(?,?,?,?,?,?,?)`).bind(id('audit'), userId || null, action, entityType, entityId || null, JSON.stringify(details), now()).run();
}
async function bodyJson(request) { try { return await request.json(); } catch { throw new Error('Invalid JSON body'); } }
function cookie(token, maxAge = 43200) { return `qx_session=${encodeURIComponent(token)}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${maxAge}`; }
async function all(stmt) { const r = await stmt.all(); return r.results || []; }
async function refreshTurnover(env, packageId) {
  const r = await env.DB.prepare(`SELECT COUNT(*) total, SUM(CASE WHEN status='complete' OR status='accepted' THEN 1 ELSE 0 END) done FROM turnover_items WHERE package_id=? AND required=1`).bind(packageId).first();
  const total = Number(r?.total || 0), done = Number(r?.done || 0), pct = total ? Math.round(done * 100 / total) : 0;
  await env.DB.prepare(`UPDATE turnover_packages SET required_items=?,complete_items=?,readiness_percent=?,status=CASE WHEN ?=100 THEN 'ready' ELSE status END,updated_at=? WHERE id=?`).bind(total, done, pct, pct, now(), packageId).run();
}


async function ensureV7(env){
  const stmts=[
    `CREATE TABLE IF NOT EXISTS qx_drawings(id TEXT PRIMARY KEY,project_id TEXT NOT NULL,drawing_no TEXT NOT NULL,title TEXT NOT NULL,revision TEXT,discipline TEXT,file_id TEXT,status TEXT NOT NULL DEFAULT 'current',created_by TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
    `CREATE UNIQUE INDEX IF NOT EXISTS idx_qx_drawings_unique ON qx_drawings(project_id,drawing_no,revision)`,
    `CREATE TABLE IF NOT EXISTS qx_drawing_pins(id TEXT PRIMARY KEY,drawing_id TEXT NOT NULL,title TEXT NOT NULL,x_percent REAL NOT NULL,y_percent REAL NOT NULL,severity TEXT NOT NULL DEFAULT 'normal',status TEXT NOT NULL DEFAULT 'open',created_by TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
    `CREATE INDEX IF NOT EXISTS idx_qx_pins_drawing ON qx_drawing_pins(drawing_id,status)`,
    `CREATE TABLE IF NOT EXISTS qx_notifications(id TEXT PRIMARY KEY,project_id TEXT,user_id TEXT,title TEXT NOT NULL,message TEXT NOT NULL,severity TEXT NOT NULL DEFAULT 'normal',entity_type TEXT,entity_id TEXT,dedupe_key TEXT UNIQUE,read_at TEXT,created_at TEXT NOT NULL)`,
    `CREATE INDEX IF NOT EXISTS idx_qx_notifications_user ON qx_notifications(user_id,read_at,created_at)`
  ]; for(const s of stmts) await env.DB.prepare(s).run();
}


async function ensureV8(env){
  await ensureV7(env);
  const stmts=[
    `CREATE TABLE IF NOT EXISTS qx_observations(id TEXT PRIMARY KEY,project_id TEXT NOT NULL,system_id TEXT,equipment_id TEXT,drawing_id TEXT,observation_type TEXT NOT NULL DEFAULT 'field',title TEXT NOT NULL,description TEXT,status TEXT NOT NULL DEFAULT 'open',severity TEXT NOT NULL DEFAULT 'normal',assigned_to TEXT,due_at TEXT,created_by TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
    `CREATE INDEX IF NOT EXISTS idx_qx_obs_project ON qx_observations(project_id,status,created_at)`,
    `CREATE TABLE IF NOT EXISTS qx_milestones(id TEXT PRIMARY KEY,project_id TEXT NOT NULL,system_id TEXT,name TEXT NOT NULL,phase TEXT NOT NULL DEFAULT 'commissioning',target_at TEXT,status TEXT NOT NULL DEFAULT 'planned',owner_id TEXT,notes TEXT,created_by TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
    `CREATE INDEX IF NOT EXISTS idx_qx_milestones_project ON qx_milestones(project_id,target_at,status)`,
    `CREATE TABLE IF NOT EXISTS qx_form_jobs(id TEXT PRIMARY KEY,project_id TEXT,name TEXT NOT NULL,discipline TEXT NOT NULL,stage TEXT NOT NULL,source_text TEXT,proposal_json TEXT,status TEXT NOT NULL DEFAULT 'proposed',created_by TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
    `CREATE TABLE IF NOT EXISTS qx_sync_conflicts(id TEXT PRIMARY KEY,project_id TEXT,user_id TEXT,device_id TEXT,entity_type TEXT,entity_id TEXT,operation_key TEXT,client_payload TEXT,server_payload TEXT,status TEXT NOT NULL DEFAULT 'open',created_at TEXT NOT NULL,resolved_at TEXT)`,
    `CREATE INDEX IF NOT EXISTS idx_qx_sync_conflicts_project ON qx_sync_conflicts(project_id,status,created_at)`
  ];
  for(const q of stmts) await env.DB.prepare(q).run();
}

async function handleApi(request, env, ctx) {
  const url = new URL(request.url), path = url.pathname, method = request.method;
  if (path === '/api/health') return json({ ok: true, app: env.APP_NAME, version: '8.0-head-to-head', time: now() });
  if (path === '/api/setup/status' && method === 'GET') { const r = await env.DB.prepare('SELECT COUNT(*) count FROM users').first(); return json({ initialized: Number(r?.count || 0) > 0 }); }
  if (path === '/api/setup/bootstrap' && method === 'POST') {
    try {
      const c = await env.DB.prepare('SELECT COUNT(*) count FROM users').first();
      if (Number(c?.count || 0) > 0) return json({ error: 'Already initialized' }, 409);
      const d = await bodyJson(request);
      if (!d.email || !d.name || String(d.password || '').length < 12) return json({ error: 'Name, email and password of at least 12 characters are required' }, 400);
      const uid = id('usr'), email = clean(d.email,200).toLowerCase(), name = clean(d.name,120), created = now();
      const pw = await hashPassword(String(d.password));
      await env.DB.prepare(`INSERT INTO users(id,email,name,role,password_salt,password_hash,active,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?)`).bind(uid,email,name,'admin',pw.salt,pw.hash,1,created,created).run();
      const token = toB64(crypto.getRandomValues(new Uint8Array(32))).replaceAll('+','-').replaceAll('/','_').replaceAll('=','');
      const expires = new Date(Date.now()+43200000).toISOString();
      await env.DB.prepare('INSERT INTO sessions(id,user_id,token_hash,expires_at,created_at) VALUES(?,?,?,?,?)').bind(id('ses'),uid,await sha256(token),expires,created).run();
      ctx.waitUntil(audit(env,uid,'bootstrap_admin','user',uid).catch(()=>{}));
      return json({user:{id:uid,email,name,role:'admin'}},201,{'set-cookie':cookie(token)});
    } catch (error) {
      console.error(JSON.stringify({level:'error',message:'bootstrap failed',detail:String(error?.message||error)}));
      return json({error:`Bootstrap failed: ${String(error?.message||error).slice(0,180)}`},500);
    }
  }
  if (path === '/api/auth/login' && method === 'POST') {
    try {
      const d = await bodyJson(request);
      const u = await env.DB.prepare('SELECT * FROM users WHERE email=? AND active=1 LIMIT 1').bind(clean(d.email,200).toLowerCase()).first();
      if (!u || !(await verifyPassword(String(d.password||''),u.password_salt,u.password_hash))) return json({error:'Invalid email or password'},401);
      const token = toB64(crypto.getRandomValues(new Uint8Array(32))).replaceAll('+','-').replaceAll('/','_').replaceAll('=','');
      const expires = new Date(Date.now()+43200000).toISOString();
      await env.DB.prepare('INSERT INTO sessions(id,user_id,token_hash,expires_at,created_at) VALUES(?,?,?,?,?)').bind(id('ses'),u.id,await sha256(token),expires,now()).run();
      ctx.waitUntil(audit(env,u.id,'login','user',u.id).catch(()=>{}));
      return json({user:{id:u.id,email:u.email,name:u.name,role:u.role}},200,{'set-cookie':cookie(token)});
    } catch (error) {
      console.error(JSON.stringify({level:'error',message:'login failed',detail:String(error?.message||error)}));
      return json({error:`Login failed: ${String(error?.message||error).slice(0,180)}`},500);
    }
  }
  if (path === '/api/auth/logout' && method === 'POST') { const t = getCookie(request,'qx_session'); if(t) await env.DB.prepare('DELETE FROM sessions WHERE token_hash=?').bind(await sha256(t)).run(); return json({ok:true},200,{'set-cookie':'qx_session=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0'}); }

  const user = await currentUser(request, env); if (!user) return json({ error:'Authentication required' },401);
  if (path === '/api/me' && method === 'GET') return json({ user });

  if (path === '/api/v7/ensure' && method === 'POST') { if(user.role!=='admin') return json({ok:true}); await ensureV7(env); return json({ok:true,version:'7.0'}); }
  if (path === '/api/notifications' && method === 'GET') { await ensureV7(env); const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT * FROM qx_notifications WHERE (user_id=? OR user_id IS NULL) AND project_id=? ORDER BY read_at IS NULL DESC,created_at DESC LIMIT 100`).bind(user.id,pid):env.DB.prepare(`SELECT * FROM qx_notifications WHERE user_id=? OR user_id IS NULL ORDER BY read_at IS NULL DESC,created_at DESC LIMIT 100`).bind(user.id)); return json({notifications:rows}); }
  if (path === '/api/notifications/scan' && method === 'POST') { await ensureV7(env); const d=await bodyJson(request); const pid=d.project_id||null; const projects=pid?[await env.DB.prepare('SELECT * FROM projects WHERE id=?').bind(pid).first()]:await all(env.DB.prepare("SELECT * FROM projects WHERE status!='archived'")); let created=0; for(const p of projects.filter(Boolean)){ const [a,b,c,w]=await Promise.all([env.DB.prepare(`SELECT COUNT(*) n FROM defects WHERE project_id=? AND category='A' AND status NOT IN ('closed','verified')`).bind(p.id).first(),env.DB.prepare(`SELECT COUNT(*) n FROM bms_points WHERE project_id=? AND overall_status='fail'`).bind(p.id).first(),env.DB.prepare(`SELECT COUNT(*) n FROM action_items WHERE project_id=? AND status NOT IN ('closed','complete','done') AND due_at IS NOT NULL AND due_at<?`).bind(p.id,now()).first(),env.DB.prepare(`SELECT COUNT(*) n FROM witness_requests WHERE project_id=? AND status IN ('requested','scheduled')`).bind(p.id).first()]); const alerts=[[Number(a?.n||0),'critical','Category A defects',`${a?.n||0} Category A defect(s) are blocking completion.`,'catA'],[Number(b?.n||0),'critical','Failed BMS I/O',`${b?.n||0} BMS point(s) are failed.`,'bmsFail'],[Number(c?.n||0),'high','Overdue actions',`${c?.n||0} action(s) are overdue.`,'overdue'],[Number(w?.n||0),'high','Witness holds',`${w?.n||0} witness/hold point(s) remain open.`,'witness']]; for(const [n,sev,title,msg,key] of alerts){if(!n)continue;try{await env.DB.prepare(`INSERT INTO qx_notifications(id,project_id,user_id,title,message,severity,entity_type,dedupe_key,created_at) VALUES(?,?,?,?,?,?,?,?,?)`).bind(id('ntf'),p.id,null,`${p.code}: ${title}`,msg,sev,'project',`${p.id}:${key}:${new Date().toISOString().slice(0,10)}`,now()).run();created++}catch{}} } return json({created}); }
  if (path === '/api/drawings' && method === 'GET') { await ensureV7(env); const pid=url.searchParams.get('project_id'); if(!pid)return json({error:'project_id required'},400); const rows=await all(env.DB.prepare(`SELECT d.*,(SELECT COUNT(*) FROM qx_drawing_pins p WHERE p.drawing_id=d.id AND p.status='open') pin_count FROM qx_drawings d WHERE d.project_id=? ORDER BY d.drawing_no,d.revision`).bind(pid)); return json({drawings:rows}); }
  if (path === '/api/drawings' && method === 'POST') { await ensureV7(env); const d=await bodyJson(request); if(!d.project_id||!d.drawing_no||!d.title)return json({error:'Project, drawing number and title required'},400); const did=id('drw'); await env.DB.prepare(`INSERT INTO qx_drawings(id,project_id,drawing_no,title,revision,discipline,file_id,status,created_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).bind(did,d.project_id,clean(d.drawing_no,100),clean(d.title,220),nullable(d.revision,40),nullable(d.discipline,80),nullable(d.file_id,120),'current',user.id,now(),now()).run(); return json({id:did},201); }
  const drawingMatch=path.match(/^\/api\/drawings\/([^/]+)$/); if(drawingMatch&&method==='GET'){await ensureV7(env);const drawing=await env.DB.prepare(`SELECT * FROM qx_drawings WHERE id=?`).bind(drawingMatch[1]).first();if(!drawing)return json({error:'Drawing not found'},404);const pins=await all(env.DB.prepare(`SELECT * FROM qx_drawing_pins WHERE drawing_id=? ORDER BY created_at DESC`).bind(drawing.id));return json({drawing,pins});}
  const pinMatch=path.match(/^\/api\/drawings\/([^/]+)\/pins$/); if(pinMatch&&method==='POST'){await ensureV7(env);const d=await bodyJson(request);if(!d.title)return json({error:'Pin title required'},400);const pid=id('pin');await env.DB.prepare(`INSERT INTO qx_drawing_pins(id,drawing_id,title,x_percent,y_percent,severity,status,created_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?)`).bind(pid,pinMatch[1],clean(d.title,300),Math.max(0,Math.min(100,Number(d.x||50))),Math.max(0,Math.min(100,Number(d.y||50))),clean(d.severity||'normal',20),'open',user.id,now(),now()).run();return json({id:pid},201);}


  // ===== QX V8 HEAD-TO-HEAD EDITION =====
  if (path === '/api/v8/ensure' && method === 'POST') { await ensureV8(env); return json({ok:true,version:'8.0'}); }

  if (path === '/api/portfolio' && method === 'GET') {
    await ensureV8(env);
    const projects=await all(env.DB.prepare(`SELECT * FROM projects WHERE status!='archived' ORDER BY updated_at DESC`));
    const rows=[];
    for(const p of projects){
      const [eq,tests,defs,pts,turn,wit]=await Promise.all([
        env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='complete' THEN 1 ELSE 0 END) done FROM equipment WHERE project_id=?`).bind(p.id).first(),
        env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='approved' THEN 1 ELSE 0 END) done FROM inspections WHERE project_id=?`).bind(p.id).first(),
        env.DB.prepare(`SELECT SUM(CASE WHEN status NOT IN ('closed','verified') THEN 1 ELSE 0 END) open,SUM(CASE WHEN category='A' AND status NOT IN ('closed','verified') THEN 1 ELSE 0 END) cat_a FROM defects WHERE project_id=?`).bind(p.id).first(),
        env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN overall_status='pass' THEN 1 ELSE 0 END) done,SUM(CASE WHEN overall_status='fail' THEN 1 ELSE 0 END) failed FROM bms_points WHERE project_id=?`).bind(p.id).first(),
        env.DB.prepare(`SELECT ROUND(AVG(readiness_percent)) readiness FROM turnover_packages WHERE project_id=?`).bind(p.id).first(),
        env.DB.prepare(`SELECT SUM(CASE WHEN status IN ('requested','scheduled') THEN 1 ELSE 0 END) open FROM witness_requests WHERE project_id=?`).bind(p.id).first()
      ]);
      const pc=(a,b)=>Number(b||0)?Math.round(Number(a||0)*100/Number(b)):0;
      const asset=pc(eq?.done,eq?.total), test=pc(tests?.done,tests?.total), bms=pc(pts?.done,pts?.total), turnover=Math.round(Number(turn?.readiness||0)), catA=Number(defs?.cat_a||0), failed=Number(pts?.failed||0), witness=Number(wit?.open||0);
      const score=Math.max(0,Math.min(100,Math.round(asset*.24+test*.24+bms*.22+turnover*.18+(catA?0:7)+(failed?0:3)+(witness?0:2))));
      rows.push({...p,asset_percent:asset,test_percent:test,bms_percent:bms,turnover_percent:turnover,open_defects:Number(defs?.open||0),cat_a:catA,failed_points:failed,witness_open:witness,readiness_score:score,risk:catA||failed?'critical':score<70?'at_risk':score<90?'watch':'on_track'});
    }
    return json({projects:rows,generated_at:now()});
  }

  if (path === '/api/offline/pack' && method === 'GET') {
    await ensureV8(env); const pid=url.searchParams.get('project_id'); if(!pid)return json({error:'project_id required'},400);
    const project=await env.DB.prepare(`SELECT * FROM projects WHERE id=?`).bind(pid).first(); if(!project)return json({error:'Project not found'},404);
    const [systems,equipment,templates,inspections,defects,points,turnover,witness,actions,drawings,observations,milestones]=await Promise.all([
      all(env.DB.prepare(`SELECT * FROM systems WHERE project_id=? ORDER BY code`).bind(pid)),all(env.DB.prepare(`SELECT * FROM equipment WHERE project_id=? ORDER BY tag`).bind(pid)),all(env.DB.prepare(`SELECT t.*,(SELECT COUNT(*) FROM template_items ti WHERE ti.template_id=t.id) item_count FROM templates t WHERE active=1 ORDER BY name`)),all(env.DB.prepare(`SELECT i.*,t.name template_name,e.tag equipment_tag FROM inspections i LEFT JOIN templates t ON t.id=i.template_id LEFT JOIN equipment e ON e.id=i.equipment_id WHERE i.project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT d.*,e.tag equipment_tag FROM defects d LEFT JOIN equipment e ON e.id=d.equipment_id WHERE d.project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT * FROM bms_points WHERE project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT * FROM turnover_packages WHERE project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT * FROM witness_requests WHERE project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT * FROM action_items WHERE project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT * FROM qx_drawings WHERE project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT * FROM qx_observations WHERE project_id=?`).bind(pid)),all(env.DB.prepare(`SELECT * FROM qx_milestones WHERE project_id=? ORDER BY target_at`).bind(pid))]);
    return json({version:8,generated_at:now(),project,systems,equipment,templates,inspections,defects,points,turnover,witness,actions,drawings,observations,milestones});
  }

  if (path === '/api/observations' && method === 'GET') { await ensureV8(env); const pid=url.searchParams.get('project_id'); if(!pid)return json({observations:[]}); const rows=await all(env.DB.prepare(`SELECT o.*,u.name assigned_name,e.tag equipment_tag,s.code system_code FROM qx_observations o LEFT JOIN users u ON u.id=o.assigned_to LEFT JOIN equipment e ON e.id=o.equipment_id LEFT JOIN systems s ON s.id=o.system_id WHERE o.project_id=? ORDER BY CASE o.severity WHEN 'critical' THEN 1 WHEN 'high' THEN 2 ELSE 3 END,o.created_at DESC`).bind(pid)); return json({observations:rows}); }
  if (path === '/api/observations' && method === 'POST') { await ensureV8(env); const d=await bodyJson(request); if(!d.project_id||!d.title)return json({error:'Project and title required'},400); const oid=id('obs'); await env.DB.prepare(`INSERT INTO qx_observations(id,project_id,system_id,equipment_id,drawing_id,observation_type,title,description,status,severity,assigned_to,due_at,created_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(oid,d.project_id,nullable(d.system_id,100),nullable(d.equipment_id,100),nullable(d.drawing_id,100),clean(d.observation_type||'field',40),clean(d.title,240),nullable(d.description,3000),'open',clean(d.severity||'normal',20),nullable(d.assigned_to,100),nullable(d.due_at,40),user.id,now(),now()).run(); return json({id:oid},201); }
  const obsMatch=path.match(/^\/api\/observations\/([^/]+)$/); if(obsMatch&&method==='PATCH'){await ensureV8(env);const d=await bodyJson(request);await env.DB.prepare(`UPDATE qx_observations SET status=COALESCE(?,status),assigned_to=COALESCE(?,assigned_to),severity=COALESCE(?,severity),description=COALESCE(?,description),updated_at=? WHERE id=?`).bind(nullable(d.status,30),nullable(d.assigned_to,100),nullable(d.severity,20),nullable(d.description,3000),now(),obsMatch[1]).run();return json({ok:true});}

  if (path === '/api/milestones' && method === 'GET') { await ensureV8(env); const pid=url.searchParams.get('project_id'); if(!pid)return json({milestones:[]}); const rows=await all(env.DB.prepare(`SELECT m.*,u.name owner_name,s.code system_code FROM qx_milestones m LEFT JOIN users u ON u.id=m.owner_id LEFT JOIN systems s ON s.id=m.system_id WHERE m.project_id=? ORDER BY COALESCE(m.target_at,'9999'),m.name`).bind(pid)); return json({milestones:rows}); }
  if (path === '/api/milestones' && method === 'POST') { await ensureV8(env); const d=await bodyJson(request); if(!d.project_id||!d.name)return json({error:'Project and milestone name required'},400); const mid=id('ms'); await env.DB.prepare(`INSERT INTO qx_milestones(id,project_id,system_id,name,phase,target_at,status,owner_id,notes,created_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`).bind(mid,d.project_id,nullable(d.system_id,100),clean(d.name,220),clean(d.phase||'commissioning',60),nullable(d.target_at,40),clean(d.status||'planned',30),nullable(d.owner_id,100),nullable(d.notes,1500),user.id,now(),now()).run(); return json({id:mid},201); }
  const mileMatch=path.match(/^\/api\/milestones\/([^/]+)$/); if(mileMatch&&method==='PATCH'){await ensureV8(env);const d=await bodyJson(request);await env.DB.prepare(`UPDATE qx_milestones SET status=COALESCE(?,status),target_at=COALESCE(?,target_at),owner_id=COALESCE(?,owner_id),notes=COALESCE(?,notes),updated_at=? WHERE id=?`).bind(nullable(d.status,30),nullable(d.target_at,40),nullable(d.owner_id,100),nullable(d.notes,1500),now(),mileMatch[1]).run();return json({ok:true});}

  if (path === '/api/equipment-readiness' && method === 'GET') {
    await ensureV8(env); const pid=url.searchParams.get('project_id'); if(!pid)return json({equipment:[]});
    const rows=await all(env.DB.prepare(`SELECT e.*,
      (SELECT COUNT(*) FROM inspections i WHERE i.equipment_id=e.id) tests_total,
      (SELECT COUNT(*) FROM inspections i WHERE i.equipment_id=e.id AND i.status='approved') tests_done,
      (SELECT COUNT(*) FROM defects d WHERE d.equipment_id=e.id AND d.category='A' AND d.status NOT IN ('closed','verified')) cat_a,
      (SELECT COUNT(*) FROM bms_points b WHERE b.equipment_id=e.id) points_total,
      (SELECT COUNT(*) FROM bms_points b WHERE b.equipment_id=e.id AND b.overall_status='pass') points_done
      FROM equipment e WHERE e.project_id=? ORDER BY e.system,e.tag`).bind(pid));
    const out=rows.map(e=>{const blockers=[];if(e.status==='blocked')blockers.push('Asset blocked');if(Number(e.cat_a||0))blockers.push(`${e.cat_a} Category A`);if(Number(e.tests_total||0)&&Number(e.tests_done||0)<Number(e.tests_total||0))blockers.push('Tests incomplete');if(Number(e.points_total||0)&&Number(e.points_done||0)<Number(e.points_total||0))blockers.push('BMS I/O incomplete');const ready=!blockers.length&&(e.status==='complete'||(Number(e.tests_total||0)>0&&Number(e.tests_done||0)===Number(e.tests_total||0)));return {...e,ready,blockers,ready_score:Math.max(0,100-(Number(e.cat_a||0)?40:0)-(Number(e.tests_total||0)>Number(e.tests_done||0)?30:0)-(Number(e.points_total||0)>Number(e.points_done||0)?30:0))}}); return json({equipment:out});
  }

  if (path === '/api/ai/form-builder' && method === 'POST') {
    await ensureV8(env); const d=await bodyJson(request); if(!d.project_id||!d.name)return json({error:'Project and template name required'},400);
    const text=clean(d.source_text||'',40000), low=text.toLowerCase(), discipline=clean(d.discipline||'General',80), stage=clean(d.stage||'commissioning',60);
    const items=[]; const add=(section,item,hold=false)=>{if(!items.some(x=>x.item_text===item))items.push({section,item_text:item,hold_point:hold,mandatory:true,response_type:'pass_fail_na'})};
    add('Preconditions','Approved drawings, specification and sequence are available and current',true); add('Identification','Equipment tag, location and labels match approved documentation'); add('Evidence','Required photos / readings are captured against the record');
    if(/bms|bacnet|modbus|jace|controller|sensor|actuator|ddc|io\b/.test(low)){add('BMS I/O','Field device state matches controller input/output');add('BMS Graphics','Graphics, command, units and status indication are verified');add('BMS Alarms','Alarm priority, delay, reset and routing are proven',true);add('Sequence','Interlocks, safeties and fail-safe sequence are functionally proven',true)}
    if(/mssb|mcc|switchboard|electrical|isolator|cable|rcbo|mcb|breaker|earthing/.test(low)){add('Electrical','Protection, isolation and labelling match approved drawings');add('Terminations','Cable identification, glands, ferrules and terminations are inspected');add('Electrical Test','Continuity, insulation resistance and earth verification are complete',true)}
    if(/ahu|fcu|fan|pump|valve|damper|hvac|mechanical/.test(low)){add('Mechanical Readiness','Plant, filters, valves, dampers and access are ready');add('Functional Test','Start/stop, modulation, safeties and control loops operate to sequence',true)}
    const sourceLines=text.split(/\r?\n/).map(x=>x.replace(/^[-*\d.\s]+/,'').trim()).filter(x=>x.length>18&&x.length<220).slice(0,12); for(const line of sourceLines)add('Project Specific',line,false);
    add('Completion','All failed items are raised as punch items and required witness points are cleared',true);
    const proposal={name:clean(d.name,180),discipline,stage,description:clean(d.description||`Qx Smart Template generated from project requirements`,500),items:items.slice(0,40),warning:'Machine-assisted draft. Review against approved project documents and competent engineering requirements before issue.'};
    const jid=id('frm'); await env.DB.prepare(`INSERT INTO qx_form_jobs(id,project_id,name,discipline,stage,source_text,proposal_json,status,created_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).bind(jid,d.project_id,proposal.name,discipline,stage,text,JSON.stringify(proposal),'proposed',user.id,now(),now()).run(); return json({id:jid,proposal},201);
  }
  if (path === '/api/ai/form-builder/apply' && method === 'POST') {
    await ensureV8(env); if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403); const d=await bodyJson(request); const job=await env.DB.prepare(`SELECT * FROM qx_form_jobs WHERE id=?`).bind(clean(d.job_id,120)).first(); if(!job)return json({error:'Template proposal not found'},404); let p={};try{p=JSON.parse(job.proposal_json||'{}')}catch{}; const tid=id('tpl'); await env.DB.prepare(`INSERT INTO templates(id,name,discipline,stage,description,version,active,created_at) VALUES(?,?,?,?,?,1,1,?)`).bind(tid,clean(p.name||job.name,180),clean(p.discipline||job.discipline,80),clean(p.stage||job.stage,60),nullable(p.description,500),now()).run(); let n=0; for(const item of (p.items||[]).slice(0,80)){n++;await env.DB.prepare(`INSERT INTO template_items(id,template_id,sort_order,section,item_text,response_type,mandatory,hold_point) VALUES(?,?,?,?,?,?,?,?)`).bind(id('ti'),tid,n*10,clean(item.section||'Checks',100),clean(item.item_text,500),clean(item.response_type||'pass_fail_na',40),item.mandatory===false?0:1,item.hold_point?1:0).run();} await env.DB.prepare(`UPDATE qx_form_jobs SET status='applied',updated_at=? WHERE id=?`).bind(now(),job.id).run(); return json({template_id:tid,item_count:n},201);
  }

  if (path === '/api/sync/v2' && method === 'POST') {
    await ensureV8(env); const d=await bodyJson(request); if(!Array.isArray(d.operations))return json({error:'operations array required'},400); const results=[];
    for(const op of d.operations.slice(0,200)){
      const key=clean(op.operation_key,140); if(!key){results.push({ok:false,error:'operation_key required'});continue;} const prior=await env.DB.prepare(`SELECT id FROM sync_operations WHERE operation_key=?`).bind(key).first(); if(prior){results.push({ok:true,duplicate:true,operation_key:key});continue;}
      const type=clean(op.entity_type,40), eid=clean(op.entity_id,120), payload=op.payload||{}, base=clean(op.base_updated_at||'',60); let table=null;
      if(type==='equipment')table='equipment'; else if(type==='defect')table='defects'; else if(type==='bms_point')table='bms_points';
      if(!table){results.push({ok:false,operation_key:key,error:'unsupported entity type'});continue;}
      const current=await env.DB.prepare(`SELECT * FROM ${table} WHERE id=?`).bind(eid).first(); if(!current){results.push({ok:false,operation_key:key,error:'entity not found'});continue;}
      if(base&&current.updated_at&&String(current.updated_at)>base){const cid=id('cnf');await env.DB.prepare(`INSERT INTO qx_sync_conflicts(id,project_id,user_id,device_id,entity_type,entity_id,operation_key,client_payload,server_payload,status,created_at) VALUES(?,?,?,?,?,?,?,?,?,'open',?)`).bind(cid,current.project_id,user.id,nullable(d.device_id,120),type,eid,key,JSON.stringify(payload),JSON.stringify(current),now()).run();results.push({ok:false,conflict:true,conflict_id:cid,operation_key:key,server:current});continue;}
      if(type==='equipment'){await env.DB.prepare(`UPDATE equipment SET status=COALESCE(?,status),updated_at=? WHERE id=?`).bind(nullable(payload.status,30),now(),eid).run();}
      if(type==='defect'){await env.DB.prepare(`UPDATE defects SET status=COALESCE(?,status),rectification_note=COALESCE(?,rectification_note),updated_at=? WHERE id=?`).bind(nullable(payload.status,30),nullable(payload.rectification_note,2500),now(),eid).run();}
      if(type==='bms_point'){const vals=['field_status','controller_status','graphics_status','alarm_status'].map(k=>nullable(payload[k],30));const overall=vals.some(x=>x==='fail')?'fail':vals.every(x=>x==='pass'||x==='na')?'pass':'in_progress';await env.DB.prepare(`UPDATE bms_points SET field_status=COALESCE(?,field_status),controller_status=COALESCE(?,controller_status),graphics_status=COALESCE(?,graphics_status),alarm_status=COALESCE(?,alarm_status),overall_status=?,comment=COALESCE(?,comment),tested_by=?,tested_at=?,updated_at=? WHERE id=?`).bind(...vals,overall,nullable(payload.comment,1500),user.id,now(),now(),eid).run();}
      await env.DB.prepare(`INSERT INTO sync_operations(id,user_id,device_id,operation_key,entity_type,entity_id,operation,payload_json,status,created_at) VALUES(?,?,?,?,?,?,?,?,?,?)`).bind(id('syn'),user.id,nullable(d.device_id,120),key,type,eid,'update',JSON.stringify(payload),'applied',now()).run();results.push({ok:true,operation_key:key});
    }
    return json({results,conflicts:results.filter(x=>x.conflict).length});
  }
  if(path==='/api/sync/conflicts'&&method==='GET'){await ensureV8(env);const pid=url.searchParams.get('project_id');const rows=await all(pid?env.DB.prepare(`SELECT * FROM qx_sync_conflicts WHERE project_id=? AND status='open' ORDER BY created_at DESC`).bind(pid):env.DB.prepare(`SELECT * FROM qx_sync_conflicts WHERE status='open' ORDER BY created_at DESC LIMIT 200`));return json({conflicts:rows});}


  if (path === '/api/dashboard' && method === 'GET') {
    const projectId = url.searchParams.get('project_id'); const where = projectId ? ' WHERE project_id=?' : '';
    const bind = (sql) => projectId ? env.DB.prepare(sql + where).bind(projectId) : env.DB.prepare(sql + where);
    const [pr, eq, ins, def, pts, wit, turn] = await Promise.all([
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status IN ('active','witnessing','handover') THEN 1 ELSE 0 END) active FROM projects WHERE status!='archived'`).first(),
      bind(`SELECT COUNT(*) total,SUM(CASE WHEN status='complete' THEN 1 ELSE 0 END) complete,SUM(CASE WHEN status='blocked' THEN 1 ELSE 0 END) blocked FROM equipment`).first(),
      bind(`SELECT COUNT(*) total,SUM(CASE WHEN status='approved' THEN 1 ELSE 0 END) approved,SUM(CASE WHEN status='witness_required' THEN 1 ELSE 0 END) witness FROM inspections`).first(),
      bind(`SELECT COUNT(*) total,SUM(CASE WHEN status NOT IN ('closed','verified') THEN 1 ELSE 0 END) open,SUM(CASE WHEN category='A' AND status NOT IN ('closed','verified') THEN 1 ELSE 0 END) cat_a FROM defects`).first(),
      bind(`SELECT COUNT(*) total,SUM(CASE WHEN overall_status='pass' THEN 1 ELSE 0 END) passed,SUM(CASE WHEN overall_status='fail' THEN 1 ELSE 0 END) failed FROM bms_points`).first(),
      bind(`SELECT COUNT(*) total,SUM(CASE WHEN status='requested' THEN 1 ELSE 0 END) requested FROM witness_requests`).first(),
      bind(`SELECT COUNT(*) total,AVG(readiness_percent) avg_readiness,SUM(CASE WHEN status='ready' OR status='approved' THEN 1 ELSE 0 END) ready FROM turnover_packages`).first()
    ]);
    const recent = await all(env.DB.prepare(`SELECT a.*,u.name user_name FROM audit_log a LEFT JOIN users u ON u.id=a.user_id ORDER BY a.created_at DESC LIMIT 12`));
    return json({ projects:pr||{},equipment:eq||{},inspections:ins||{},defects:def||{},points:pts||{},witness:wit||{},turnover:turn||{},recent });
  }

  if (path === '/api/projects' && method === 'GET') {
    const rows = await all(env.DB.prepare(`SELECT p.*,
      (SELECT COUNT(*) FROM equipment e WHERE e.project_id=p.id) equipment_count,
      (SELECT COUNT(*) FROM defects d WHERE d.project_id=p.id AND d.status NOT IN ('closed','verified')) open_defects,
      (SELECT COUNT(*) FROM defects d WHERE d.project_id=p.id AND d.category='A' AND d.status NOT IN ('closed','verified')) cat_a,
      (SELECT COUNT(*) FROM systems s WHERE s.project_id=p.id) system_count,
      (SELECT ROUND(AVG(readiness_percent)) FROM turnover_packages t WHERE t.project_id=p.id) turnover_readiness
      FROM projects p ORDER BY p.updated_at DESC`)); return json({ projects:rows });
  }
  if (path === '/api/projects' && method === 'POST') {
    if(!roleAllowed(user,['admin','pm','engineer'])) return json({error:'Insufficient permission'},403); const d=await bodyJson(request); if(!d.code||!d.name) return json({error:'Project code and name are required'},400);
    const pid=id('prj'); await env.DB.prepare(`INSERT INTO projects(id,code,name,client,site_address,status,completion_target,project_type,commissioning_manager,practical_completion,created_by,created_at,updated_at) VALUES(?,?,?,?,?,'active',?,?,?,?,?,?,?)`).bind(pid,clean(d.code,60),clean(d.name,180),nullable(d.client,180),nullable(d.site_address,300),nullable(d.completion_target,30),clean(d.project_type||'commercial',40),nullable(d.commissioning_manager,120),nullable(d.practical_completion,30),user.id,now(),now()).run();
    ctx.waitUntil(audit(env,user.id,'create','project',pid,{code:d.code})); return json({id:pid},201);
  }

  if (path === '/api/systems' && method === 'GET') { const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT s.*,(SELECT COUNT(*) FROM equipment e WHERE e.project_id=s.project_id AND (e.system=s.code OR e.system=s.name)) equipment_count,(SELECT COUNT(*) FROM defects d JOIN equipment e ON e.id=d.equipment_id WHERE e.project_id=s.project_id AND (e.system=s.code OR e.system=s.name) AND d.status NOT IN ('closed','verified')) open_defects FROM systems s WHERE s.project_id=? ORDER BY s.code`).bind(pid):env.DB.prepare('SELECT * FROM systems ORDER BY updated_at DESC LIMIT 500')); return json({systems:rows}); }
  if (path === '/api/systems' && method === 'POST') { if(!roleAllowed(user,['admin','pm','engineer'])) return json({error:'Insufficient permission'},403); const d=await bodyJson(request); if(!d.project_id||!d.code||!d.name)return json({error:'Project, system code and name are required'},400); const sid=id('sys'); await env.DB.prepare(`INSERT INTO systems(id,project_id,code,name,discipline,parent_system_id,location,status,target_date,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).bind(sid,d.project_id,clean(d.code,80),clean(d.name,180),clean(d.discipline||'BMS',80),nullable(d.parent_system_id,80),nullable(d.location,180),clean(d.status||'construction',40),nullable(d.target_date,30),now(),now()).run(); ctx.waitUntil(audit(env,user.id,'create','system',sid)); return json({id:sid},201); }

  if (path === '/api/equipment' && method === 'GET') { const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT e.*,(SELECT COUNT(*) FROM defects d WHERE d.equipment_id=e.id AND d.status NOT IN ('closed','verified')) open_defects,(SELECT COUNT(*) FROM inspections i WHERE i.equipment_id=e.id AND i.status='approved') approved_tests FROM equipment e WHERE e.project_id=? ORDER BY e.system,e.tag`).bind(pid):env.DB.prepare(`SELECT e.*,(SELECT COUNT(*) FROM defects d WHERE d.equipment_id=e.id AND d.status NOT IN ('closed','verified')) open_defects FROM equipment e ORDER BY e.updated_at DESC LIMIT 1000`)); return json({equipment:rows}); }
  if (path === '/api/equipment' && method === 'POST') { if(!roleAllowed(user,['admin','pm','engineer','site_manager','technician','electrician'])) return json({error:'Insufficient permission'},403); const d=await bodyJson(request); if(!d.project_id||!d.tag||!d.name)return json({error:'Project, tag and name are required'},400); const eid=id('eq'); await env.DB.prepare(`INSERT INTO equipment(id,project_id,tag,name,system,subsystem,building,level,area,equipment_type,manufacturer,model,serial_number,status,qr_code,install_status,qa_status,cx_status,turnover_status,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,'not_started',?,'not_started','not_started','not_started','not_started',?,?)`).bind(eid,d.project_id,clean(d.tag,100),clean(d.name,180),nullable(d.system,120),nullable(d.subsystem,120),nullable(d.building,120),nullable(d.level,120),nullable(d.area,120),nullable(d.equipment_type,120),nullable(d.manufacturer,120),nullable(d.model,120),nullable(d.serial_number,120),eid,now(),now()).run(); ctx.waitUntil(audit(env,user.id,'create','equipment',eid,{tag:d.tag})); return json({id:eid,qr:`${url.origin}/?asset=${eid}`},201); }

  if (path === '/api/templates' && method === 'GET') { const rows=await all(env.DB.prepare(`SELECT t.*,(SELECT COUNT(*) FROM template_items ti WHERE ti.template_id=t.id) item_count,(SELECT COUNT(*) FROM template_items ti WHERE ti.template_id=t.id AND ti.hold_point=1) hold_points FROM templates t WHERE active=1 ORDER BY discipline,name`)); return json({templates:rows}); }
  if (path === '/api/templates' && method === 'POST') {
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403);
    const d=await bodyJson(request); if(!d.name)return json({error:'Template name is required'},400);
    const tid=id('tpl'); await env.DB.prepare(`INSERT INTO templates(id,name,discipline,stage,description,version,active,created_at) VALUES(?,?,?,?,?,1,1,?)`).bind(tid,clean(d.name,180),clean(d.discipline||'General',80),clean(d.stage||'qa',60),nullable(d.description,800),now()).run();
    ctx.waitUntil(audit(env,user.id,'create','template',tid)); return json({id:tid},201);
  }
  const tplMatch=path.match(/^\/api\/templates\/([^/]+)$/);
  if(tplMatch && method==='GET'){
    const tid=tplMatch[1]; const template=await env.DB.prepare(`SELECT t.*,(SELECT COUNT(*) FROM template_items ti WHERE ti.template_id=t.id) item_count,(SELECT COUNT(*) FROM template_items ti WHERE ti.template_id=t.id AND ti.hold_point=1) hold_points FROM templates t WHERE t.id=?`).bind(tid).first();
    if(!template)return json({error:'Template not found'},404); const items=await all(env.DB.prepare(`SELECT * FROM template_items WHERE template_id=? ORDER BY sort_order,id`).bind(tid)); return json({template,items});
  }
  if(tplMatch && method==='PATCH'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403);
    const tid=tplMatch[1], d=await bodyJson(request); const exists=await env.DB.prepare('SELECT id,version FROM templates WHERE id=?').bind(tid).first(); if(!exists)return json({error:'Template not found'},404);
    await env.DB.prepare(`UPDATE templates SET name=COALESCE(?,name),discipline=COALESCE(?,discipline),stage=COALESCE(?,stage),description=?,version=version+1 WHERE id=?`).bind(nullable(d.name,180),nullable(d.discipline,80),nullable(d.stage,60),nullable(d.description,800),tid).run();
    ctx.waitUntil(audit(env,user.id,'update','template',tid)); return json({ok:true});
  }
  if(tplMatch && method==='DELETE'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403);
    const tid=tplMatch[1]; await env.DB.prepare('UPDATE templates SET active=0 WHERE id=?').bind(tid).run(); ctx.waitUntil(audit(env,user.id,'archive','template',tid)); return json({ok:true});
  }
  const tplDup=path.match(/^\/api\/templates\/([^/]+)\/duplicate$/);
  if(tplDup && method==='POST'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403);
    const source=await env.DB.prepare('SELECT * FROM templates WHERE id=?').bind(tplDup[1]).first(); if(!source)return json({error:'Template not found'},404);
    const tid=id('tpl'); await env.DB.prepare(`INSERT INTO templates(id,name,discipline,stage,description,version,active,created_at) VALUES(?,?,?,?,?,1,1,?)`).bind(tid,clean(`${source.name} - Copy`,180),source.discipline,source.stage,source.description,now()).run();
    const items=await all(env.DB.prepare('SELECT * FROM template_items WHERE template_id=? ORDER BY sort_order').bind(source.id)); for(const x of items){await env.DB.prepare(`INSERT INTO template_items(id,template_id,sort_order,section,item_text,response_type,mandatory,hold_point) VALUES(?,?,?,?,?,?,?,?)`).bind(id('ti'),tid,x.sort_order,x.section,x.item_text,x.response_type,x.mandatory,x.hold_point).run();}
    ctx.waitUntil(audit(env,user.id,'duplicate','template',tid,{source_id:source.id})); return json({id:tid},201);
  }
  const tplItems=path.match(/^\/api\/templates\/([^/]+)\/items$/);
  if(tplItems && method==='POST'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403);
    const d=await bodyJson(request); if(!d.item_text)return json({error:'Check text is required'},400); const tid=tplItems[1]; const exists=await env.DB.prepare('SELECT id FROM templates WHERE id=?').bind(tid).first(); if(!exists)return json({error:'Template not found'},404);
    const mx=await env.DB.prepare('SELECT COALESCE(MAX(sort_order),0) m FROM template_items WHERE template_id=?').bind(tid).first(); const iid=id('ti'); await env.DB.prepare(`INSERT INTO template_items(id,template_id,sort_order,section,item_text,response_type,mandatory,hold_point) VALUES(?,?,?,?,?,?,?,?)`).bind(iid,tid,Number(mx?.m||0)+10,clean(d.section||'Checks',100),clean(d.item_text,700),clean(d.response_type||'pass_fail_na',40),d.mandatory===false?0:1,d.hold_point?1:0).run();
    await env.DB.prepare('UPDATE templates SET version=version+1 WHERE id=?').bind(tid).run(); ctx.waitUntil(audit(env,user.id,'create','template_item',iid,{template_id:tid})); return json({id:iid},201);
  }
  const tplItem=path.match(/^\/api\/template-items\/([^/]+)$/);
  if(tplItem && method==='PATCH'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403); const d=await bodyJson(request), iid=tplItem[1]; const old=await env.DB.prepare('SELECT * FROM template_items WHERE id=?').bind(iid).first(); if(!old)return json({error:'Template check not found'},404);
    await env.DB.prepare(`UPDATE template_items SET section=?,item_text=?,response_type=?,mandatory=?,hold_point=? WHERE id=?`).bind(clean(d.section||old.section||'Checks',100),clean(d.item_text||old.item_text,700),clean(d.response_type||old.response_type||'pass_fail_na',40),d.mandatory===false?0:1,d.hold_point?1:0,iid).run(); await env.DB.prepare('UPDATE templates SET version=version+1 WHERE id=?').bind(old.template_id).run(); ctx.waitUntil(audit(env,user.id,'update','template_item',iid)); return json({ok:true});
  }
  if(tplItem && method==='DELETE'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403); const iid=tplItem[1]; const old=await env.DB.prepare('SELECT template_id FROM template_items WHERE id=?').bind(iid).first(); if(!old)return json({error:'Template check not found'},404); const used=await env.DB.prepare('SELECT COUNT(*) n FROM inspection_results WHERE template_item_id=?').bind(iid).first(); if(Number(used?.n||0)>0)return json({error:'This check is already used in a test record. Duplicate the template and edit the copy to preserve historical commissioning records.'},409); await env.DB.prepare('DELETE FROM template_items WHERE id=?').bind(iid).run(); await env.DB.prepare('UPDATE templates SET version=version+1 WHERE id=?').bind(old.template_id).run(); ctx.waitUntil(audit(env,user.id,'delete','template_item',iid)); return json({ok:true});
  }


  if (path === '/api/inspections' && method === 'GET') { const pid=url.searchParams.get('project_id'); const q=`SELECT i.*,t.name template_name,t.discipline,t.stage,e.tag equipment_tag,e.name equipment_name,u.name assigned_name,(SELECT COUNT(*) FROM inspection_results r WHERE r.inspection_id=i.id AND r.result='fail') fails,(SELECT COUNT(*) FROM inspection_results r WHERE r.inspection_id=i.id AND r.result IS NOT NULL) answered,(SELECT COUNT(*) FROM inspection_results r WHERE r.inspection_id=i.id) total_lines FROM inspections i JOIN templates t ON t.id=i.template_id LEFT JOIN equipment e ON e.id=i.equipment_id LEFT JOIN users u ON u.id=i.assigned_to`; const rows=await all(pid?env.DB.prepare(q+' WHERE i.project_id=? ORDER BY i.updated_at DESC').bind(pid):env.DB.prepare(q+' ORDER BY i.updated_at DESC LIMIT 1000')); return json({inspections:rows}); }
  if (path === '/api/inspections' && method === 'POST') { const d=await bodyJson(request); if(!d.project_id||!d.template_id||!d.title)return json({error:'Project, template and title are required'},400); const iid=id('ins'); await env.DB.prepare(`INSERT INTO inspections(id,project_id,equipment_id,template_id,title,status,assigned_to,created_by,created_at,updated_at) VALUES(?,?,?,?,?,'in_progress',?,?,?,?)`).bind(iid,d.project_id,nullable(d.equipment_id,100),d.template_id,clean(d.title,220),nullable(d.assigned_to,100),user.id,now(),now()).run(); const items=await all(env.DB.prepare('SELECT id FROM template_items WHERE template_id=? ORDER BY sort_order').bind(d.template_id)); for(const item of items){ await env.DB.prepare(`INSERT INTO inspection_results(id,inspection_id,template_item_id) VALUES(?,?,?)`).bind(id('res'),iid,item.id).run(); } ctx.waitUntil(audit(env,user.id,'create','inspection',iid)); return json({id:iid},201); }
  const insMatch=path.match(/^\/api\/inspections\/([^/]+)$/); if(insMatch&&method==='GET'){ const iid=insMatch[1]; const inspection=await env.DB.prepare(`SELECT i.*,t.name template_name,t.stage,t.discipline,e.tag equipment_tag,e.name equipment_name FROM inspections i JOIN templates t ON t.id=i.template_id LEFT JOIN equipment e ON e.id=i.equipment_id WHERE i.id=?`).bind(iid).first(); if(!inspection)return json({error:'Not found'},404); const results=await all(env.DB.prepare(`SELECT r.*,ti.section,ti.item_text,ti.response_type,ti.mandatory,ti.hold_point,u.name completed_by_name FROM inspection_results r JOIN template_items ti ON ti.id=r.template_item_id LEFT JOIN users u ON u.id=r.completed_by WHERE r.inspection_id=? ORDER BY ti.sort_order`).bind(iid)); return json({inspection,results}); }
  const resMatch=path.match(/^\/api\/inspection-results\/([^/]+)$/); if(resMatch&&method==='PATCH'){ const d=await bodyJson(request); const rid=resMatch[1]; const existing=await env.DB.prepare(`SELECT r.*,i.project_id,i.equipment_id,i.id inspection_id,ti.item_text,ti.hold_point FROM inspection_results r JOIN inspections i ON i.id=r.inspection_id JOIN template_items ti ON ti.id=r.template_item_id WHERE r.id=?`).bind(rid).first(); if(!existing)return json({error:'Not found'},404); const result=['pass','fail','na',null].includes(d.result)?d.result:null; await env.DB.prepare(`UPDATE inspection_results SET result=?,value_text=?,comment=?,completed_by=?,completed_at=? WHERE id=?`).bind(result,nullable(d.value_text,500),nullable(d.comment,1200),user.id,now(),rid).run(); if(result==='fail'){ const exists=await env.DB.prepare(`SELECT id FROM defects WHERE inspection_id=? AND title=? AND status NOT IN ('closed','verified') LIMIT 1`).bind(existing.inspection_id,`Failed check: ${existing.item_text}`).first(); if(!exists){ const did=id('def'); await env.DB.prepare(`INSERT INTO defects(id,project_id,equipment_id,inspection_id,title,description,discipline,severity,category,status,source,raised_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,'high','A','open','inspection',?,?,?,?)`).bind(did,existing.project_id,existing.equipment_id,existing.inspection_id,`Failed check: ${existing.item_text}`,nullable(d.comment,1200),'QA',user.id,now(),now()).run(); } } const counts=await env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN result IS NOT NULL THEN 1 ELSE 0 END) answered,SUM(CASE WHEN result='fail' THEN 1 ELSE 0 END) fails FROM inspection_results WHERE inspection_id=?`).bind(existing.inspection_id).first(); const status=Number(counts?.answered||0)===Number(counts?.total||0)?(Number(counts?.fails||0)>0?'submitted':'witness_required'):'in_progress'; await env.DB.prepare('UPDATE inspections SET status=?,updated_at=? WHERE id=?').bind(status,now(),existing.inspection_id).run(); ctx.waitUntil(audit(env,user.id,'update_result','inspection_result',rid,{result})); return json({ok:true,status}); }

  if (path === '/api/defects' && method === 'GET') { const pid=url.searchParams.get('project_id'); const q=`SELECT d.*,e.tag equipment_tag,e.name equipment_name,u.name assigned_name,r.name raised_name FROM defects d LEFT JOIN equipment e ON e.id=d.equipment_id LEFT JOIN users u ON u.id=d.assigned_to LEFT JOIN users r ON r.id=d.raised_by`; const rows=await all(pid?env.DB.prepare(q+' WHERE d.project_id=? ORDER BY CASE d.category WHEN \'A\' THEN 1 WHEN \'B\' THEN 2 ELSE 3 END,d.updated_at DESC').bind(pid):env.DB.prepare(q+' ORDER BY d.updated_at DESC LIMIT 1000')); return json({defects:rows}); }
  if (path === '/api/defects' && method === 'POST') { const d=await bodyJson(request); if(!d.project_id||!d.title)return json({error:'Project and title are required'},400); const did=id('def'); await env.DB.prepare(`INSERT INTO defects(id,project_id,equipment_id,inspection_id,title,description,discipline,severity,category,status,assigned_to,due_date,source,raised_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,'open',?,?,?, ?,?,?)`).bind(did,d.project_id,nullable(d.equipment_id,100),nullable(d.inspection_id,100),clean(d.title,220),nullable(d.description,2000),clean(d.discipline||'general',80),clean(d.severity||'medium',20),clean(d.category||'B',1),nullable(d.assigned_to,100),nullable(d.due_date,30),clean(d.source||'field',40),user.id,now(),now()).run(); ctx.waitUntil(audit(env,user.id,'create','defect',did)); return json({id:did},201); }
  const defMatch=path.match(/^\/api\/defects\/([^/]+)$/); if(defMatch&&method==='PATCH'){ const d=await bodyJson(request), did=defMatch[1]; const status=clean(d.status||'open',30); await env.DB.prepare(`UPDATE defects SET status=?,rectification_note=COALESCE(?,rectification_note),verified_by=CASE WHEN ? IN ('verified','closed') THEN ? ELSE verified_by END,verified_at=CASE WHEN ? IN ('verified','closed') THEN ? ELSE verified_at END,updated_at=? WHERE id=?`).bind(status,nullable(d.rectification_note,2000),status,user.id,status,now(),now(),did).run(); ctx.waitUntil(audit(env,user.id,'update','defect',did,{status})); return json({ok:true}); }

  if (path === '/api/bms-points' && method === 'GET') { const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT p.*,e.tag equipment_tag FROM bms_points p LEFT JOIN equipment e ON e.id=p.equipment_id WHERE p.project_id=? ORDER BY p.controller,p.point_name LIMIT 5000`).bind(pid):env.DB.prepare(`SELECT p.*,e.tag equipment_tag FROM bms_points p LEFT JOIN equipment e ON e.id=p.equipment_id ORDER BY p.updated_at DESC LIMIT 2000`)); return json({points:rows}); }
  if (path === '/api/bms-points/import' && method === 'POST') { if(!roleAllowed(user,['admin','pm','engineer','technician']))return json({error:'Insufficient permission'},403); const d=await bodyJson(request); if(!d.project_id||!Array.isArray(d.points))return json({error:'project_id and points array required'},400); let created=0; for(const p of d.points.slice(0,5000)){ if(!p.point_name||!p.point_type)continue; await env.DB.prepare(`INSERT INTO bms_points(id,project_id,equipment_id,system_id,controller,point_name,point_type,io_address,cable_number,terminal,field_device,engineering_units,normal_state,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id('pt'),d.project_id,nullable(p.equipment_id,100),nullable(p.system_id,100),nullable(p.controller,120),clean(p.point_name,180),clean(p.point_type,20).toUpperCase(),nullable(p.io_address,80),nullable(p.cable_number,80),nullable(p.terminal,80),nullable(p.field_device,160),nullable(p.engineering_units,50),nullable(p.normal_state,80),now(),now()).run(); created++; } ctx.waitUntil(audit(env,user.id,'bulk_import','bms_point',d.project_id,{created})); return json({created},201); }
  const ptMatch=path.match(/^\/api\/bms-points\/([^/]+)$/); if(ptMatch&&method==='PATCH'){ const d=await bodyJson(request), pid=ptMatch[1]; const vals=['pass','fail','na','not_tested']; const fs=vals.includes(d.field_status)?d.field_status:'not_tested', cs=vals.includes(d.controller_status)?d.controller_status:'not_tested', gs=vals.includes(d.graphics_status)?d.graphics_status:'not_tested', as=vals.includes(d.alarm_status)?d.alarm_status:'not_tested'; const statuses=[fs,cs,gs,as].filter(x=>x!=='na'); const overall=statuses.includes('fail')?'fail':(statuses.length&&statuses.every(x=>x==='pass')?'pass':'not_tested'); await env.DB.prepare(`UPDATE bms_points SET field_status=?,controller_status=?,graphics_status=?,alarm_status=?,overall_status=?,comment=?,tested_by=?,tested_at=?,updated_at=? WHERE id=?`).bind(fs,cs,gs,as,overall,nullable(d.comment,1000),user.id,now(),now(),pid).run(); return json({ok:true,overall}); }

  if (path === '/api/turnover' && method === 'GET') { const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT t.*,s.code system_code,s.name system_name FROM turnover_packages t LEFT JOIN systems s ON s.id=t.system_id WHERE t.project_id=? ORDER BY t.readiness_percent,s.code`).bind(pid):env.DB.prepare(`SELECT t.*,s.code system_code,s.name system_name FROM turnover_packages t LEFT JOIN systems s ON s.id=t.system_id ORDER BY t.updated_at DESC LIMIT 500`)); return json({packages:rows}); }
  if (path === '/api/turnover' && method === 'POST') { const d=await bodyJson(request); if(!d.project_id||!d.name)return json({error:'Project and package name required'},400); const tid=id('top'); await env.DB.prepare(`INSERT INTO turnover_packages(id,project_id,system_id,name,status,readiness_percent,required_items,complete_items,created_at,updated_at) VALUES(?,?,?,?,'draft',0,0,0,?,?)`).bind(tid,d.project_id,nullable(d.system_id,100),clean(d.name,200),now(),now()).run(); const defaults=['Approved as-built drawings','Equipment/tag register complete','QA/ITR records complete','Electrical test records complete','BMS I/O checkout complete','Functional tests approved','Witness tests signed','Category A defects closed','Category B defects accepted','O&M manuals available','Controller/software backups archived','Training/operations handover complete']; let i=0; for(const name of defaults){ await env.DB.prepare(`INSERT INTO turnover_items(id,package_id,category,name,required,status,updated_at) VALUES(?,?,?, ?,1,'missing',?)`).bind(id('toi'),tid,i<2?'Drawings':i<7?'Testing':i<9?'Defects':'Handover',name,now()).run(); i++; } await refreshTurnover(env,tid); return json({id:tid},201); }
  const topMatch=path.match(/^\/api\/turnover\/([^/]+)$/); if(topMatch&&method==='GET'){ const pack=await env.DB.prepare(`SELECT t.*,s.code system_code,s.name system_name FROM turnover_packages t LEFT JOIN systems s ON s.id=t.system_id WHERE t.id=?`).bind(topMatch[1]).first(); if(!pack)return json({error:'Not found'},404); const items=await all(env.DB.prepare(`SELECT * FROM turnover_items WHERE package_id=? ORDER BY category,name`).bind(topMatch[1])); return json({package:pack,items}); }
  const toiMatch=path.match(/^\/api\/turnover-items\/([^/]+)$/); if(toiMatch&&method==='PATCH'){ const d=await bodyJson(request), iid=toiMatch[1]; const row=await env.DB.prepare('SELECT package_id FROM turnover_items WHERE id=?').bind(iid).first(); if(!row)return json({error:'Not found'},404); await env.DB.prepare(`UPDATE turnover_items SET status=?,note=?,updated_by=?,updated_at=? WHERE id=?`).bind(clean(d.status||'missing',30),nullable(d.note,1200),user.id,now(),iid).run(); await refreshTurnover(env,row.package_id); return json({ok:true}); }

  if (path === '/api/witness' && method === 'GET') { const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT w.*,i.title inspection_title,s.code system_code FROM witness_requests w LEFT JOIN inspections i ON i.id=w.inspection_id LEFT JOIN systems s ON s.id=w.system_id WHERE w.project_id=? ORDER BY w.requested_for`).bind(pid):env.DB.prepare(`SELECT w.*,i.title inspection_title,s.code system_code FROM witness_requests w LEFT JOIN inspections i ON i.id=w.inspection_id LEFT JOIN systems s ON s.id=w.system_id ORDER BY w.updated_at DESC LIMIT 500`)); return json({witness:rows}); }
  if (path === '/api/witness' && method === 'POST') { const d=await bodyJson(request); if(!d.project_id||!d.requested_for)return json({error:'Project and requested date/time required'},400); const wid=id('wit'); await env.DB.prepare(`INSERT INTO witness_requests(id,project_id,inspection_id,system_id,requested_for,witness_party,status,created_by,created_at,updated_at) VALUES(?,?,?,?,?,?,'requested',?,?,?)`).bind(wid,d.project_id,nullable(d.inspection_id,100),nullable(d.system_id,100),clean(d.requested_for,40),nullable(d.witness_party,180),user.id,now(),now()).run(); return json({id:wid},201); }

  if (path === '/api/users' && method === 'GET') { const rows=await all(env.DB.prepare(`SELECT id,email,name,role,active,created_at FROM users ORDER BY name`)); return json({users:rows}); }
  if (path === '/api/users' && method === 'POST') { if(!roleAllowed(user,['admin']))return json({error:'Admin only'},403); const d=await bodyJson(request); if(!d.email||!d.name||String(d.password||'').length<12)return json({error:'Name, email and 12+ character password required'},400); const pw=await hashPassword(String(d.password)), uid=id('usr'); await env.DB.prepare(`INSERT INTO users(id,email,name,role,password_salt,password_hash,active,created_at,updated_at) VALUES(?,?,?,?,?,?,1,?,?)`).bind(uid,clean(d.email,200).toLowerCase(),clean(d.name,120),clean(d.role||'technician',30),pw.salt,pw.hash,now(),now()).run(); return json({id:uid},201); }

  if (path === '/api/files' && method === 'POST') { const projectId=url.searchParams.get('project_id'); if(!projectId)return json({error:'project_id required'},400); const fileName=clean(request.headers.get('x-file-name')||'evidence.bin',240), contentType=request.headers.get('content-type')||'application/octet-stream'; if(request.body===null)return json({error:'File body required'},400); const key=`${projectId}/${new Date().toISOString().slice(0,10)}/${crypto.randomUUID()}-${fileName.replace(/[^a-zA-Z0-9._-]/g,'_')}`; const obj=await env.FILES.put(key,request.body,{httpMetadata:{contentType},customMetadata:{uploadedBy:user.id}}); const fid=id('file'); await env.DB.prepare(`INSERT INTO files(id,project_id,equipment_id,inspection_id,defect_id,object_key,file_name,content_type,size_bytes,uploaded_by,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).bind(fid,projectId,nullable(url.searchParams.get('equipment_id'),100),nullable(url.searchParams.get('inspection_id'),100),nullable(url.searchParams.get('defect_id'),100),key,fileName,contentType,Number(obj?.size||0),user.id,now()).run(); return json({id:fid,file_name:fileName},201); }
  const fileMatch=path.match(/^\/api\/files\/([^/]+)$/); if(fileMatch&&method==='GET'){ const f=await env.DB.prepare('SELECT * FROM files WHERE id=?').bind(fileMatch[1]).first(); if(!f)return json({error:'Not found'},404); const o=await env.FILES.get(f.object_key); if(!o)return json({error:'Object missing'},404); const h=new Headers(); o.writeHttpMetadata(h); h.set('etag',o.httpEtag); h.set('content-disposition',`inline; filename="${f.file_name.replaceAll('"','')}"`); return new Response(o.body,{headers:h}); }

  if (path === '/api/readiness' && method === 'GET') {
    const pid=url.searchParams.get('project_id'); if(!pid)return json({error:'project_id required'},400);
    const [eq,ins,defs,pts,wit,turn] = await Promise.all([
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='complete' THEN 1 ELSE 0 END) complete FROM equipment WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status IN ('approved','complete') THEN 1 ELSE 0 END) complete,SUM(CASE WHEN status IN ('submitted','witness_required') THEN 1 ELSE 0 END) pending FROM inspections WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT SUM(CASE WHEN status NOT IN ('closed','verified') THEN 1 ELSE 0 END) open,SUM(CASE WHEN category='A' AND status NOT IN ('closed','verified') THEN 1 ELSE 0 END) cat_a FROM defects WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN overall_status='pass' THEN 1 ELSE 0 END) passed,SUM(CASE WHEN overall_status='fail' THEN 1 ELSE 0 END) failed FROM bms_points WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status IN ('completed','accepted','passed') THEN 1 ELSE 0 END) complete,SUM(CASE WHEN status='requested' THEN 1 ELSE 0 END) pending FROM witness_requests WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,AVG(readiness_percent) avg_readiness,SUM(CASE WHEN readiness_percent=100 THEN 1 ELSE 0 END) ready FROM turnover_packages WHERE project_id=?`).bind(pid).first()
    ]);
    const blockers=[]; let earned=0,totalWeight=100;
    if(Number(defs?.cat_a||0)===0) earned+=20; else blockers.push({code:'NO_CAT_A',severity:'critical',label:`${defs.cat_a} Category A defect(s) open`,route:'defects'});
    if(Number(ins?.total||0)>0 && Number(ins?.complete||0)===Number(ins?.total||0)) earned+=20; else blockers.push({code:'TESTS_COMPLETE',severity:'high',label:`${Math.max(0,Number(ins?.total||0)-Number(ins?.complete||0))} test(s) not approved`,route:'inspections'});
    if(Number(pts?.total||0)===0 || (Number(pts?.failed||0)===0 && Number(pts?.passed||0)===Number(pts?.total||0))) earned+=20; else blockers.push({code:'BMS_PROVEN',severity:Number(pts?.failed||0)?'critical':'high',label:`${pts.failed||0} failed / ${Math.max(0,Number(pts?.total||0)-Number(pts?.passed||0))} unproven BMS point(s)`,route:'points'});
    if(Number(wit?.pending||0)===0) earned+=15; else blockers.push({code:'WITNESS_CLEAR',severity:'high',label:`${wit.pending} witness request(s) pending`,route:'witness'});
    if(Number(turn?.total||0)>0 && Number(turn?.ready||0)===Number(turn?.total||0)) earned+=15; else blockers.push({code:'TURNOVER_READY',severity:'medium',label:`Turnover readiness ${Math.round(Number(turn?.avg_readiness||0))}%`,route:'turnover'});
    const eqPct=Number(eq?.total||0)?Math.round(Number(eq?.complete||0)*100/Number(eq.total)):100; earned += Math.round(10*eqPct/100);
    const score=Math.min(100,Math.round(earned)); const metrics={equipment:eq||{},inspections:ins||{},defects:defs||{},points:pts||{},witness:wit||{},turnover:turn||{},equipment_percent:eqPct};
    if(url.searchParams.get('snapshot')==='1'){await env.DB.prepare(`INSERT INTO readiness_snapshots(id,project_id,score,blocker_count,blockers_json,metrics_json,created_at) VALUES(?,?,?,?,?,?,?)`).bind(id('rdy'),pid,score,blockers.length,JSON.stringify(blockers),JSON.stringify(metrics),now()).run();}
    return json({score,grade:score>=95?'READY':score>=80?'NEAR READY':score>=60?'AT RISK':'BLOCKED',blockers,metrics});
  }

  if (path === '/api/actions' && method === 'GET') { const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT a.*,u.name assigned_name FROM action_items a LEFT JOIN users u ON u.id=a.assigned_to WHERE a.project_id=? ORDER BY CASE a.priority WHEN 'critical' THEN 1 WHEN 'high' THEN 2 WHEN 'normal' THEN 3 ELSE 4 END,a.due_at`).bind(pid):env.DB.prepare(`SELECT a.*,u.name assigned_name FROM action_items a LEFT JOIN users u ON u.id=a.assigned_to ORDER BY a.updated_at DESC LIMIT 500`)); return json({actions:rows}); }
  if (path === '/api/actions' && method === 'POST') { const d=await bodyJson(request); if(!d.project_id||!d.title)return json({error:'Project and title required'},400); const aid=id('act'); await env.DB.prepare(`INSERT INTO action_items(id,project_id,system_id,equipment_id,title,description,priority,status,assigned_to,due_at,source,source_id,created_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,'open',?,?,?,?,?,?,?)`).bind(aid,d.project_id,nullable(d.system_id,100),nullable(d.equipment_id,100),clean(d.title,220),nullable(d.description,2000),clean(d.priority||'normal',20),nullable(d.assigned_to,100),nullable(d.due_at,40),clean(d.source||'manual',40),nullable(d.source_id,100),user.id,now(),now()).run(); return json({id:aid},201); }
  const actionMatch=path.match(/^\/api\/actions\/([^/]+)$/); if(actionMatch&&method==='PATCH'){const d=await bodyJson(request);await env.DB.prepare(`UPDATE action_items SET status=COALESCE(?,status),priority=COALESCE(?,priority),assigned_to=COALESCE(?,assigned_to),due_at=COALESCE(?,due_at),updated_at=? WHERE id=?`).bind(nullable(d.status,30),nullable(d.priority,20),nullable(d.assigned_to,100),nullable(d.due_at,40),now(),actionMatch[1]).run();return json({ok:true});}

  if (path === '/api/signoffs' && method === 'GET') { const pid=url.searchParams.get('project_id'); const rows=await all(pid?env.DB.prepare(`SELECT * FROM signoffs WHERE project_id=? ORDER BY signed_at DESC`).bind(pid):env.DB.prepare(`SELECT * FROM signoffs ORDER BY signed_at DESC LIMIT 500`)); return json({signoffs:rows}); }
  if (path === '/api/signoffs' && method === 'POST') { const d=await bodyJson(request); if(!d.project_id||!d.entity_type||!d.entity_id||!d.signer_name)return json({error:'Project, entity and signer required'},400); const sid=id('sig'); await env.DB.prepare(`INSERT INTO signoffs(id,project_id,entity_type,entity_id,signoff_type,signer_name,signer_company,signer_role,signature_data,declaration,signed_at,created_by,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(sid,d.project_id,clean(d.entity_type,40),clean(d.entity_id,100),clean(d.signoff_type||'acceptance',40),clean(d.signer_name,160),nullable(d.signer_company,160),nullable(d.signer_role,120),nullable(d.signature_data,100000),nullable(d.declaration,1200),now(),user.id,now()).run();ctx.waitUntil(audit(env,user.id,'signoff',d.entity_type,d.entity_id,{signer:d.signer_name,type:d.signoff_type}));return json({id:sid,signed_at:now()},201); }

  if (path === '/api/ai/project-builder' && method === 'POST') { const d=await bodyJson(request); if(!d.project_id||!d.source_text)return json({error:'Project and source text required'},400); const text=clean(d.source_text,50000); const tags=[...text.matchAll(/\b(?:AHU|FCU|MSSB|MCC|VSD|PUMP|FAN|CHW|HHW|BMS|JACE|DDC|UPS)[-_ ]?\d{1,4}[A-Z]?\b/gi)].map(m=>m[0].toUpperCase().replace(/\s+/g,'-')); const unique=[...new Set(tags)].slice(0,500); const proposal={equipment:unique.map(tag=>({tag,name:tag,equipment_type:tag.split(/[-_]/)[0]})),suggested_templates:['Cable & Termination QA','BMS Point-to-Point / I/O Checkout','MSSB/MCC Pre-Energisation QA','Functional Performance Test'],notes:'Machine-assisted proposal only. Review against approved drawings, specifications and sequence before applying.'}; const jid=id('aij'); await env.DB.prepare(`INSERT INTO ai_jobs(id,project_id,job_type,status,source_text,proposal_json,created_by,created_at,updated_at) VALUES(?,?,'project_builder','proposed',?,?,?,?,?)`).bind(jid,d.project_id,text,JSON.stringify(proposal),user.id,now(),now()).run(); return json({id:jid,proposal},201); }

  if (path === '/api/sync/batch' && method === 'POST') { const d=await bodyJson(request); if(!Array.isArray(d.operations))return json({error:'operations array required'},400); const results=[]; for(const op of d.operations.slice(0,250)){const key=clean(op.operation_key,120);if(!key){results.push({ok:false,error:'missing operation_key'});continue;}const existing=await env.DB.prepare('SELECT id FROM sync_operations WHERE operation_key=?').bind(key).first();if(existing){results.push({ok:true,duplicate:true,operation_key:key});continue;}await env.DB.prepare(`INSERT INTO sync_operations(id,user_id,device_id,operation_key,entity_type,entity_id,operation,payload_json,status,created_at) VALUES(?,?,?,?,?,?,?,?,?,?)`).bind(id('syn'),user.id,nullable(d.device_id,120),key,clean(op.entity_type||'unknown',50),nullable(op.entity_id,100),clean(op.operation||'update',30),JSON.stringify(op.payload||{}),'received',now()).run();results.push({ok:true,operation_key:key,accepted:true});}return json({results}); }


  // Qx V5 field productivity + completion intelligence routes.
  if (path === '/api/my-work' && method === 'GET') {
    const pid=url.searchParams.get('project_id');
    const actionSql=`SELECT a.*,p.code project_code,e.tag equipment_tag FROM action_items a LEFT JOIN projects p ON p.id=a.project_id LEFT JOIN equipment e ON e.id=a.equipment_id WHERE a.assigned_to=? AND a.status NOT IN ('closed','complete','done') ${pid?'AND a.project_id=?':''} ORDER BY CASE a.priority WHEN 'critical' THEN 1 WHEN 'high' THEN 2 WHEN 'normal' THEN 3 ELSE 4 END, COALESCE(a.due_at,'9999-12-31') LIMIT 100`;
    const defectSql=`SELECT d.*,p.code project_code,e.tag equipment_tag FROM defects d LEFT JOIN projects p ON p.id=d.project_id LEFT JOIN equipment e ON e.id=d.equipment_id WHERE d.assigned_to=? AND d.status NOT IN ('closed','verified') ${pid?'AND d.project_id=?':''} ORDER BY CASE d.category WHEN 'A' THEN 1 WHEN 'B' THEN 2 ELSE 3 END,COALESCE(d.due_date,'9999-12-31') LIMIT 100`;
    const inspectionSql=`SELECT i.*,p.code project_code,e.tag equipment_tag,t.name template_name FROM inspections i LEFT JOIN projects p ON p.id=i.project_id LEFT JOIN equipment e ON e.id=i.equipment_id LEFT JOIN templates t ON t.id=i.template_id WHERE i.assigned_to=? AND i.status NOT IN ('approved','rejected') ${pid?'AND i.project_id=?':''} ORDER BY i.updated_at DESC LIMIT 100`;
    const witnessSql=`SELECT w.*,p.code project_code,s.code system_code,i.title inspection_title FROM witness_requests w LEFT JOIN projects p ON p.id=w.project_id LEFT JOIN systems s ON s.id=w.system_id LEFT JOIN inspections i ON i.id=w.inspection_id WHERE w.status IN ('requested','scheduled') ${pid?'AND w.project_id=?':''} ORDER BY w.requested_for LIMIT 100`;
    const bindMaybe=(sql)=>pid?env.DB.prepare(sql).bind(user.id,pid):env.DB.prepare(sql).bind(user.id);
    const [actions,defects,inspections,witness]=await Promise.all([
      all(bindMaybe(actionSql)),all(bindMaybe(defectSql)),all(bindMaybe(inspectionSql)),
      all(pid?env.DB.prepare(witnessSql).bind(pid):env.DB.prepare(witnessSql))
    ]);
    return json({actions,defects,inspections,witness});
  }

  if (path === '/api/completion-matrix' && method === 'GET') {
    const pid=url.searchParams.get('project_id'); if(!pid)return json({error:'project_id required'},400);
    const systems=await all(env.DB.prepare(`SELECT s.*,
      (SELECT COUNT(*) FROM equipment e WHERE e.project_id=s.project_id AND (e.system=s.code OR e.system=s.name)) equipment_total,
      (SELECT COUNT(*) FROM equipment e WHERE e.project_id=s.project_id AND (e.system=s.code OR e.system=s.name) AND e.status='complete') equipment_complete,
      (SELECT COUNT(*) FROM inspections i JOIN equipment e ON e.id=i.equipment_id WHERE i.project_id=s.project_id AND (e.system=s.code OR e.system=s.name)) tests_total,
      (SELECT COUNT(*) FROM inspections i JOIN equipment e ON e.id=i.equipment_id WHERE i.project_id=s.project_id AND (e.system=s.code OR e.system=s.name) AND i.status='approved') tests_approved,
      (SELECT COUNT(*) FROM defects d LEFT JOIN equipment e ON e.id=d.equipment_id WHERE d.project_id=s.project_id AND (e.system=s.code OR e.system=s.name OR d.equipment_id IS NULL) AND d.category='A' AND d.status NOT IN ('closed','verified')) cat_a,
      (SELECT COUNT(*) FROM bms_points b WHERE b.project_id=s.project_id AND b.system_id=s.id) points_total,
      (SELECT COUNT(*) FROM bms_points b WHERE b.project_id=s.project_id AND b.system_id=s.id AND b.overall_status='pass') points_passed,
      (SELECT ROUND(AVG(t.readiness_percent)) FROM turnover_packages t WHERE t.system_id=s.id) turnover_readiness
      FROM systems s WHERE s.project_id=? ORDER BY s.code`).bind(pid));
    const rows=systems.map(x=>{
      const eqTotal=Number(x.equipment_total||0), eqDone=Number(x.equipment_complete||0), testTotal=Number(x.tests_total||0), testDone=Number(x.tests_approved||0), ptTotal=Number(x.points_total||0), ptDone=Number(x.points_passed||0), catA=Number(x.cat_a||0), turn=Number(x.turnover_readiness||0);
      const qa=testTotal?Math.round(testDone*100/testTotal):(eqTotal?0:100), io=ptTotal?Math.round(ptDone*100/ptTotal):100, install=eqTotal?Math.round(eqDone*100/eqTotal):100;
      const score=Math.round(install*.25+qa*.25+io*.2+(catA?0:15)+turn*.15);
      return {...x,install_percent:install,qa_percent:qa,io_percent:io,punch_clear:catA===0,turnover_percent:turn,readiness_score:Math.max(0,Math.min(100,score))};
    });
    return json({systems:rows});
  }

  if (path === '/api/equipment/import' && method === 'POST') {
    if(!roleAllowed(user,['admin','pm','engineer','technician']))return json({error:'Insufficient permission'},403);
    const d=await bodyJson(request); if(!d.project_id||!Array.isArray(d.equipment))return json({error:'project_id and equipment array required'},400);
    let created=0,skipped=0;
    for(const e of d.equipment.slice(0,5000)){
      if(!e.tag||!e.name){skipped++;continue;}
      try{await env.DB.prepare(`INSERT INTO equipment(id,project_id,tag,name,system,subsystem,building,level,area,equipment_type,manufacturer,model,serial_number,status,qr_code,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,'not_started',?,?,?)`).bind(id('eq'),d.project_id,clean(e.tag,120),clean(e.name,220),nullable(e.system,160),nullable(e.subsystem,160),nullable(e.building,120),nullable(e.level,80),nullable(e.area,160),nullable(e.equipment_type,80),nullable(e.manufacturer,120),nullable(e.model,120),nullable(e.serial_number,120),clean(e.tag,120),now(),now()).run();created++;}catch{skipped++;}
    }
    ctx.waitUntil(audit(env,user.id,'bulk_import','equipment',d.project_id,{created,skipped}).catch(()=>{}));
    return json({created,skipped},201);
  }

  const passportMatch=path.match(/^\/api\/equipment\/([^/]+)\/passport$/);
  if(passportMatch&&method==='GET'){
    const eid=passportMatch[1];
    const equipment=await env.DB.prepare(`SELECT e.*,p.code project_code,p.name project_name FROM equipment e JOIN projects p ON p.id=e.project_id WHERE e.id=?`).bind(eid).first();
    if(!equipment)return json({error:'Equipment not found'},404);
    const [inspections,defects,points,files,signoffs]=await Promise.all([
      all(env.DB.prepare(`SELECT i.*,t.name template_name FROM inspections i LEFT JOIN templates t ON t.id=i.template_id WHERE i.equipment_id=? ORDER BY i.updated_at DESC`).bind(eid)),
      all(env.DB.prepare(`SELECT * FROM defects WHERE equipment_id=? ORDER BY updated_at DESC`).bind(eid)),
      all(env.DB.prepare(`SELECT * FROM bms_points WHERE equipment_id=? ORDER BY controller,point_name`).bind(eid)),
      all(env.DB.prepare(`SELECT f.*,u.name uploaded_by_name FROM files f LEFT JOIN users u ON u.id=f.uploaded_by WHERE f.equipment_id=? ORDER BY f.created_at DESC`).bind(eid)),
      all(env.DB.prepare(`SELECT * FROM signoffs WHERE entity_type='equipment' AND entity_id=? ORDER BY signed_at DESC`).bind(eid))
    ]);
    return json({equipment,inspections,defects,points,files,signoffs});
  }

  const equipmentPatch=path.match(/^\/api\/equipment\/([^/]+)$/);
  if(equipmentPatch&&method==='PATCH'){
    const d=await bodyJson(request),eid=equipmentPatch[1];
    const allowed=['not_started','qa','commissioning','witness','blocked','complete'];
    const status=allowed.includes(d.status)?d.status:null;
    await env.DB.prepare(`UPDATE equipment SET status=COALESCE(?,status),serial_number=COALESCE(?,serial_number),manufacturer=COALESCE(?,manufacturer),model=COALESCE(?,model),updated_at=? WHERE id=?`).bind(status,nullable(d.serial_number,120),nullable(d.manufacturer,120),nullable(d.model,120),now(),eid).run();
    ctx.waitUntil(audit(env,user.id,'update','equipment',eid,{status}).catch(()=>{}));return json({ok:true});
  }

  if(path==='/api/files'&&method==='GET'){
    const pid=url.searchParams.get('project_id'),eid=url.searchParams.get('equipment_id'),did=url.searchParams.get('defect_id'),iid=url.searchParams.get('inspection_id');
    let sql=`SELECT f.*,u.name uploaded_by_name,e.tag equipment_tag FROM files f LEFT JOIN users u ON u.id=f.uploaded_by LEFT JOIN equipment e ON e.id=f.equipment_id WHERE 1=1`,vals=[];
    if(pid){sql+=' AND f.project_id=?';vals.push(pid)} if(eid){sql+=' AND f.equipment_id=?';vals.push(eid)} if(did){sql+=' AND f.defect_id=?';vals.push(did)} if(iid){sql+=' AND f.inspection_id=?';vals.push(iid)} sql+=' ORDER BY f.created_at DESC LIMIT 500';
    const stmt=env.DB.prepare(sql);const rows=await all(vals.length?stmt.bind(...vals):stmt);return json({files:rows});
  }

  const witnessPatch=path.match(/^\/api\/witness\/([^/]+)$/);
  if(witnessPatch&&method==='PATCH'){
    const d=await bodyJson(request);await env.DB.prepare(`UPDATE witness_requests SET status=COALESCE(?,status),outcome=COALESCE(?,outcome),comment=COALESCE(?,comment),updated_at=? WHERE id=?`).bind(nullable(d.status,30),nullable(d.outcome,1000),nullable(d.comment,1000),now(),witnessPatch[1]).run();return json({ok:true});
  }

  if(path==='/api/ai/project-builder/apply'&&method==='POST'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403);
    const d=await bodyJson(request);if(!d.job_id)return json({error:'job_id required'},400);
    const job=await env.DB.prepare(`SELECT * FROM ai_jobs WHERE id=?`).bind(clean(d.job_id,100)).first();if(!job)return json({error:'Builder job not found'},404);
    let proposal={};try{proposal=JSON.parse(job.proposal_json||'{}')}catch{}
    let created=0,skipped=0;
    for(const e of (proposal.equipment||[]).slice(0,1000)){
      try{await env.DB.prepare(`INSERT INTO equipment(id,project_id,tag,name,equipment_type,status,qr_code,created_at,updated_at) VALUES(?,?,?,?,?,'not_started',?,?,?)`).bind(id('eq'),job.project_id,clean(e.tag,120),clean(e.name||e.tag,220),nullable(e.equipment_type,80),clean(e.tag,120),now(),now()).run();created++;}catch{skipped++;}
    }
    await env.DB.prepare(`UPDATE ai_jobs SET status='applied',approved_json=?,reviewed_by=?,updated_at=? WHERE id=?`).bind(JSON.stringify({created,skipped}),user.id,now(),job.id).run();
    return json({created,skipped});
  }

  if(path==='/api/report/project'&&method==='GET'){
    const pid=url.searchParams.get('project_id');if(!pid)return json({error:'project_id required'},400);
    const project=await env.DB.prepare(`SELECT * FROM projects WHERE id=?`).bind(pid).first();if(!project)return json({error:'Project not found'},404);
    const [dash,systems,files,signoffs]=await Promise.all([
      (async()=>{const req=new Request(new URL(`/api/dashboard?project_id=${encodeURIComponent(pid)}`,url.origin),{headers:request.headers});return null})(),
      all(env.DB.prepare(`SELECT * FROM systems WHERE project_id=? ORDER BY code`).bind(pid)),
      all(env.DB.prepare(`SELECT f.*,e.tag equipment_tag FROM files f LEFT JOIN equipment e ON e.id=f.equipment_id WHERE f.project_id=? ORDER BY f.created_at DESC`).bind(pid)),
      all(env.DB.prepare(`SELECT * FROM signoffs WHERE project_id=? ORDER BY signed_at DESC`).bind(pid))
    ]);
    const counts={
      equipment:await env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='complete' THEN 1 ELSE 0 END) complete FROM equipment WHERE project_id=?`).bind(pid).first(),
      defects:await env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status NOT IN ('closed','verified') THEN 1 ELSE 0 END) open,SUM(CASE WHEN category='A' AND status NOT IN ('closed','verified') THEN 1 ELSE 0 END) cat_a FROM defects WHERE project_id=?`).bind(pid).first(),
      tests:await env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='approved' THEN 1 ELSE 0 END) approved FROM inspections WHERE project_id=?`).bind(pid).first(),
      points:await env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN overall_status='pass' THEN 1 ELSE 0 END) passed,SUM(CASE WHEN overall_status='fail' THEN 1 ELSE 0 END) failed FROM bms_points WHERE project_id=?`).bind(pid).first(),
      turnover:await env.DB.prepare(`SELECT COUNT(*) total,ROUND(AVG(readiness_percent)) avg_readiness FROM turnover_packages WHERE project_id=?`).bind(pid).first()
    };
    return json({project,counts,systems,files,signoffs,generated_at:now()});
  }


  // ===== QX V6 CLOSEOUT STUDIO / COMMISSIONING COPILOT =====
  if(path==='/api/signoffs'&&method==='GET'){
    const pid=url.searchParams.get('project_id'); if(!pid)return json({error:'project_id required'},400);
    const rows=await all(env.DB.prepare(`SELECT s.*,u.name created_by_name FROM signoffs s LEFT JOIN users u ON u.id=s.created_by WHERE s.project_id=? ORDER BY s.signed_at DESC`).bind(pid));
    return json({signoffs:rows});
  }
  if(path==='/api/signoffs'&&method==='POST'){
    const d=await bodyJson(request); if(!d.project_id||!d.entity_type||!d.entity_id||!d.signer_name)return json({error:'Project, entity and signer are required'},400);
    const sid=id('sig'); await env.DB.prepare(`INSERT INTO signoffs(id,project_id,entity_type,entity_id,signoff_type,signer_name,signer_company,signer_role,signature_data,declaration,signed_at,created_by,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(sid,d.project_id,clean(d.entity_type,40),clean(d.entity_id,120),clean(d.signoff_type||'acceptance',60),clean(d.signer_name,160),nullable(d.signer_company,160),nullable(d.signer_role,120),nullable(d.signature_data,250000),nullable(d.declaration,2000),now(),user.id,now()).run();
    if(d.entity_type==='inspection') await env.DB.prepare(`UPDATE inspections SET signed_status='signed',signed_by=?,signed_at=?,updated_at=? WHERE id=?`).bind(clean(d.signer_name,160),now(),now(),d.entity_id).run();
    ctx.waitUntil(audit(env,user.id,'signoff','signoff',sid,{entity_type:d.entity_type,entity_id:d.entity_id}).catch(()=>{})); return json({id:sid},201);
  }

  if(path==='/api/documents'&&method==='GET'){
    const pid=url.searchParams.get('project_id');if(!pid)return json({error:'project_id required'},400);
    const rows=await all(env.DB.prepare(`SELECT d.*,f.file_name,f.content_type,f.size_bytes,s.code system_code,e.tag equipment_tag FROM document_register d LEFT JOIN files f ON f.id=d.object_file_id LEFT JOIN systems s ON s.id=d.system_id LEFT JOIN equipment e ON e.id=d.equipment_id WHERE d.project_id=? ORDER BY d.doc_type,d.title,d.revision DESC`).bind(pid));return json({documents:rows});
  }
  if(path==='/api/documents'&&method==='POST'){
    const d=await bodyJson(request);if(!d.project_id||!d.title||!d.doc_type)return json({error:'Project, title and document type are required'},400);
    const did=id('doc'); await env.DB.prepare(`INSERT INTO document_register(id,project_id,document_no,title,doc_type,revision,status,object_file_id,equipment_id,system_id,supersedes_id,uploaded_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?, ?,?,?,?,?,?,?)`).bind(did,d.project_id,nullable(d.document_no,100),clean(d.title,220),clean(d.doc_type,80),nullable(d.revision,40),clean(d.status||'current',40),nullable(d.object_file_id,120),nullable(d.equipment_id,120),nullable(d.system_id,120),nullable(d.supersedes_id,120),user.id,now(),now()).run();
    return json({id:did},201);
  }

  if(path==='/api/copilot/project'&&method==='GET'){
    const pid=url.searchParams.get('project_id');if(!pid)return json({error:'project_id required'},400);
    const [p,eq,ins,defs,pts,wit,turn,docs]=await Promise.all([
      env.DB.prepare(`SELECT * FROM projects WHERE id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='complete' THEN 1 ELSE 0 END) complete,SUM(CASE WHEN status='blocked' THEN 1 ELSE 0 END) blocked FROM equipment WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='approved' THEN 1 ELSE 0 END) approved,SUM(CASE WHEN status IN ('submitted','witness_required') THEN 1 ELSE 0 END) awaiting FROM inspections WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status NOT IN ('closed','verified') THEN 1 ELSE 0 END) open,SUM(CASE WHEN category='A' AND status NOT IN ('closed','verified') THEN 1 ELSE 0 END) cat_a FROM defects WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN overall_status='pass' THEN 1 ELSE 0 END) passed,SUM(CASE WHEN overall_status='fail' THEN 1 ELSE 0 END) failed FROM bms_points WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='requested' THEN 1 ELSE 0 END) pending FROM witness_requests WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,ROUND(AVG(readiness_percent)) avg_readiness,SUM(CASE WHEN readiness_percent=100 THEN 1 ELSE 0 END) ready FROM turnover_packages WHERE project_id=?`).bind(pid).first(),
      env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='current' THEN 1 ELSE 0 END) current_docs FROM document_register WHERE project_id=?`).bind(pid).first()
    ]);
    if(!p)return json({error:'Project not found'},404);
    const recommendations=[];
    const push=(priority,title,why,route)=>recommendations.push({priority,title,why,route});
    if(Number(defs?.cat_a||0)>0) push(1,'Clear Category A punch items',`${defs.cat_a} Category A item(s) are blocking readiness.`,'defects');
    if(Number(pts?.failed||0)>0) push(2,'Recover failed BMS points',`${pts.failed} BMS I/O point(s) are currently failed.`,'points');
    const unproven=Math.max(0,Number(pts?.total||0)-Number(pts?.passed||0)-Number(pts?.failed||0)); if(unproven>0)push(3,'Drive BMS I/O completion',`${unproven} point(s) remain unproven.`,'points');
    const unapproved=Math.max(0,Number(ins?.total||0)-Number(ins?.approved||0));if(unapproved>0)push(4,'Approve commissioning tests',`${unapproved} inspection/test record(s) are not approved.`,'inspections');
    if(Number(wit?.pending||0)>0)push(5,'Close witness hold points',`${wit.pending} witness request(s) are pending.`,'witness');
    if(Number(turn?.total||0)===0)push(6,'Create turnover packages','No turnover package exists yet.','turnover');
    else if(Number(turn?.avg_readiness||0)<100)push(6,'Complete turnover evidence',`Average turnover readiness is ${Math.round(Number(turn?.avg_readiness||0))}%.`,'turnover');
    if(Number(docs?.total||0)<3)push(7,'Build controlled document register','Closeout document register is still sparse.','closeout');
    const eqPct=Number(eq?.total||0)?Math.round(Number(eq?.complete||0)*100/Number(eq.total)):0;
    const score=Math.max(0,Math.min(100,100-(Number(defs?.cat_a||0)>0?25:0)-(Number(pts?.failed||0)>0?20:0)-Math.min(20,unapproved*2)-Math.min(15,Number(wit?.pending||0)*5)-Math.max(0,15-Math.round(Number(turn?.avg_readiness||0)*0.15))));
    const forecast=score>=90?'READY / FINAL CLOSEOUT':score>=75?'NEAR READY':score>=50?'COMMISSIONING AT RISK':'NOT READY';
    return json({project:p,score,forecast,equipment_percent:eqPct,recommendations:recommendations.sort((a,b)=>a.priority-b.priority),metrics:{equipment:eq,tests:ins,defects:defs,bms:pts,witness:wit,turnover:turn,documents:docs},generated_at:now()});
  }

  if(path==='/api/client-share'&&method==='POST'){
    if(!roleAllowed(user,['admin','pm','engineer']))return json({error:'PM/Engineer permission required'},403);
    const d=await bodyJson(request);if(!d.project_id)return json({error:'project_id required'},400);
    const token=crypto.randomUUID().replaceAll('-','')+crypto.randomUUID().replaceAll('-','');const hash=await sha256(token);const sid=id('shr');
    const expires=d.expires_days?new Date(Date.now()+Math.max(1,Math.min(90,Number(d.expires_days)))*86400000).toISOString():null;
    await env.DB.prepare(`INSERT INTO client_share_links(id,project_id,token_hash,label,expires_at,created_by,created_at) VALUES(?,?,?,?,?,?,?)`).bind(sid,d.project_id,hash,nullable(d.label,120),expires,user.id,now()).run();
    return json({id:sid,url:`${url.origin}/share/${token}`,expires_at:expires},201);
  }

  if(path==='/api/handover/dossier'&&method==='GET'){
    const pid=url.searchParams.get('project_id');if(!pid)return json({error:'project_id required'},400);
    const project=await env.DB.prepare(`SELECT * FROM projects WHERE id=?`).bind(pid).first();if(!project)return json({error:'Project not found'},404);
    const [equipment,systems,inspections,defects,points,turnover,docs,signoffs]=await Promise.all([
      all(env.DB.prepare(`SELECT * FROM equipment WHERE project_id=? ORDER BY system,tag`).bind(pid)),all(env.DB.prepare(`SELECT * FROM systems WHERE project_id=? ORDER BY code`).bind(pid)),all(env.DB.prepare(`SELECT i.*,t.name template_name,e.tag equipment_tag FROM inspections i LEFT JOIN templates t ON t.id=i.template_id LEFT JOIN equipment e ON e.id=i.equipment_id WHERE i.project_id=? ORDER BY i.updated_at DESC`).bind(pid)),all(env.DB.prepare(`SELECT d.*,e.tag equipment_tag FROM defects d LEFT JOIN equipment e ON e.id=d.equipment_id WHERE d.project_id=? ORDER BY d.category,d.status,d.created_at`).bind(pid)),all(env.DB.prepare(`SELECT * FROM bms_points WHERE project_id=? ORDER BY controller,point_name`).bind(pid)),all(env.DB.prepare(`SELECT t.*,s.code system_code FROM turnover_packages t LEFT JOIN systems s ON s.id=t.system_id WHERE t.project_id=? ORDER BY s.code,t.name`).bind(pid)),all(env.DB.prepare(`SELECT d.*,f.file_name FROM document_register d LEFT JOIN files f ON f.id=d.object_file_id WHERE d.project_id=? ORDER BY d.doc_type,d.title`).bind(pid)),all(env.DB.prepare(`SELECT * FROM signoffs WHERE project_id=? ORDER BY signed_at DESC`).bind(pid))]);
    const h=(v='')=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
    const pc=(a,b)=>b?Math.round(a*100/b):0; const eqDone=equipment.filter(x=>x.status==='complete').length, testDone=inspections.filter(x=>x.status==='approved').length, openA=defects.filter(x=>x.category==='A'&&!['closed','verified'].includes(x.status)).length, ptsPass=points.filter(x=>x.overall_status==='pass').length;
    const rows=(arr,fn)=>arr.length?arr.map(fn).join(''):'<tr><td colspan="6">No records</td></tr>';
    const html=`<!doctype html><html><head><meta charset="utf-8"><title>${h(project.code)} Qx Handover Dossier</title><style>body{font-family:Arial,sans-serif;color:#182235;margin:32px}header{display:flex;justify-content:space-between;border-bottom:3px solid #1f91ff;padding-bottom:20px}h1{margin:0}.kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin:24px 0}.k{border:1px solid #ccd5e2;padding:12px;border-radius:8px}.k b{font-size:24px;display:block}table{width:100%;border-collapse:collapse;margin:12px 0 28px;font-size:12px}th,td{border:1px solid #d9e0ea;padding:7px;text-align:left;vertical-align:top}th{background:#eef4fb}h2{margin-top:30px}.warn{color:#b00020}.ok{color:#087b47}@media print{button{display:none}body{margin:12mm}.page{page-break-before:always}}</style></head><body><header><div><h1>Elliot Qx — Handover Dossier</h1><p><b>${h(project.code)} — ${h(project.name)}</b><br>${h(project.client||'')} • ${h(project.site_address||'')}</p></div><div><button onclick="print()">Print / Save PDF</button><p>Generated ${h(now())}</p></div></header><div class="kpis"><div class="k"><span>Assets complete</span><b>${pc(eqDone,equipment.length)}%</b></div><div class="k"><span>Tests approved</span><b>${pc(testDone,inspections.length)}%</b></div><div class="k"><span>BMS proven</span><b>${pc(ptsPass,points.length)}%</b></div><div class="k"><span>Cat A open</span><b class="${openA?'warn':'ok'}">${openA}</b></div><div class="k"><span>Signoffs</span><b>${signoffs.length}</b></div></div><h2>System Completion</h2><table><tr><th>System</th><th>Name</th><th>Discipline</th><th>Status</th><th>Target</th></tr>${rows(systems,x=>`<tr><td>${h(x.code)}</td><td>${h(x.name)}</td><td>${h(x.discipline)}</td><td>${h(x.status)}</td><td>${h(x.target_date||'')}</td></tr>`)}</table><h2>Equipment Register</h2><table><tr><th>Tag</th><th>Name</th><th>System</th><th>Location</th><th>Status</th></tr>${rows(equipment,x=>`<tr><td>${h(x.tag)}</td><td>${h(x.name)}</td><td>${h(x.system||'')}</td><td>${h([x.building,x.level,x.area].filter(Boolean).join(' / '))}</td><td>${h(x.status)}</td></tr>`)}</table><div class="page"></div><h2>QA / Commissioning Records</h2><table><tr><th>Test</th><th>Asset</th><th>Template</th><th>Status</th><th>Signed</th></tr>${rows(inspections,x=>`<tr><td>${h(x.title)}</td><td>${h(x.equipment_tag||'')}</td><td>${h(x.template_name||'')}</td><td>${h(x.status)}</td><td>${h(x.signed_status||'')}</td></tr>`)}</table><h2>Defect / Punch Register</h2><table><tr><th>Cat</th><th>Title</th><th>Asset</th><th>Status</th><th>Severity</th></tr>${rows(defects,x=>`<tr><td>${h(x.category)}</td><td>${h(x.title)}</td><td>${h(x.equipment_tag||'')}</td><td>${h(x.status)}</td><td>${h(x.severity)}</td></tr>`)}</table><h2>BMS I/O Summary</h2><table><tr><th>Controller</th><th>Point</th><th>Type</th><th>Address</th><th>Status</th></tr>${rows(points,x=>`<tr><td>${h(x.controller||'')}</td><td>${h(x.point_name)}</td><td>${h(x.point_type)}</td><td>${h(x.io_address||'')}</td><td>${h(x.overall_status)}</td></tr>`)}</table><div class="page"></div><h2>Turnover Packages</h2><table><tr><th>System</th><th>Package</th><th>Status</th><th>Readiness</th></tr>${rows(turnover,x=>`<tr><td>${h(x.system_code||'')}</td><td>${h(x.name)}</td><td>${h(x.status)}</td><td>${h(x.readiness_percent)}%</td></tr>`)}</table><h2>Controlled Documents</h2><table><tr><th>No.</th><th>Title</th><th>Type</th><th>Rev</th><th>Status</th></tr>${rows(docs,x=>`<tr><td>${h(x.document_no||'')}</td><td>${h(x.title)}</td><td>${h(x.doc_type)}</td><td>${h(x.revision||'')}</td><td>${h(x.status)}</td></tr>`)}</table><h2>Digital Signoffs</h2><table><tr><th>Type</th><th>Signer</th><th>Company</th><th>Role</th><th>Date</th></tr>${rows(signoffs,x=>`<tr><td>${h(x.signoff_type)}</td><td>${h(x.signer_name)}</td><td>${h(x.signer_company||'')}</td><td>${h(x.signer_role||'')}</td><td>${h(x.signed_at)}</td></tr>`)}</table></body></html>`;
    return new Response(html,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}});
  }

  return json({ error:'API route not found' },404);
}

export default {
  async fetch(request, env, ctx) {
    const url = new URL(request.url);
    try {
      if (url.pathname.startsWith('/share/')) {
        const token=url.pathname.split('/')[2]||''; if(!token)return new Response('Invalid share link',{status:404});
        const row=await env.DB.prepare(`SELECT c.*,p.code,p.name,p.client,p.site_address FROM client_share_links c JOIN projects p ON p.id=c.project_id WHERE c.token_hash=? AND c.revoked_at IS NULL AND (c.expires_at IS NULL OR c.expires_at>?)`).bind(await sha256(token),now()).first();
        if(!row)return new Response('Share link expired or invalid',{status:404});
        const [eq,defs,pts,turn]=await Promise.all([env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN status='complete' THEN 1 ELSE 0 END) complete FROM equipment WHERE project_id=?`).bind(row.project_id).first(),env.DB.prepare(`SELECT SUM(CASE WHEN status NOT IN ('closed','verified') THEN 1 ELSE 0 END) open,SUM(CASE WHEN category='A' AND status NOT IN ('closed','verified') THEN 1 ELSE 0 END) cat_a FROM defects WHERE project_id=?`).bind(row.project_id).first(),env.DB.prepare(`SELECT COUNT(*) total,SUM(CASE WHEN overall_status='pass' THEN 1 ELSE 0 END) passed FROM bms_points WHERE project_id=?`).bind(row.project_id).first(),env.DB.prepare(`SELECT ROUND(AVG(readiness_percent)) r FROM turnover_packages WHERE project_id=?`).bind(row.project_id).first()]);
        const pct=(a,b)=>b?Math.round(Number(a||0)*100/Number(b)):0; const safe=v=>String(v??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
        return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${safe(row.code)} Qx Status</title><style>body{font-family:Arial;background:#071426;color:#eaf2ff;margin:0;padding:36px}.wrap{max-width:1000px;margin:auto}.card{background:#0e2037;border:1px solid #29415f;border-radius:16px;padding:22px}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-top:20px}.n{font-size:36px;font-weight:800}.muted{color:#9eb0c7}@media(max-width:700px){.grid{grid-template-columns:1fr 1fr}}</style></head><body><div class="wrap"><p class="muted">ELLIOT QX • LIVE CLIENT VIEW</p><h1>${safe(row.code)} — ${safe(row.name)}</h1><p>${safe(row.client||'')} • ${safe(row.site_address||'')}</p><div class="grid"><div class="card"><span class="muted">Assets complete</span><div class="n">${pct(eq?.complete,eq?.total)}%</div></div><div class="card"><span class="muted">BMS proven</span><div class="n">${pct(pts?.passed,pts?.total)}%</div></div><div class="card"><span class="muted">Open defects</span><div class="n">${Number(defs?.open||0)}</div><small>Category A: ${Number(defs?.cat_a||0)}</small></div><div class="card"><span class="muted">Turnover readiness</span><div class="n">${Math.round(Number(turn?.r||0))}%</div></div></div><p class="muted" style="margin-top:24px">Read-only live status generated by Elliot Qx. Detailed commissioning records remain controlled within the project workspace.</p></div></body></html>`,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}});
      }
      if (url.pathname.startsWith('/api/')) return await handleApi(request, env, ctx);
      return serveStatic(url.pathname);
    } catch (error) {
      console.error(JSON.stringify({level:'error',message:error?.message||String(error),path:url.pathname}));
      return url.pathname.startsWith('/api/') ? json({error:'Server error',detail:env.ENVIRONMENT==='development'?String(error?.message||error):undefined},500) : new Response('Application error',{status:500});
    }
  }
};
