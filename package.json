{
  "name": "elliot-qx-commissioning-os",
  "version": "8.1.0",
  "private": true,
  "type": "module",
  "description": "Elliot Qx V8.1 - template library fix commissioning OS",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler deploy --dry-run",
    "db:local": "wrangler d1 migrations apply elliot-qx-db --local",
    "db:remote": "wrangler d1 migrations apply elliot-qx-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "wrangler": "latest"
  }
}
